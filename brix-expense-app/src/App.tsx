export function App() {
  return (
    <main className="page">
      <div className="card">
        <h1>Brix Expense</h1>
        <p className="muted">Infrastructure placeholder — real app lands next.</p>
        <p className="small">
          This page exists to verify the sub-app build pipeline deploys cleanly
          alongside the billing tool and Margin Minder. Once the deploy is green
          this is replaced by the receipt + purchase-request flows.
        </p>
      </div>
    </main>
  );
}
