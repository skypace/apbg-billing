# Local Netlify folder
.netlify
.claude/*
!.claude/skills/
.claude/skills/**/__pycache__/

# Vite app build artifacts
app/node_modules
app/dist
app-expense/node_modules
app-expense/dist
app-expense/*.tsbuildinfo
app-expense/package-lock.json

# NOTE: public/sales-next/ and public/expense/ are intentionally checked
# in for now because Netlify's deploy doesn't appear to be running our
# build command from netlify.toml. Until that's reconfigured, run the
# matching `npm run build --prefix <app>` locally before committing
# changes, and commit the updated public/ output alongside.
