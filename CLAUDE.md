> **Architecture handbook:** This repo is part of a multi-repo system documented in [`activespacescience/Skilliosis_Mytosis_Architecture`](https://github.com/activespacescience/Skilliosis_Mytosis_Architecture/blob/main/ARCHITECTURE.md). When work in this repo touches architecture (new external service, new cross-repo dependency, new deploy target, new MCP/connector, new env-var category, new/renamed/archived repo, new gateway proxy route), update `ARCHITECTURE.md` in the same change via the GitHub MCP (`asm-mcp-tools.netlify.app/github`, tool `github_create_or_update_file`). Brand-new projects should be planned in that repo's `projects/<project-name>/` folder before any code is written here.

---

# apbg-billing — Claude orientation

## TL;DR

This repo hosts **five surfaces** that share one Netlify deploy at `apbg-billing.netlify.app`, all fronted through the parent gateway at `alamedapointbg.com`:

1. **3rd-Party Billing** — the original AI vendor-bill processing tool (AP)
2. **BRIX Margin Control** — React/Vite SPA for sales / margin / customer analytics (`app/`, builds to `public/sales-next/`, surfaced at `/margin/` on the gateway)
3. **Brixpense** — React/Vite SPA for internal expense + purchase requests; expenses auto-approve, PRs route to an approver (chosen by the submitter) who logs in to the same Supabase auth and approves in-app (`app-expense/`, builds to `public/expense/`, surfaced at `/expense/`)
4. **Sync orchestration manifest** — cross-repo lint contract for the `ops.*` schema (`architecture/sync-manifest.json`)
5. **Interactive user guide** — markdown source in `docs/`, viewer in `public/docs/`, surfaced at `/margin/docs/margin-control/`
6. **APBG Master Handbook** — company-wide user guide (every app) + Operations SOP Manual. Chapters in `docs/handbook/*.md` + `manifest.json` (chapter registry: owners, `last_reviewed`, source docs), viewer at `public/docs/handbook/index.html` (served at `/docs/handbook/`, via gateway `/margin/docs/handbook/`). Freshness: Master Control → APBG Handbook → **Run sweep** (`netlify/functions/handbook-sweep.mjs`, superadmin; compares source-file commit dates vs `last_reviewed`, needs `GITHUB_TOKEN` env for the private repos).

Plus **two Supabase edge functions** whose source lives here but which deploy separately to the Supabase project:

- `sync-qbo` (v35) — QBO → Supabase reads
- `push-qbo-item` (v2) — Supabase → QBO writebacks (Item.Active + Category ParentRef)

## What's deployed today

| Surface | Lives in | URL |
|---|---|---|
| AP / 3rd-Party Billing | `public/*.html` + `netlify/functions/` | `alamedapointbg.com/billing/` |
| BRIX Margin Control (v0.9.27) | `app/` → built into `public/sales-next/` | `alamedapointbg.com/margin/` |
| Brixpense (v0.1.0) | `app-expense/` → built into `public/expense/` | `alamedapointbg.com/expense/` |
| Sub-Distributor Portal (v0.1.0) | `app-distributor/` → built into `public/distributor/` | `alamedapointbg.com/distributor/` |
| User Guide | `docs/margin-control/` + viewer in `public/docs/margin-control/` | `alamedapointbg.com/margin/docs/margin-control/` |
| Master Control admin panel (incl. ResQ Sync controls) | `public/control.html` | `alamedapointbg.com/control` |
| MCP Servers app (MCP status + both reconnect paths) | `public/mcp.html` | `alamedapointbg.com/mcp` |
| QBO + Service Fusion OAuth setup | `public/setup.html` | `alamedapointbg.com/billing/setup.html` |

> **ResQ ↔ Service Fusion sync moved out (2026-06-28).** The old in-repo sync (`public/sync.html` + `resq-sf-sync*.mjs` + the 5-min cron) was decommissioned and replaced by the focused edge-function sync in **`skypace/apbg-resq-sync`** (Supabase, 15-min pg_cron, state-machine idempotency). Operators now manage it from **Master Control → ResQ Sync** (`control.html`), which calls `netlify/functions/resq-sync-control.mjs` → `ops.resq_sync_*` RPCs. Linked-customer mapping (`ops.sync_customers`) stays here (shared by both repos + `expense-to-bill`).

## Where to look first

1. **[`README.md`](README.md)** — repo map.
2. **[`architecture/MARGIN-CONTROL.md`](architecture/MARGIN-CONTROL.md)** — Margin Control architecture.
3. **[`architecture/BRIXPENSE.md`](architecture/BRIXPENSE.md)** — Brixpense architecture.
4. **[`architecture/PRODUCTION.md`](architecture/PRODUCTION.md)** — formula → BOM → work order → POs → production order, and the accounting that settles it.
5. **[`architecture/README.md`](architecture/README.md)** — sync orchestration manifest.
6. **[`docs/margin-control/user-guide.md`](docs/margin-control/user-guide.md)** — end-user guide for BRIX.
7. **[`activespacescience/Skilliosis_Mytosis_Architecture/ARCHITECTURE.md`](https://github.com/activespacescience/Skilliosis_Mytosis_Architecture/blob/main/ARCHITECTURE.md)** — master cross-repo handbook.

## Tech stack at a glance

- **AP tool:** Vanilla HTML + JS in `public/`, served as-is by Netlify.
- **BRIX Margin Control:** React 18 + Vite 5 + TypeScript 5 + MUI v6 + MUI X v7 Pro.
- **Brixpense:** React 18 + Vite 5 + TypeScript 5 + Radix UI + shadcn-style wrappers + Tailwind 3 (dark navy glass-morphism theme).
- **Backend (Netlify Functions):** ESM `.mjs` files. Bill processing, OAuth callbacks, expense requests, OCR (Claude API), QBO bill creation, ResQ-sync control proxy (`resq-sync-control.mjs`). (The ResQ↔SF sync engine itself moved to `skypace/apbg-resq-sync`.)
- **Backend (Supabase Edge Functions):** Deno runtime. `sync-qbo` and `push-qbo-item`.
- **Data:** Supabase `ops.*` schema. Brixpense uses `ops.expense_requests/_attachments/_approvals/_settings`.

## Build pipeline (`netlify.toml`)

```
git push main
  └─▶ Netlify build
       1. node architecture/lint-manifest.mjs       ← sync-manifest gate
       2. mkdir -p public/docs && cp -r docs/. public/docs/
       3. npm install --prefix app                  ← Margin Control deps
       4. npm run build --prefix app                ← tsc -b && vite build
       5. npm install --prefix app-expense          ← Brixpense deps
       6. npm run build --prefix app-expense        ← tsc + vite build
       Publishes public/ as static + netlify/functions/ as Lambdas
```

## Brixpense approval model (live)

Final design after two passes:

| Request type | Flow |
|---|---|
| `expense` | Auto-approved on submit (draft→approved). No email. No manager-approval workflow. Recorded in `expense_approvals` as `decided_by='system (auto-approve)'`. **Whether Submit also posts to QuickBooks depends on `as_bill` (2026-08-14):** unpaid **Bill** (`as_bill=true`) → approve only, nothing auto-loads into QBO — a human separately clicks "Post to QuickBooks" (PendingList / SFExpenses / the post-submit success screen) whenever it's actually ready, which calls `expense-request-link-bill`. Already-**paid** (posts as a QBO **Purchase**) → Submit approves AND posts in the same click (`ExpenseForm.tsx` sees `mode:'purchase'` from `expense-request-notify` and immediately calls `expense-request-link-bill`) — a failed auto-post falls back to the same manual button + failure-notify path as a retry. |
| `purchase_request` | Submitter picks an approver from `expense_settings.manager_emails` (required). `expense-request-notify` validates the choice, flips status to `pending`, and sends a notification email pointing at `/expense/queue`. The approver logs into Supabase (same auth as alamedapointbg) and approves in-app at `/expense/review/:id`. `expense-request-decide` requires Bearer JWT, checks caller != submitter and `lower(caller.email) == lower(manager_email)`. RLS enforces the same. Fulfillment ("Log Receipt") already required a manual click and is unaffected by the 2026-08-14 gate. |

No magic-link tokens. No anonymous approval path. The email is a notification, not an authorization.

## Credentials

- **Supabase URL:** `https://gfsdpwiqzshhexkofiif.supabase.co`
- **Supabase anon key:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imdmc2Rwd2lxenNoaGV4a29maWlmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzU1OTUyMzcsImV4cCI6MjA5MTE3MTIzN30.AygnPJwQ5NfIeKwPtkO6tgVYmkV3MAxL1lMFwN9HPnY`
- **Schema:** `ops` (use `Accept-Profile: ops` header or Supabase client `{ db: { schema: 'ops' } }`)
- **QBO Realm ID:** `9130352144155116` — **one shared QuickBooks company, but MULTIPLE distinct Intuit apps connect to it.** Each surface (apbg-billing, pacerfinance MCP, melt-dashboard's primary + secondary app, etc.) is its own Intuit Developer app with its **own `client_id`/app id and its own registered redirect URI**. Same realm ≠ same app. Do NOT assume a redirect URI you don't recognize is an orphan of "the" app — it likely belongs to another app pointed at this same realm. Known QBO redirect URIs in use:
  - apbg-billing (AP tool): `https://apbg-billing.netlify.app/.netlify/functions/oauth-callback` (`public/setup.html`)
  - pacerfinance QBO MCP: `https://pacerfinance.netlify.app/qbo-oauth-callback` (`pacerfinance/src/shared/oauth.ts`)
  - melt-dashboard: `oauth-callback.mjs` on `melt-dashboard.netlify.app`
  - `https://apbg-finance.netlify.app/callback` — a separate Intuit app's callback pointed at the same realm (a distinct app, not an orphaned billing redirect).
- **MUI X Pro license:** required at build time via `VITE_MUI_X_LICENSE` env var on Netlify.
- **Anthropic API key:** required for receipt OCR — the AP tool's PDF drop (`process-inbound`), Brixpense receipt uploads (`expense-ocr`) and the AP inbox (`bill-email-process-background`). Env var `ANTHROPIC_API_KEY`.
- **Email provider:** `RESEND_API_KEY` or `SENDGRID_API_KEY` (Brixpense notification emails only).
- **Netlify site:** linked to this repo, auto-deploys on push to `main`. Publish dir: `public/`.

## Service Fusion OAuth (READ before touching any SF connect flow)

There are **three separate Service Fusion Connected Apps** against the one SF account, each with its own `client_id`, its own registered redirect URI, and its own token row. Mixing them up is how you get `invalid redirect URL` (authorize step) or `invalid_client` (token-exchange step). This map cost a multi-hour scavenger hunt on 2026-07-24 because it was written nowhere — do not delete it.

| App | client_id | Registered redirect URI | Token row | Used by |
|---|---|---|---|---|
| **Billing / Brixpense** | `TNpu3bVz9XAIgey_7e` | `https://gfsdpwiqzshhexkofiif.supabase.co/functions/v1/sf-oauth-callback` (Supabase edge fn) | `ops.sf_token_cache` (id=1) | `sync-sf` (job mirror), `sf-receipt-sync` (Brixpense expenses), apbg-billing SF reads |
| **ResQ sync** | `PP8LobxMYi-SZFhI1t` (`ops.resq_sync_config.sf_app_client_id`) | `https://gfsdpwiqzshhexkofiif.supabase.co/functions/v1/sf-connect` | `ops.resq_sf_token_cache` | `skypace/apbg-resq-sync` only |
| **OLD ResQ updater (dead)** | `f8cqiTHnjoFS-QMeuc` ("MEUC") | (legacy Netlify redirect) | — | nothing — retired |

**Re-authing the billing app** (the one Brixpense expenses depend on): sign into SF in a browser, then open
`https://api.servicefusion.com/oauth/authorize?response_type=code&client_id=TNpu3bVz9XAIgey_7e&redirect_uri=https%3A%2F%2Fgfsdpwiqzshhexkofiif.supabase.co%2Ffunctions%2Fv1%2Fsf-oauth-callback`
→ approve → the Supabase `sf-oauth-callback` exchanges the code (using Supabase env `SF_CLIENT_ID` + `SF_CLIENT_SECRET`, which **must match `TNpu3bVz9XAIgey_7e`**) and writes tokens to `ops.sf_token_cache`. `setup.html` + `control.html` reconnect buttons now point here (were wrongly hardcoded to the dead MEUC id + Netlify redirect until 2026-07-24).

**Gotchas:**
- `invalid redirect URL` = the redirect in your authorize link isn't registered on that app. `invalid_client` = the **Supabase env** `SF_CLIENT_ID`/`SF_CLIENT_SECRET` don't match the app whose code you're exchanging.
- **QBO app-sharing trap (2026-07-24):** Intuit invalidates prior refresh tokens when the same app+realm gets re-authorized. pacerfinance's QBO was aligned to the billing Netlify app (`ABCV3BJb…`) on 7/15 — so re-authorizing either surface kills the other's refresh token. This is the likely cause of the serial QBO token deaths. Until pacerfinance gets its OWN Intuit app, expect its QBO token to die whenever the billing app is reconnected (and vice versa); the `qbo_netlify_chain` health check + break-glass fallback in `qbo-helpers.mjs` limit the blast to an email instead of an outage. **Master Control → Connections & Reconnect** (`control.html` + `netlify/functions/connections.mjs`) lists every SF/QBO app with its client id, its own health light, and the correct authorize link — use it instead of hunting for reconnect URLs.
- SF access tokens last ~1h; the refresh token auto-rotates on each refresh. Token refresh is lease-guarded (`fn_sf_token_claim_refresh`) so concurrent functions don't race-rotate it.
- SF rate-limits aggressively (429). SF-hitting crons are deliberately throttled: `sf-job-sync` daily 09:00 UTC, `sf-receipt-sync-fresh` 3×/day (03:00/15:00/21:00 UTC). Don't add high-frequency SF pollers or parallel SF scans — that's what caused the 2026-06/07 expense-sync 429 outage.
- **`sf-receipt-sync-crawl` is DISABLED (2026-08-04)** — `cron.job.active=false`, not deleted. Its only job was the historical catch-up after the epoch-gate outage, and that is finished. Left running it would keep dredging up pre-outage expenses that were already keyed into QBO by hand, which is a duplicate-bill generator, not a safety net. Re-enable only for a deliberate, supervised backfill (`select cron.alter_job(jobid, active := true) from cron.job where jobname='sf-receipt-sync-crawl'`) and check `ops.fn_sf_expense_duplicates()` before the next autopost.
- **The expense pipeline is event-driven now.** Invoiced-status email → `pullJobExpenses` → `sf-receipt-sync?landJob=` lands the receipt in seconds. `sf-receipt-sync-fresh` stays as the backstop for the one case the hook cannot see: **an expense added to a job that is ALREADY invoiced** (no status change → no email → no hook), plus any email that gets filtered or a status template that quietly stops firing.
- The pacerfinance MCP (`pacerfinance.netlify.app/servicefusion`) is a **separate** SF integration; its token is not `ops.sf_token_cache`. Under 429 pressure its `sf_get_job` silently drops the `expenses` array (returns the job with no expenses) — don't trust it for bulk expense reads.
- **SF returns the UNIX EPOCH, not null, for empty date fields (2026-08-04).** An SF expense that has never been EDITED comes back with `updated_at: "1970-01-01T00:00:00+00:00"`, and `date` is epoch unless a human typed one. `sf-receipt-sync`'s date gate read `ex.updated_at || ex.created_at`, so epoch won the `||`, tested as 1970 < `SF_SWEEP_START_DATE`, and **every never-edited expense was silently skipped for two months** — 10 landed since the 2026-06-03 cutoff, every sweep reported `drafts: 0`, and nothing went red. Never trust an SF date field without an epoch guard (`realDateMs()` / `newestRealDate()`).
- **ALWAYS pass an explicit `sort` on SF `GET /jobs`.** Without one SF falls into a query plan that hangs 20s–2min+ on our ~22k-row jobs table (brix-order learned this in its session 1.18; `sf-receipt-sync`'s pageCount probe re-learned it the hard way — it was eating ~100s of the 150s edge wall by itself and killing the sweep before it could log). Also: the page-size param is **`per-page`** (hyphen). SF silently ignores `per_page` and uses its default page size.
- **Supabase edge functions are hard-killed at 150s with no chance to log.** Any budgeted loop must start its clock before the FIRST network call, check the deadline per unit of work (not per batch), and cap individual fetches with `AbortSignal.timeout`. A run killed mid-flight writes nothing to `ops.sync_log`, and any cursor that resumes from "the last success row" then never advances — that is how the daily receipt crawl sat dead for 9 days while its cron fired normally.
- **~97% of SF "expenses" are blank rows** (no vendor, amount, notes, or category) — SF creates them on ordinary delivery jobs. `isEmptyExpense()` drops them. A blank vendor WITH an amount still lands.
- **If you add a filter to `landJob`, add a counter next to it.** Every sweep logs `expensesSeen / skippedEmpty / skippedByDate / drafts / alreadyLanded / elapsedMs`, and `ops.fn_sf_receipt_coverage()` goes RED when informative expenses are seen but none land. `drafts: 0` alone is what let the epoch bug hide — it reads the same whether SF had nothing new or the gate was discarding everything.
- **Invoiced-status emails to `sf-status@alamedapointbg.com` pull that job's expenses immediately** (`vendor-email-intake.mjs` → `pullJobExpenses` → `sf-receipt-sync?landJob=`). SF has no webhooks; its notification email IS the event. The crons are the safety net for jobs whose email never arrives, not the primary path.
- **SF receipt auto-attach is OFF — the switch is `SF_FETCH_RECEIPTS` in the SUPABASE edge-function env (2026-08-25).** SF expenses land DATA-ONLY (vendor/amount/date/job + the SF# deep link); the bill document is attached by a human in Brixpense (the expense edit form has attach controls). Why: the receipt file only exists on the admin-portal job page, and that cookie scrape was the flakiest link in the pipeline (intermittent 20s+ SF-side hangs). Set `SF_FETCH_RECEIPTS=1` in the Supabase project env (NOT Netlify — `sf-receipt-sync` is an edge function) to restore auto-attach with no deploy; sweep results log `fetchReceipts` in `ops.sync_log` so you can tell which mode a run used. The `?receipts=<job#>` diagnostic always resolves receipt URLs regardless of the flag.
- **Expense-sync scope is `status=Invoiced` jobs only** (`sf-receipt-sync`). An SF expense on a job that never reaches Invoiced never lands in Brixpense — the ops workflow must close jobs out to Invoiced (the ResQ 🔒 Close flow does). Fixing a blank `purchased_from` in SF after a draft landed creates a NEW draft (the dedup key includes the vendor) — the old blank draft stays behind as an orphan; archive it from the SF Expenses tab.
- **SF expense rows are staff-only + archivable (2026-07-24).** RLS `expense_requests_select_sf` requires `ops.fn_is_staff()` (gateway `user_metadata.role` superadmin/admin) — the old policy leaked internal vendors/amounts to every authenticated login on the shared project (brix-order customers, boelter, thehubdesign). Archive = `archived_at`/`archived_by` soft-hide (row + `sf_expense_id` dedup kept so the sync can't re-land it); autopost auto-archives pre-cutoff (`SF_AUTOPOST_MIN_RECEIPT_DATE`) historical drafts on each daily run — QBO is the source of truth for those, edit them there. QBO-error rows retry automatically on every daily run (the `autopost_notified_at` stamp only dedups the alert email, not the retry).

## What's actively being worked on

As of 2026-05-12:

- **BRIX Margin Control polish + features.** Sub-screens maturing: Customers master, Items master, Categories, P&L Alignment editor, Sales Reps.
- **Brixpense post-launch polish.** Approval model finalized: expense=auto, PR=in-app authed approval. Active gaps: mobile sidebar, admin settings UI for `ops.expense_settings`, entity → department → COGS cascade on the form, edit-flow data load on `/expense/edit/:id`.
- **QBO writeback expansion.** `push-qbo-item` + Brixpense `expense-request-link-bill` cover Item.Active, Category ParentRef, and AccountBasedExpenseLineDetail bill creation.

## Business rules (preserved verbatim)

### Entity split

- `entity = 'brix'` or `'AS'` = Alameda Soda / Brix Beverage (CA S-corp)
- `entity = 'freeflow'` or `'FF'` = FreeFlow Beverage Solutions (MA S-corp)
- `entity = 'shared'` = split between both

### Department-to-COGS mapping

| Department | QBO Account ID | Account Name |
|---|---|---|
| delivery | 1150040011 | B2B - Direct Labor (COGS) |
| service | 1150040012 | Service - Direct Labor (COGS) |
| reman | 1150040013 | Reman - Direct Labor (COGS) |
| ops (shared) | 1150040007 | Direct Labor |

Legacy AP-tool mappings (Service COGS 101, Equipment Sales COGS 42) are the default fallback in Brixpense's `expense-request-link-bill` when `cogs_account_id` is null.

### Payroll split (confirmed)

- **To FreeFlow 100%:** Nadell $47K, Feliciano $44K, Eric V $55K, Benavides $52K, Anthony V $72K, Andrade $37K = $305K
- **Split 50/50:** Marco $105K, Joel $75K = $90K each side
- **To Alameda Soda (drivers):** Onate $46K, McGee $43K = $89K
- **Officers at AS:** Sky $132K, Sloan $112K (eliminated mid-2027)

### Melt store economics

- Avg equipment $247K/store at 7.7% margin
- Install labor $4K/store
- Service $12K/store/yr + PM $6K/store/yr
- 26 existing + 8 new starting June 2026 = 34 EOY 2026
- Target 58 total by EOY 2028

## Cross-repo references

- **`activespacescience/Skilliosis_Mytosis_Architecture`** — master architecture handbook.
- **`skypace/apbg-gateway`** (alamedapointbg.com) — parent gateway. Proxies `/billing/*`, `/margin/*`, and `/expense/*` here.
- **`skypace/apbg-resq-sync`** — the ResQ ↔ Service Fusion ↔ QBO sync engine (Supabase edge functions; 15-min pg_cron). Replaced the old in-repo `resq-sf-sync*` (decommissioned here 2026-06-28). Managed from this repo's Master Control → ResQ Sync.
- **`skypace/melt-dashboard`** (`/melt/`) — Melt equipment portal.
- **`skypace/APBG-OPS`** (`/operations/`) — PACER operational KPI dashboard.
- **`skypace/APBG-Leasing-Rental`** — separate Railway-backed equipment leasing stack.
- **`skypace/pacerfinance`** (`pacerfinance.netlify.app`) — QBO + Zoho MCP server.
- **`skypace/Pacer-outlook`** (`pacer-outlook.netlify.app`) — Outlook MCP.

## Do not

- **Break the existing billing tool** (`public/*.html` + `netlify/functions/`). Daily-driver for AP.
- **Break Brixpense** (`app-expense/` + `netlify/functions/expense-request-*.mjs`).
- **Expose the Supabase service_role key in client code** — anon key only.
- **Hard-delete staff records** — set `status='inactive'` instead.
- **Modify `ops.qbo_token_cache` or `ops.sf_token_cache` directly** — use the lease RPCs / Netlify Blobs.
- **Add new writers to `ops.*` tables without updating `architecture/sync-manifest.json`.** The lint will fail the build.
- **Commit build output (`public/expense/`, `public/sales-next/`) via the GitHub MCP `github_create_or_update_file` tool with pre-encoded base64.** The MCP base64-encodes whatever you pass — if you hand it pre-encoded base64, it gets double-encoded and stored as literal text on disk. Netlify then serves base64 text instead of HTML/JS and the SPA never loads. Either (a) run `npm run build --prefix app-expense` locally and commit via normal `git add/commit`, or (b) if you must use the MCP, pass the raw decoded content as a UTF-8 string. PR #61 was a full-day debug of this exact mistake.
- **Add a new credential/token store without a health check.** Three silent multi-week outages (SF token 6/29→7/24, Netlify-blob QBO token ~6/10→7/24) happened because each integration minted its own token store and none got monitored — the credential that died was never the one being watched. Any new token/session/credential store MUST ship, in the same change, with a check in `ops.fn_sync_health_extra()` (part of `ops.sync_health()`, which the 15-min `health-alert` pg_cron emails on red/yellow). Currently watched: qbo_token, sf_token, resq_sf_token, sf_portal_cookie + the pipeline checks (receipt sync, autopost, jobs sync, employees cache, distributor notify, vendor payments + funding, ap_inbox). NOT watchable from this DB: pacerfinance / melt-dashboard / Pacer-outlook tokens (live in those sites' own stores — need an HTTP probe if ever monitored).
- **Default-schema bug in Supabase Edge Functions.** `createClient(url, key)` defaults the JS data client to schema `public`. All our tables live in `ops`. If you write an edge function that does `sb.from('qbo_invoices').insert(...)`, the insert silently no-ops (PostgREST returns an error, supabase-js doesn't throw). Always pass `{ db: { schema: 'ops' } }` to `createClient`, or use `.schema('ops').from(...)` per query. RPC calls (`.rpc(...)`) are name-based and not affected by this.

---

## Historical context (kept for reference)

Originally framed as "build the PACER Ops Dashboard." Most of that agenda shipped or split out:

| Original priority | Status |
|---|---|
| **P1 — Upgrade to a real app** | ✓ Shipped. `app/` is the v0.9.27 SPA. |
| **P2 — Roster CRUD** | → Moved to `APBG-OPS`. |
| **P3 — Sync gaps** | Partial. `sync-qbo` v35 + materialized view refresh; FleetComplete + HR moved to APBG-OPS. |
| **P4 — Dashboard KPIs** | Split between BRIX and APBG-OPS. |
| **P5 — Auth** | ✓ Shipped. Supabase Email/Password. |

Full original brief: [`PACER-KPI-SPEC.md`](PACER-KPI-SPEC.md), [`FLEET-HR-INTEGRATION.md`](FLEET-HR-INTEGRATION.md), [`PACER-OPS-README.md`](PACER-OPS-README.md).

---

## Change log

| Date | Version | Change |
|---|---|---|
| 2026-09-02 | production: the run guide, and four things it found | **A click-by-click guide to running a batch, written by driving the real screens — which is how it found four bugs the code review had not.** Ask (Sky): "create a user guide with images of what to click and how do use the system and i can have my qc pwrson test it." New handbook chapter **[Running a Production Run — Click by Click](docs/handbook/10a-production-run-guide.md)** — one run of Hangar 25 Cola from "we need 500 cases" to received stock, **15 screenshots of the actual application** (`public/production-guide/`, captured headless against the built SPA with live BOM data), and a **20-step QC test script** with the expected result per step, including the deliberate failure (lots that do not add up to the yield must be refused). Chapter 10 refreshed alongside it — it still advertised five tabs and the Co-Pack (Legacy) tab removed on 2026-07-26, and never mentioned the lane switch, which is the usual reason "my BOM has disappeared". ⚠ **The four bugs are the real value here, and none is visible by reading the code — each needed the screen rendered.** (1) **The purchase-order detail hid most of the purchase order.** `PoDetailModal` filtered its lines by inventory lane, and a materials PO is mostly `excluded` items BY DESIGN (a can body and a tolling charge are not finished goods) — so the live Quantum PO showed **1 line of 7, $8 of $11,918**, and the Calderoni PO could not be opened at all (`filteredPos` dropped it, so `po` was null and the modal returned nothing). **You could not receive materials from that screen.** Lane scoping belongs on the LIST, never inside a document whose totals must match the PDF and QuickBooks; the list filter now also keeps a PO raised by a work order in this lane, which is what makes the all-`excluded` ingredient PO reachable. (2) **Unit cost rendered in whole dollars** (`fm()`), so a can read `$0` and tolling read `$1` — 4 dp now, matching the work-order materials table. (3) **The New Work Order preview lied about the money**: it re-implemented the material calculation client-side, so it multiplied the **per-run** compounding fee by the run size and priced cans and tolling off the stale QuickBooks cost — **$595,055 estimated on a run whose materials are $14,100** — and, reading only the BOM line's vendor slot, warned "7 without a vendor" when every one is set in Materials & Pricing. It also listed the recipe lines, which have no item and never become PO lines, as rows of `?`. (4) **The BOM editor's component picker** only offered `track_locations` items, so the gallon, tolling, cans and Velcorin could not be named and their rows read blank. **Root cause of 3 and 4 is one thing: three screens each re-implemented the price/vendor precedence instead of the server's.** New `app/src/lib/componentSourcing.ts` is the single implementation — `componentVendorId` / `componentUnitCost` / `componentRequiredQty`, BOM-line override > `production_items` > the QuickBooks mirror, and `per_run` never multiplied — and both screens call it. Verified by re-rendering: the create-form preview now totals **$14,100**, to the cent the same as the work order it creates. Also `inp()` gains `minWidth: 0` / `maxWidth: 100%` — a grid item's default `min-width: auto` let a `<select>` holding a long BOM name print underneath the next field. ⚠ **No migration and no server change**: every one of these was a client reading the data wrongly, and the live RPCs were checked and found correct first. |
| 2026-09-02 | production: lot codes and born-on dates ride the shipment home | **The co-packer's lot codes and born-on dates are recorded on the run and printed on the BOL, one line per lot.** Ask (Sky): "when the shipment of produced product goes out from Quantum back to Alameda Soda, can you enter the born-on date codes and the batch codes on the order so we can track for quality control." Quantum's own invoice already speaks in lots — 1462 lists each flavour's tolling with its batch codes in parentheses — and nothing here could hold them: a work order had ONE batch code (ours) and the return shipment was ONE transfer line. **Shipped (migration `20260902t`):** `ops.work_order_lots` (lot code · born-on · best-by · cases), entered with the yield, in a new **Edit lots** panel on the work order, or in the Ship dialog; written only through `fn_wo_set_lots` / `fn_wo_advance`. ⚠ **The lot quantities must total the recorded yield** — a case is in exactly one lot, so a mismatch is refused with both numbers in the message rather than accepted as rounding (verified live: 410 vs 500 refused; 210 + 205 + 85 lands). `inventory_transfer_lines` gains `lot_code` / `born_on_date` / `best_by_date` and **the finished-goods transfer is written one line per lot** — which is the whole trick: `fn_ship_transfer` and `fn_receive_transfer` already stamp every movement with `source_doc_line_id`, so each inventory movement traces to its lot with neither function changing. New `ops.v_lot_trace` (lot → run → BOL → ship/receive dates → movement count) is the recall query. **The BOL PDF grows Lot and Born-on columns when the shipment carries lots** (Best by as a sub-line; plain layout otherwise) — rendered and inspected, not just read. Stock → Transfers gets the same optional Lot / Born-on fields on the create form and the detail table. ⚠ **Deliberately no lot column on `inventory_movements`** — the movement already points at the line that carries the lot, and a second copy is the kind that drifts. ⚠ **Found and fixed while here: migration `20260902r` (and `k` before it) had `CREATE OR REPLACE`d `fn_wo_create_pipeline`, `fn_wo_generate_pos` and `fn_bom_save_v2` BY NAME — the exact 20260820b maintenance trap: the guard WRAPPER was overwritten with the body and the `__i` inner went stale.** Bodies moved back into `__i`, wrappers re-minted, inner EXECUTE revoked (`20260902s`). Docs: `architecture/PRODUCTION.md` gains "Lots and born-on dates — QC on the way home". |
| 2026-09-02 | production: the BOM finally carries what the vendors actually bill | **Asked whether the BOM had every charge from both vendors: it did not, and reading the vendors' own invoices — not our item list — is what showed it.** Ask (Sky): "Did you make sure that the BOM has all the materials we get charged from both vendors? Did you look at older POs to insure all items are listed from both?" There were no older Refractor POs (the pipeline tables were empty) and no Brixpense expenses for Quantum, so the evidence is QuickBooks' bill attachments — Quantum invoices **1462** (05/2025 final), **1741** (04/2026 deposit), 1583, 1766 and bill 171778 (cans), plus every AC Calderoni line booked to Can Raw Materials since 2025. **Four things wrong on the Quantum side and one Calderoni charge missing entirely:** (1) Quantum bills **ONE tolling line, "Basic Fill: Tolling" at $0.62/can** (both 1462 and 1741); the BOM carried FILL LABOR $0.33 + PACK OFF $0.055 = $0.385 — two lines Quantum never bills, understating tolling by 38%. Now one line at $0.62 on item 531 (kept — renaming an item that carries history buys confusion); 532 PACK OFF removed from every BOM and retired in the item master. (2) **Velcorin** $0.02/can — not on the BOM at all. (3) **Dunnage** — CHEP pallet + polystrap + shrinkwrap, $50 × 114 pallets for 233,088 cans ≈ 85 cases a pallet — not on the BOM at all. (4) Cans: $0.26 vs **$0.328** on the last can bill. (5) **Calderoni bills a flat "canning fees" line of $1,173.33 on every run** since 2026-03 (Feb, April, May, June, September) on top of the syrup — nowhere on the BOM. ⚠ **A flat per-run fee cannot be a per-case quantity**, so BOM lines gain **`qty_basis`**: `per_yield` (default, scales with the run) or `per_run` (one per work order); `fn_wo_create_pipeline` and `fn_bom_material_requirements` honour it and the editor shows "per unit / per run". **Two QuickBooks items created** for the charges that had none — `1390 VELCORIN 12OZ CAN` (NonInventory) and `1391 CANNING RUN FEE (SYRUP COMPOUNDING)` (Service), both to Can Raw Materials 294, neither Inventory; dunnage reuses the existing `565 DUNNAGE FEE PER PALLET`. Quantum's ZIP (80516) read off the invoice header and filled. ⚠ **The 24-pk TRAY is on NO Quantum invoice** — left on the BOM as instructed with a note saying exactly that; who supplies it is Sky's question, not a guess. **Result:** per-case variable cost **$25.85** for Hangar 25 Cola (was $17.52) plus $1,173.33 a run; all seven BOMs still pre-flight at **2 POs / 0 blockers**; verified live with a 500-case run — PO-2026-00015 Calderoni (187.5 gal + the run fee) and PO-2026-00016 Quantum ($11,918.04: 12,000 cans, 12,000 tolling, 12,000 Velcorin, 500 trays, 5.88 pallets) — then rolled back. ⚠ **The gallon price is plausible, not proven:** Calderoni bills syrup as lump sums ("can ingredients" $5,073 for the May run that Quantum deposited as 4,400 gal finished ≈ **$6.92/gal of concentrate**), inside the $4.25–$7.44 range the 1GNS items carry. ⚠ **The $21.36 the finished-case QuickBooks items carry is now demonstrably too low.** Migration `20260902s`. Docs: `architecture/PRODUCTION.md` gains "What the vendors actually bill — the BOM reconciled to their invoices". |
| 2026-09-02 | production: a place to set prices and vendors; POs, BOLs and batch sheets as branded PDFs that email | **Three asks (Sky): a section to adjust items, materials, prices and who we buy from ("since I can't manage those in QuickBooks"); BOLs and moves between Quantum and Brix; and PO / BOL / batch-sheet PDFs that look exactly like The Melt system's and get emailed.** **(1) `ops.production_items`** — the purchased-item master: vendor + price per stocked component (the flavour gallons, cans, tray, fill labour, pack-off), edited on **Production → Materials & Pricing → Purchased items & vendors**, grouped by vendor so it reads as "here is Quantum's order, here is Calderoni's". **Precedence everywhere a cost or vendor is read: BOM-line override > production_items > raw_ingredients > QBO mirror** — `fn_wo_create_pipeline` and `fn_bom_preflight` both changed to read it (migration `20260902r`). ⚠ **The BOM line's vendor slot became an OVERRIDE, not the default**: the seed (`20260902q`) cleared every line vendor that merely repeated the master, because a master shadowed by seven identical copies is not a master — changing who supplies trays is now one edit, and the seven BOMs still pre-flight at 2 POs / 0 blockers. Seeded prices carry the note "seeded from QuickBooks purchase cost — confirm" until saved; the QBO cost is shown alongside and **never written to**. **(2) Quantum ↔ Brix BOLs needed no new machinery** — Stock → Transfers already lets any physical location, co-packer included, be shipper or consignee; what was missing was the addresses (`BRIX-WAREHOUSE` and `QUANTUM-CANNING` both had NONE, so a BOL would have printed blank blocks — filled; Quantum's ZIP deliberately left blank rather than guessed) and the document itself. **(3) `netlify/functions/production-doc.mjs` + `lib/production-docs.mjs`**: one pdf-lib renderer in the Melt design (both marks, red accent rules, company left / number right, grey meta blocks with accent border, accent table header, zebra rows, grand total, signatures, grey footer) for all three documents — **so the batching sheet a co-packer gets looks like the PO that came with it**. GET renders inline; POST files the PDF in the private `production-docs` bucket FIRST, emails it as an attachment (`sendEmail` gained `cc`), and logs `ops.production_doc_sends` — ⚠ **the PDF we email is the PDF we keep**; prices and addresses move and "what did we send them" must not. The email modal leads with what has already gone out for the same document. Company identity / accent / From: live on `production_settings` (edit without a deploy; `doc_from` must be Resend-verified or `sendEmail` falls back to alerts@ quietly). **Buttons:** PO detail → View PDF / Email…; Transfers detail → View BOL PDF / Email… (the old in-browser HTML print is gone — one renderer); Formulas → Batching sheet PDF at the shown batch size / Email…; Work Orders → Batching sheet sized to the run / Email sheet…. ⚠ **Verified by rendering, not by reading:** fixtures for all three rasterised and inspected — caught the long legal name being clipped to "Brix Beverage Dba Alameda Point Beverage..." (now wraps), and confirmed the smart quotes / em dash / ₂ / ✓ in a note survive `winAnsi()` instead of throwing inside the wrap measure. **Not exercised live:** the deployed function needs a hub session and no JWT secret is reachable from here, so the first real View PDF click is the end-to-end proof; the renderer, the SQL and the email helper path are each proven separately. Migrations `q`, `r` applied live; `[functions."production-doc"] timeout = 26`. Docs: `architecture/PRODUCTION.md` gains "Prices and vendors — one master, one precedence" and "The documents — PO, BOL, batching sheet, as PDFs". |
| 2026-09-02 | production: the empty cans are back, and nothing we build is an inventory item | **The six empty-can items are live again — restored, not recreated — and the Service/Non-inventory rule now has something enforcing it.** Ask (Sky): "restore the empty cans from qbo. Or just re-create them using the same naming convention as the active one, make them non-inventory items… I need to make sure everything that we build is either a service item or non inventory items so that it doesn't screw anything up with inventory except for the items that we bring back into the warehouse which will absolutely be inventory items, but those are already built." ⚠ **Restoring beat recreating, and the reason is worth keeping: all six were ALREADY `NonInventory`** — right expense account (294 Can Raw Materials), right cost ($0.26), no asset account, `TrackQtyOnHand` off — identical in every respect to the live `687`. They also carry the purchase history. A fresh near-identical item would have **split that history in two** (not recoverable by editing) and left two confusable names in the QuickBooks item list; recreating would have been strictly worse than reactivating. **Shipped:** `qbo-raw-materials` **v2** gains `action:'restore_components'` — preview by default, `commit:true` to write; it walks only items a BOM actually depends on (not a general reactivate-everything button — QuickBooks is full of deliberately retired items), reactivates them, and **strips the " (deleted)" suffix QBO appends on deactivation and does not remove on the way back**. An optional `rename` map handles a name needing more than the mechanical strip: `685` and `690` were missing `EMPTY` and now match the live convention `CAN <FLAVOUR> 12OZ SLEEK EMPTY`. Ran clean — 6 restored, 0 skipped, 0 failed — and **all seven BOMs now pre-flight at 2 POs, 0 blockers**. ⚠ **It REFUSES an Inventory-type item by design**, and that is the rule made operational rather than remembered: reviving an inventory item revives a quantity and a valuation, which is an accounting decision, not housekeeping. **The state of the books was audited before touching anything and already conformed:** every BOM component is Service (fill labour, pack off) or NonInventory (gallon, cans, tray); the seven `24P####  … CASE` items are the ONLY Inventory items in the pipeline, all with real quantities on hand — which is exactly right, since those are the ones that come back to the warehouse. To stop it drifting, `ops.fn_bom_preflight` now returns **`warnings`** alongside `blockers` and flags any component whose QBO type is `Inventory`. ⚠ **A warning, deliberately not a blocker:** an inventory component does not fail the push — the PO posts fine — it quietly starts tracking quantity and value for something nobody counts, and surfaces weeks later as an inventory-adjustment problem. Calling it a blocker would teach people to click past the category that means *stop*. Docs: `architecture/PRODUCTION.md` gains "Item types — Service or Non-inventory, never Inventory" with the per-role table, and the deactivated-cans gap is closed. |
| 2026-09-02 | production: two purchase orders, and the BOM says who gets them | **A run raises exactly TWO materials POs now — Calderoni and Quantum — and the bill of materials states which vendor gets which before anyone commits it.** Ask (Sky): "the system needs to create only two PO's. The items are ordered from only Quantum and Calderoni so you'll need to move those trays back over to Quantum and the cans over Quantum and why don't you take a look at one of the Quantum bills just to make sure that all of the correct items are on the bill of materials and that the bill of materials list which vendor gets the PO." **He is right and the billing history proves it, which is what makes this a data fix rather than a judgement call:** the can body and the 24-pk tray were vendored to **Craft Beverage Packaging Solutions**, so PO generation — which groups by whatever vendor is on the line — produced THREE POs. But **every Craft line ever booked is a monthly period charge** ("May 2025 hours log", "Oct 20th–31st", "Dec-25", "Jan-26"), i.e. labour, never a can or a tray; **Quantum's lines are where the can units appear** ("Deposit - 202,000 units @ $0.31", "adjusted to 251,988 units @ $0.37", "Cream soda cans"). Craft is not a packaging supplier to this run. Cans and trays moved to Quantum (1744) on all seven BOMs. ⚠ **Six of the seven BOMs also had NO can line at all** — 24 printed cans is the largest packaging component in the case, so leaving it off under-stated the Quantum PO by about $6/case. The lines are on now, mapped from the BOM's **gallon item** rather than its name (the same explicit-map discipline the ingredient seed used). ⚠ **That surfaced a real trap and the fix is the more useful half of this change: six of those can items are DEACTIVATED in QuickBooks** (`685/686/688/689/690/691`; QBO appends "(deleted)" to an inactive item's name — only Old Fountain's `687` is live). Refractor will write a PO carrying one perfectly happily; **QuickBooks refuses the push**, which is the worst possible moment to find out. New **`ops.fn_bom_preflight(bom_id)`** asks both questions up front — how many POs and to whom, and what would stop one — and renders on the BOM editor and the work-order form. A deactivated item and a line with no vendor are **blockers, not warnings**. ⚠ **The ingredients are deliberately not counted as POs**: they ride under the gallon line as detail and never become a line of their own, so a pre-flight that counted them would contradict the roll-up. **Reactivating the six can items was left to a human on purpose** — whether those designs are current is not a code decision, and the pre-flight now makes the gap impossible to miss instead of silent. Verified live end to end: 500 cases of Hangar 25 Cola → **PO-2026-00013 AC CALDERONI 187.5 gal @ $5.38 = $1,008.75** (ingredient detail intact underneath) and **PO-2026-00014 Quantum Canning $7,752.84** (12,000 cans, 12,000 fill, 12,000 pack-off, 500 trays); `po_count: 2`; test data removed afterwards. ⚠ **A second stale price, found by the same bill:** the BOM carries **$0.26** a can while Quantum billed $0.31 and $0.37. Together with the stale gallon that explains most of the gap between our computed **$17.52** a case and the **$21.36** the finished-case items in QuickBooks already carry — the two prices to refresh before a real run. Docs: `architecture/PRODUCTION.md` gains "The pre-flight — which vendor gets which PO". |
| 2026-09-02 | production: the concentrate volume is derived, and the ingredients prove it | **Every flavour is a 5:1 syrup, so the concentrate is one sixth of the finished volume — and the system now computes that instead of being told it.** Correction (Sky), after the roll-up below shipped with the BOM's typed 1 gal/case: "each case of soda will be roughly 5 to one — one part concentrate and five parts water, or 1/6 of the soda. That math should correlate and tie to that number … if all of that flavor, sugar, sodium phosphate and everything is being entered, it should tell you how many gallons that actually is." **He is right and the old number was wrong in a way nobody could have caught by looking at it:** 1 gal/case was neither of the two defensible readings (2.25 gal if the gallon were finished soda, 0.375 gal at 5:1) and it multiplied the entire ingredient purchase order by 2.7. Now `concentrate = cans × oz ÷ 128 ÷ (1 + throw)` = 2.25 ÷ 6 = **0.375 gal per case**, written onto the gallon BOM line by `fn_bom_sync_from_formula` on every rebuild so it cannot drift. A 500-case run orders **187.5 gal of syrup, not 500** (verified end to end live). **The part worth keeping is the CHECK, which is what he was actually asking for.** Every material on the sheet except water ends up inside that concentrate, so their weight over its volume is the syrup's solids loading — a number any beverage person reads instantly. New `ops.fn_formula_batch_basis(bom_id)` computes it, and **six of the seven flavours land between 5.63 and 6.51 lb per gallon, textbook for a 5:1 fountain syrup, agreeing with each other**: Cola 6.11, Orange 6.32, Ginger Beer 6.51, Lemon-Lime 5.70, Crème 5.66, Root Beer 5.63. That mutual agreement is what makes the ratio believable rather than asserted, and setting the throw wrong sends the number somewhere obviously silly. ⚠ **Hangar 25 DIET Cola reads 0.25 lb/gal and that is CORRECT, not a fault** — it is monk-fruit sweetened with no bulk sugar, so its syrup really is almost all water. The check detects a diet formula (no material above 2% of the finished weight) and says the band does not apply, rather than printing a warning on one of seven products that could never be cleared — the same "amber nobody reads" rule as everywhere else in this repo. **The throw ratio lives on the FORMULA** (`product_formulas.dilution_ratio`, seeded 5 from Calderoni's own product codes on the bills — "Cola Syrup - 5XB0", "Root Beer 5XW0") **and is written down to `product_bom.dilution_ratio`**, which the older `fn_bom_scale_runs` already read and which was sitting at 0 on all seven; one source, one derived copy, maintained by the rebuild. `fn_batch_plan` now also reports the concentrate gallons a run needs and what each tank size implies — the TANK is finished product, since the co-packer dilutes and carbonates, so the tank arithmetic itself is unchanged. ⚠ **New top finding, and it is now the highest-value data fix left: the gallon prices in QuickBooks look stale, and everything hinges on them.** The 1GNS items carry $4.25–$7.44; the only 1GNS lines ever billed were $32–38/gal in May 2025 (a pilot), while the current 3-gallon BIB price works out to about **$9.58/gal** (`3G6121, 50 @ $28.75`). Since the gallon is the master price and the whole ingredient breakdown is allocated out of it, a stale gallon price is now a wrong per-case cost everywhere. |
| 2026-09-02 | production: the ingredients roll up into the 1-gallon item | **The purchase order in Refractor lists every ingredient and its quantity; the line that reaches QuickBooks is one gallon line at the gallon price.** Ask (Sky), immediately after the change below: "on each syrup we do have the 1 gallon code … tie those together in the production system, not QuickBooks, so all of those materials roll up into the 1 gallon syrup item … the master gallon holds the pricing. That way we don't need to put the individual ingredients into QuickBooks. We just put the gallons into QuickBooks, but we still need to see how much of each one … so they can order how much of each ingredient they need, and so we can see how much they're charging us per item." **The billing history says he is right, which is why this is the model and not just the convenient answer:** every AC Calderoni line ever booked is priced per GALLON of a flavour — `3G6121 HANGAR 25 COLA, 210 @ 28.75, "Cola Syrup 5XB0"` — and not one is per ingredient. Calderoni compounds the batch; the ingredient list is the SPECIFICATION of what goes in it. **Shipped:** a BOM component line is now either **stocked** (`component_qbo_item_id` — the gallon, cans, tray, fill labour, pack off; unchanged in every respect) or **recipe** (`ingredient_id`, usually no item — sugar, citric acid, flavor), and a recipe line points at the flavour's gallon through `product_bom_lines.rollup_qbo_item_id`. At PO generation the gallon gets an ordinary line (*500 gal of `1GNS6121` @ $5.38*) and the ingredients are filed underneath it in `ops.purchase_order_line_details` (*Cane Sugar 1 102.8 lbs, Cola Flavor 41.4 lbs, Sodium Gluconate 0.945 lbs*). ⚠ **The QuickBooks push needed NO change at all** — that is the whole point of modelling it as detail under a real line rather than teaching `postPurchaseOrder` to collapse lines, and it is why `push-qbo-item`, inventory movements and receiving are all untouched. **Two costs, and the doc says which is true:** `allocated_cost` splits the gallon price across the materials BY WEIGHT so the breakdown always adds back to what we are billed (verified end to end on a live 500-case run: $2 590.53 + $97.25 + $2.22 = $2 690.00 exactly), and it is labelled an allocation, not a quote — flavour is a rounding error by weight and a large share of the real cost, so weight-allocation flatters sugar and starves flavour; `quoted_cost` carries a real price wherever a material has one, which is what eventually makes the gallon price checkable. **`raw_ingredients.purchase_mode`** (`rollup` default / `direct`) decides whether a material gets its own PO line, and it is explicit ON PURPOSE: without it, one click on "create the missing QuickBooks items" would silently turn a Calderoni gallon order into seventeen ingredient lines. **⚠ A correction applied minutes after its own migration, worth keeping:** the recipe detail first rode inside `work_order_materials`, which meant relaxing `component_qbo_item_id` to NULL and teaching `fn_wo_advance` to skip those rows in TWO places — the consume movement and the component cost. Two chances for a later edit to forget, with an inventory movement for a thing that has no item, or a batch costed twice, as the payoffs. Moved to its own `ops.work_order_recipe_lines`, which `fn_wo_advance` cannot see; the NOT NULL guard went back on. **Found by the billing history and fixed: all seven BOM gallon lines were vendored to ALAMEDA SODA COMPANY PRODUCTION** — so a work order would have raised the ingredient PO against the wrong company. Every gallon ever billed came from AC CALDERONI (1099); Alameda Soda Production is the vendor for the OTHER end of the run, the finished cases. The two were conflated because until today the gallon WAS the entire bill of materials. **Consequence of all this: the 17 raw materials do NOT need QuickBooks items after all.** `ops.raw_ingredients` still earns its place — pack size, vendor, and any directly-purchased price live there — but the "create the items" button is now a rarely-needed escape hatch rather than a prerequisite, and the Raw Materials tab says so. ⚠ **Still assumed and worth checking before a real PO goes out: how many gallons of the 1-gallon item go into a case.** The BOM says 1, and that is what a run will order. A case is 2.25 gal of finished product and Calderoni's own BIB syrups are 5:1 concentrates, so 1 gal/case is neither obvious reading and nobody has confirmed it — it is one field (`qty_per` on the gallon line) and it multiplies the entire ingredient bill. Docs: `architecture/PRODUCTION.md` gains a "The roll-up" section. |
| 2026-09-02 | production: the formula finally reaches the purchase order | **A case BOM now lists the actual ingredients, and the work order turns them into one PO per vendor.** Ask (Sky), walking the process end to end: each formula "is basically everything that goes into a can of soda and each one of those needs to be built into a bill of materials … a BOM for a Hangar 25 Cola 24 pack would have the raw ingredients phosphoric acid sugar all that stuff and then it would have 24 single cans then a sleek tray then a tolling charge per case and a fill charge per case … somehow you have to do the math on the formula to figure out how much of each item is actually in a case." **What was actually there could not do that, for a reason worth stating plainly: every case BOM had four lines and NONE of them was an ingredient** — `1 gal 1GNS#### <flavor>` (the entire liquid, as one purchased thing bought from ALAMEDA SODA COMPANY PRODUCTION), 24 × fill labour, 24 × pack off, 1 × tray. The recipe was hidden below the gallon, so no AC Calderoni purchase order could ever fall out of a work order. And **not one of the 34 formula ingredient rows was linked to an item** (`component_qbo_item_id` null on all of them) because **no ingredient item existed in QuickBooks at all** — no sugar, no citric acid, no phosphoric acid, no flavor. That is the "items still need to be created" gap, and it is why the link column was empty rather than merely unused. **Shipped:** `ops.raw_ingredients`, the shared ingredient master — one row per physical material, seeded to 18 from the seven live batching sheets and linked to all 34 formula rows (names mapped EXPLICITLY, not by regex: the sheets spell water three ways and Old Fountain's citric acid carries a batching instruction inside its own name, and a regex loose enough for those would also silently merge two materials that only look alike). It separates how a material is **batched** (percent by weight, lbs) from how it is **bought** (vendor, pack, cost, item). `ops.fn_formula_case_requirements(bom_id)` is THE per-case arithmetic — cans × oz ÷ 128 = gal/case, × density = lbs of liquid, × percent = lbs of that material — and the BOM rebuild, the recipe preview and the work order all call it, so there is one answer to "how much sugar is in a case" (verified: Hangar 25 Cola = 2.25 gal × 8.4 lb/gal = 18.9 lb of liquid, 2.2056 lb of sugar). `ops.fn_bom_sync_from_formula` writes those rows onto the BOM; `ops.fn_batch_plan(bom_id, cases)` answers the MOQ question ("500 cases = 1 125 gal; a 1 500 gal tank makes 666 — add 166 to fill it"); `ops.fn_wo_create_production_po` creates the other end of the run, one PO to ALAMEDA SODA COMPANY PRODUCTION for the finished cases at the merged per-case cost. New **Raw Materials** tab, a Recipe panel + **Rebuild from formula** in the BOM editor, a **Batch plan** panel on the work order, and a **Create production PO** action. ⚠ **Four rules are load-bearing and a later edit will want to undo them.** (1) **Water is not purchased** — it is 87–99% of every formula by weight and comes out of the wall at the co-packer; it stays on the batching sheet (it is what makes the percentages total 100) and is excluded from every PO, or a run orders two tons of water. (2) **Recipe units are not purchase units** — the formula says lbs, the vendor sells 50-lb bags, so `recipe_qty` is the need and `required_qty` is packs rounded UP; collapsing them is how a PO orders 11.7 bags of sugar. (3) **A BOM line has an owner** (`product_bom_lines.source`): the rebuild owns `formula` rows, `fn_bom_save_v2` owns `manual` ones, and neither touches the other — **found while wiring it that the BOM editor loaded every line and saved them all back, which would have wiped `source` off the recipe and made the next rebuild add a SECOND copy of every ingredient**; the save RPC and the editor now both respect the split. (4) **Nothing invents a cost** — all 17 purchased materials ship with cost and pack blank, and that renders as a visible gap rather than a plausible number that becomes a QuickBooks bill. **Accounting:** everything settles through **Can Raw Materials** (QBO account 294, already the expense account on the gallon items) — Calderoni in, Quantum in, and the production PO for finished cases as the offset, so the account nets to zero per run and any balance is a real variance. `purchase_orders.po_kind` keeps a materials PO and the production PO apart; summing them as spend double-counts the batch. **Item creation is deliberately NOT done automatically:** new `qbo-raw-materials` edge function (deployed, `verify_jwt=false`) previews the 17 items and writes only on a second, explicit click — a QuickBooks item cannot be deleted once created, only deactivated, and an item whose name already exists is LINKED rather than duplicated (a duplicate splits purchase history in two). Live preview verified: 17 candidates, expensed to account 294. **Also fixed in passing: `product_bom.finished_vol_per_yield_gal` on the Diet Cola BOM held 1000** — a BATCH size, not gallons per case — so "make 1 000 gal of Diet Cola" would have scaled to ONE case; all seven are now 2.25 and the other six had been null. ⚠ **Three findings left for a human, not guessed at.** No Calderoni price list exists, so per-case cost is not computable yet. **Six of seven BOMs have no empty-can line** because items 685/686/688/689/690/691 are INACTIVE in QuickBooks — someone deactivated them, and whether those can designs are current is not a code decision. And **cans and trays are vendored to Craft Beverage Packaging Solutions, not Quantum Canning**, which contradicts the process as described — PO generation groups by whatever vendor is on the line, so a run currently produces three POs, not two; the fix is a vendor change on the BOM, not in code. Docs: new `architecture/PRODUCTION.md`. |
| 2026-08-29 | fleet reconciled to the real trucks | **Fleet Complete lists DEVICES, not trucks — ten assets covered six vehicles, and the list now says which is which.** Ask (Sky): "Those vehicles are gone. but can you link up the vehicles in fleet complete with the actual vehicles". **The link was made from telemetry, not names.** Asset `b680811a` ("Ford Transit 350 H", no VIN) looked like a seventh truck. On 2026-05-08 it and asset `2ce0040a` ("2024 F350 H", VIN 1FTBF7C81RKA29414) **started 18 seconds apart, finished 94 seconds apart, and logged 246 vs 250 miles on the same Hangar 200 → San Jose → Salinas → Santa Cruz → Hangar 200 run** — one journey recorded twice, by two devices on one truck; one segmented the day into 5 ignition cycles, the other reported it as a single 250-mile trip. The name suffix "H" matched too, but the name is what you check LAST: the same-day comparison is what distinguishes a duplicate device from two trucks convoying, and it also cleared asset `f59ee330`, which ran a different window and half the distance. Migration `20260829b` adds `linked_vehicle_id` (self-FK, `check (<> id)`, ours — the hourly upsert never touches it, so the link survives the sync rewriting the asset's name); the duplicate is pointed at the real vehicle, archived, and **the header now counts vehicles, not rows**. The two remaining unidentified assets are unpaired trackers — asset names are the devices' own serial numbers, zero trips ever, one lone snapshot from 2024-12-03 — and are **deliberately left UNLINKED** rather than matched to something plausible: there is no evidence tying them to a truck, and a wrong link is worse than a visible gap. **⚠ A CORRECTION TO YESTERDAY'S FINDING, which I had raised as an alarm.** I reported the Transit `1FTBR1C89PKB65386` as "ACTIVE IN FLEET COMPLETE — it is being driven", so uninsured if Progressive lapsed. That was read off `status='active'` in the FC roster, which is not evidence of movement: the truck has **1 trip ever, last telemetry 2026-05-18 near Auburn, silent since**. Sky confirms all four Progressive-only vehicles are gone, and the telemetry independently agrees — **GEICO's six scheduled VINs are exactly the six assets still reporting trips**. Nothing is uninsured; the outstanding job runs the other way (get all four off the Progressive schedule so we stop paying for vehicles we do not own). Corrected in SOP-11, on the GEICO document record, and on each row. The **umbrella-over-GL-only and $1M-vs-$2M auto limit finding STANDS** and is the real issue. All six gone/duplicate/unpaired rows are **archived, never deleted** — a registration and service history are the record for the period we owned the vehicle, and a claim can arrive after the truck has. **Bug caught by the headless test, worth remembering:** the new `linked_vehicle_id` field was inserted by anchoring on `{ k:'entity', l:'Our entity', … }`, a line the **sds** spec carries verbatim and earlier in the file — so it landed on the SDS form and an SDS save would have PATCHed a column `compliance_sds` does not have. The test noticed it missing from the vehicles PATCH body; the file now asserts the field is inside the vehicles spec and absent from sds. Anchor on something unique to the block you mean. |
| 2026-08-29 | fleet & vehicles | **Every vehicle we own now has a record — registration, odometer, service, insurance, and its own document drawer — built ON the Fleet Complete table rather than beside it.** Ask (Sky): the GEICO certificate, plus "lets also make a vehicle section in compliance and list all vehicles, we want to list their last repair date, odometer, license VIN, registration, with registration notices, and a file system where we can keep documents related to the vehicle, i will also use the fleet complete api to wire in data to this as well." **The design decision that mattered was not building a new table.** `ops.fleet_vehicles` ALREADY EXISTED — the `sync-fleetcomplete` edge function upserts 10 trucks into it hourly, and my first migration was a `create table` that correctly failed as a no-op. A second vehicle list is how a lapsed registration ends up filed against the row nobody reads, so migration `20260829a` **extends it in place** (registration_expires / registration_notes / plate_state / odometer_at / odometer_source / last_service_date / last_service_notes / next_service_due / insurance_carrier / insurance_policy / entity / assigned_to / archived_at / source). **The split with the sync is strict and enforced in the UI, not by a comment:** FC owns vehicle_name / vin / license_plate / make / model / year / status and rewrites them every hour, so those fields render **DISABLED** on a `source='fleetcomplete'` row and `saveEditor` skips every disabled input — verified headless that a PATCH from that form carries only our 13 columns and none of FC's. An edit there would otherwise look saved and silently revert within the hour, which is worse than not offering it. A vehicle FC cannot see is keyed in with `source='manual'` and a synthetic `fc_asset_id='manual:<vin>'` that can never collide with a real asset id, so the hourly upsert stays intact. **Deliberately no unique index on VIN** — a duplicate or mistyped VIN would make that upsert throw and take the whole fleet sync down; a duplicate VIN is a data-quality problem, not an outage. **Documents are ordinary vault rows** (`compliance_documents.vehicle_id` + category `vehicle`), so a registration card sits beside the COI it is evidence against; a doc filed under a vehicle now has exactly one home, like a party document does. **Security: anon SELECT on `ops.fleet_vehicles` is REVOKED and its two duplicate `USING(true)` anon policies dropped** — VINs, plates and (now) insurance policy numbers were readable with the public anon key on a project that also authenticates brix-order customers and distribution partners. Verified nothing depended on it before revoking: `public/dashboard.html` is superadmin-gated and sends its session bearer, and Refractor's Fleet page reads through `sbq` on the caller's own token. Staff INSERT/UPDATE grants are written next to the policies (the 20260825a lesson). **The tab tells you what is wrong, not just what exists:** registration expired / due in 45 days / no registration date / not matched to an insurance policy / asset with no VIN or plate, mirrored into two Dashboard tiles; an odometer prints its read date or says "no read date" rather than implying a number is current. **⚠ THE FINDINGS, which are the actual point of reconciling the two schedules.** GEICO `9300171094-02` (06/24–**12/24/2026**, holder THE CITY OF ALAMEDA) is filed, and against Progressive `975506554` it is NOT a like-for-like swap. (1) **Auto liability has HALVED** — Progressive carries $2M CSL + $1M UM/UIM; GEICO shows $1M CSL, no UM/UIM, **and the new Great American umbrella sits over GL ONLY**, so a serious-injury collision (the likeliest large claim this company will ever face) is capped at $1M with nothing above it. (2) **Four Progressive vehicles are not on GEICO** — 2017 Metris ×2, 2017 C-Max, and Ford Transit `1FTBR1C89PKB65386` (plate 36162W3). **That Transit is ACTIVE IN FLEET COMPLETE — it is being driven**, so if Progressive is cancelled the way Spinnaker just was, it is uninsured; the other three are not in FC at all, so either they need adding to GEICO or they were sold and we are still paying for them. All four are on the tab, three keyed in by hand. (3) **It is a SIX-MONTH policy** expiring one week before the Progressive and Great American renewals — the four renewal dates no longer line up, and an annual December habit now misses both the GL cycle and this. Also **owned autos only**: hired and non-owned auto liability is not on it. Three FC assets carry no VIN or plate at all (names look like device serials, unsynced since 05-09) — flagged on each row as unpairable to any insurance schedule. SOP-11 §1/§2/§5/§6 rewritten around all of it. **Found and fixed in passing: `CLAUDE.md` had SHIPPED with unresolved merge-conflict markers** (`<<<<<<< HEAD` … `>>>>>>>` around the 08-28 NDA rows) from an earlier merge — HEAD was the superset, kept, and the repo swept clean. Manifest: `brix-compliance:app` gains `ops.fleet_vehicles` as a multi-writer with `sync-fleetcomplete`, with the column ownership split written into both notes so widening that upsert without widening the UI lock is caught. Tests 103 passing. |
| 2026-08-29 | spinnaker cancelled | **The overlap is resolved — Spinnaker is cancelled, Great American is our GL.** Sky: "We are deleting our spinnaker insurance today." `CSB-00128559-00` **archived, not deleted**, and the record says why in the row itself: it is an **OCCURRENCE-form** policy, so it still answers for anything that HAPPENED between 2026-01-01 and cancellation, years later if need be — a lapsed liability policy is not a dead record, and deleting one destroys the only pointer a future claim has. The stale "we may be paying twice" note on `PLF574240` is replaced with the resolution. **Four consequences of a mid-term cancellation are now on the page and in SOP-11, because none of them happens by itself:** (1) **every certificate issued under Spinnaker is void going forward and any additional-insured / waiver-of-subrogation ENDORSEMENT dies with the policy** — holders needing current evidence, **the landlord (LPC / City of Alameda) first**, need reissued certificates from Ram Risk and any contractually required AI endorsement re-done under Great American; (2) **check the cancellation date is on or after 8/25** — the new policy incepted then, and backdating to recover more premium would open a window with NO general liability; (3) get the written cancellation notice and file it (it is the proof we were not double-covered and it fixes the date); (4) expect a return premium, pro-rata or short-rate. Carriers table + Dashboard card show the row as cancelled with the occurrence-form note rather than dropping it. |
| 2026-08-28 | ram risk broker + new GL/umbrella | **New broker of record, a new GL carrier, and the umbrella gap is closed — filed, and the COI request process is now on the page where people ask for certificates.** Sky sent Ram Risk Group's COI rules plus an ACORD 25 dated 08/27/2026. **What the certificate actually says (read, not assumed):** producer **Ram Risk Group, LLC** (Corey Ramsey, 11891 U.S. Hwy 1 Ste 100, North Palm Beach FL 33408, (561) 206-4733, coi@ramriskgroup.com); insurer **Great American (NAIC 16691)**; **GL `PLF574240` 8/25/26–8/25/27** ($1M occ / $2M agg / $2M products-comp-op / $500K rented premises / $20K med / $1M pers & adv) and **umbrella `XSF574242`** ($5M each occurrence / $5M aggregate). Both filed in `ops.compliance_documents`. ⚠ **Two things the document raises that nobody asked about.** (1) **Two GL policies overlap** — Great American incepted 8/25/26 while Spinnaker/Thimble `CSB-00128559-00` runs to 12/31/26; either the old one was cancelled at inception (get the confirmation and file it) or we are paying twice. The old row is struck through, not deleted, until that is answered. (2) **The umbrella shows over GL ONLY** — this CLOSES the 'no umbrella' gap raised in SOP-11 on 2026-08-17, but an umbrella that does not sit over AUTO (Progressive) and EMPLOYERS' LIABILITY (AmTrust) leaves the two lines most likely to produce a claim above primary limits — a road fatality, a serious warehouse injury — uncovered where it matters most. Confirm the underlying schedule in writing. **Portal:** Insurance & Company opens with a **Need a certificate of insurance?** panel — the four fields Ram Risk requires (holder name AND complete address · description of operations · coverage types and limits · endorsement requirements), their Mon–Fri 9–5 hours with the next-business-day rule spelled out (**their office is in Florida, so if those are Eastern hours a Friday-afternoon ask is a Tuesday certificate**), the contact table, and a **Compose the request** button that opens a pre-addressed mail with the four fields as a skeleton — with the text shown on the page as well, because plenty of desktops have no mail handler and a silent `mailto:` is a dead button. The ACORD's own warning is repeated in the panel: **ticking additional-insured is not the endorsement**. Carriers table + Dashboard contact card updated; auto and WC explicitly still come from their own brokers. SOP-11 §1 schedule, who-to-call, and §3 rewritten to the real process. |
| 2026-08-28 | sign-once prompt | **The place to sign is now the first thing on the NDAs tab.** Ask (Sky): "give me a place to sign on the portal and then use that." The pad already existed under NDAs → Signatures, but it was a sub-panel below two tables, which is not a place anybody finds. Now an amber panel sits at the TOP of the tab whenever no signature is on file — "Sign once, and we'll use it from then on" + a **Sign here →** button straight into the pad — and it disappears the moment one is saved. Pad grown 130→190px with a crosshair cursor and a "sign above the line" hint; name prefills from the profile, so it is literally sign and press Save. ⚠ **Worth recording: I offered to GENERATE an adopted signature** (his name set in a formal italic, which is what e-sign products mean by adopt-your-signature) and briefly stored one. Sky's answer was no — he wants to sign it himself, once, on the portal. That is the right call and the row was deleted: a signature that the person never made is not evidence of anything, however standard the practice. Verified headless both ways: with no signature the banner shows, opens the pad, and a drawn stroke saves as a PNG data URL; with one on file the banner stays hidden. |
| 2026-08-28 | nda signatures + mutual | **Our own signature is on the agreement now, it goes out from legal@, both brands are on it, and there is a mutual version.** Ask (Sky): "you need my signature or whomever signs on behalf of the company so the signature is stored in there, also it needs to come from legal@alamedapoibg.com. it would be nice to have alameda soda logo and brix bev logo. Also i need a MNDA selection so you can have MNDA language in there it changes it." **(1) Signatures.** New `ops.nda_signatories` (migration `20260828a`, applied live) — draw or upload once under **NDAs → Signatures** and it prints in the Company block from then on. Until now the counterparty signed properly and WE didn't, which is a poor look on our own paper. Three rules make it safe: the image is **SNAPSHOTTED onto the agreement** (`company_signature_data`) exactly like `body_source`, and the freeze trigger was widened to cover it — re-drawing your signature must never alter a document somebody already signed; only a `data:` image passes validation, size-capped, because this ends up embedded in a legal record where a remote URL would be a live dependency and free text would be an injection; and **a missing signature is not an error** — the agreement still goes out with a typed name over a rule, which is what we sent for months. Saving a title does NOT wipe a stored signature (absent field means leave it; a test and a headless check both pin that). Sender links carry `company_signatory_id` and read the signature **fresh at send time**, so a re-drawn signature takes effect next send instead of stranding a stale image on every future agreement — and a link issued BEFORE signatures existed (there is already one live) falls back to matching the signatory by the signer's NAME, so it picks the signature up on its own rather than having to be re-issued, which would invalidate the link its holder already has. **(2) legal@.** `NDA_EMAIL_FROM`, default `legal@alamedapointbg.com`, on all 9 send sites across the three functions — this is the address a counterparty replies to with "can we change clause 9". ⚠ It must be a REAL monitored mailbox, and `sendEmail()` falls back to the alerts address when Resend rejects a sender, so a misconfiguration degrades quietly rather than bouncing. **(3) Both logos** — Brix round mark + Alameda Soda seal in the email header (absolute gateway URLs, alt text, since many clients block images) and as a real PDF letterhead (`lib/nda/nda-logos.mjs`, downscaled to 260px and base64'd rather than read off disk: a Netlify function only ships files the bundler can see it import, and a document that renders without letterhead because a path moved is worse than a slightly bigger module; wrapped in try/catch — letterhead is decoration, the document is the point). **(4) MNDA.** `lib/nda/mnda-v1.mjs` (code `mutual-nda`) + a shipped-template registry so both seed; picker on the staff send form and on the delegate's page, and a sender link can be PINNED to one flavour or left to their choice. `mutual` is a flag **on the agreement**, snapshotted — never derived from the template code, so a renamed template can't retroactively change how an executed agreement reads; it drives the preamble ("Brix"/"Counterparty" + each Party acts as both Discloser and Recipient) and the signature-block headings in HTML and PDF alike. ⚠ **The mutual text is an ADAPTATION of the approved one-way agreement, not separately drafted paper — counsel should read it.** Two departures are deliberate and tested: **§7 is narrower** (the one-way "no similar product" covenant cannot be reciprocal between two beverage companies without stopping both of us trading, so it is scoped to use OF the other side's Confidential Information with an express independent-development carve-out), and the **Work Product assignment is REMOVED** (mutual, that clause would assign OUR work to THEM). **⚠ A REAL BUG FOUND DURING LIVE VERIFICATION, and it predates this work: `pdf-lib`'s PNG decoder does not throw on a malformed file — on some it SPINS FOREVER, synchronously.** A try/catch cannot catch that and a Promise timeout cannot interrupt it (a synchronous loop blocks the event loop). The live sign endpoint hung past the platform timeout and the agreement came out **signed with no PDF, nothing filed in the vault and no email to either side** — the signature is recorded BEFORE those steps, by design, so the whole best-effort-with-visible-failure posture was defeated silently. Fixed with new `lib/nda-image.mjs`: the PNG/JPEG **container is validated structurally before the decoder ever sees it** (chunk walk with bounds checks — the file that hung us declared a chunk 4.8 MB long inside a 166-byte buffer), and it is checked at the two points a human can still fix it (the signing page and the Signatures editor) rather than only where the document renders. `embedSignature` degrades to no-image rather than throwing, so a bad picture can never cost an executed agreement. Tests 83→95. Verified headless: the signatures table renders the stored image, both pickers drive the payload, a pinned link hides the picker and says which agreement it sends, and saving a signatory without touching the pad sends no `signature_data`. |
| 2026-08-28 | nda sender links — name + email | **Issuing a sender link is now two boxes: their name and their email.** Ask (Sky): "Just allow me to put names in with their link and their email address so I can send them their link to set up." The emailing already happened; the friction was the FORM — it demanded a label and a countersigning officer before it would do anything, and a form with eight blanks is a form nobody fills in. Now **name + email are the only required fields** and everything else defaults (label → their name; 90 days; 5 NDAs per 24h; purpose/services optional), with the rest behind a **More options** disclosure so nothing is hidden, just not in the way. **The one default worth arguing about is the countersigning officer**, which is printed on every agreement the delegate sends: it comes from the issuing staff member's **profile name** (`user_metadata.full_name`), read the same way on both the page and the server, and is **never derived from the email address** — "Skypace" is not a signature. With no name on file the issuer is ASKED rather than handed a guess, and the panel says which case it is in before you press the button. **New `link_resend`** ("Send again" per live row): the raw token is stored only as a sha256, so re-sending the original is impossible by construction — it mints a NEW token and kills the old one in the same write, which is the honest behaviour anyway since a link that needs re-sending has usually gone astray. The confirm says that out loud; person, signer and limits carry over. `SESSION` on compliance.html gained `name` (from the JWT's user_metadata, not the address). **Verified headless both ways** against a mocked API: with a profile name the panel shows exactly two inputs and reports "countersigned by Sky Pace", the payload carries an empty label the server fills in, and the link renders; with no profile name the signer field goes up empty and the server refuses with a sentence naming the reason. |
| 2026-08-28 | nda sender links | **Someone with no hub login can hand out NDAs — from a named, expiring, revocable, rate-limited link that can only send.** Ask (Sky): "Is there a way for someone to get a link that allows them to give out ndas without signing in?" The honest framing came first and shaped everything: **a link that emails as us is a credential** — whoever holds it can send Brix-branded mail to any address, which is a phishing tool with our domain on it — so the answer was not a looser gate on `nda-admin` but a **separate function**, `nda-send.mjs`, which contains no code to list, open, revoke, download or edit anything. That separation is STRUCTURAL, not a role check: no mistake in a gate there can expose another counterparty's agreement, and if a delegate ever needs those, that is the moment to give them a login rather than widen this. **Guard rails, each load-bearing:** the link belongs to a NAMED person (`person_name`+`person_email` required — a link nobody owns is a shared secret, and a shared secret gets pasted into a group chat); it is **rate-limited on a ROLLING 24 hours** (`ops.fn_nda_link_sends_24h`, counted from the agreements themselves so a failed counter stamp changes nothing, and rolling so a leaked link can't send its quota at 23:59 and again a minute later); it expires (90 days default, 1–365 — **no unlimited option exists**) and revokes instantly; `recent` returns only what THIS link created and only company/status/dates — no addresses, no signer details, no PDF; and **every send emails compliance AND the holder**, so the audit trail lands in a mailbox an abuser doesn't control. **The company signer is fixed by the ISSUER** — the delegate dispatches a document an officer already executed, they never sign for us; a genuine countersignature would be a different flow. A sender link also cannot publish a template: on an unseeded environment it is told to ask the office rather than improvising the terms it sends. Token model is the same one (32 random bytes, only the sha256 stored) — the raw token is shown once at creation and emailed to the holder, so a lost link is RE-ISSUED, never recovered. Migration `20260827a` (applied live): `ops.nda_sender_links` + `nda_agreements.sender_link_id`, staff-only RLS with the **GRANTs written next to the policies** (the 20260825a lesson — Postgres checks privileges before RLS). New `/nda-send` page + gateway proxy row; staff issue and revoke from **Compliance & Safety → NDAs → Sender links**. Guard rails extracted to pure helpers in `nda-lib.mjs` (`linkUnusable` / `pickServices` / `clampLinkTtl` / `clampLinkRate`) so the issuing and sending endpoints cannot drift on what "usable" means — tests 77→83 pin each: revoked stays revoked, expiry bites, an unknown token reads EXACTLY like a malformed one (a probe must not learn which tokens exist), zero is not "forever" or "no limit", and a delegate cannot write free text into the services clause of an executed agreement. **No watcher added, deliberately** — still no cron and no background pipeline, so there is nothing new for `ops.sync_health()` to watch; a chase cron would need one in the same change. Docs: `architecture/NDA-ESIGN.md` gains a "Sending without a login" section. |
| 2026-08-27 | nda e-sign v0.1.0 | **Vendors, co-packers and labs sign our NDA on a link — fillable, signable, and it files itself.** Ask (Sky): "when we work with vendors that we need to provide an NDA, can you make this nda fillable and signable on a link so that people can enter their info and read it and it saves their contact, use etc and provides electonic NDA?" **Flow:** Compliance & Safety → **NDAs** → Send → they get a link → they fill in their own legal name, entity type and address, name a signer, read the agreement, type their name and sign on screen → the executed PDF is emailed to both sides, and filed in the compliance vault (new category **`legal`**, doc_type NDA) against the counterparty's `insured_parties` row, so an NDA sits beside their COI and W-9 instead of in somebody's inbox. **Four design rules do the load-bearing work, because an NDA is only worth having if you can later show what they agreed to, that it was them, and what you actually gave them.** (1) **The signed text is SNAPSHOTTED onto the agreement**, not referenced — templates stay editable (publish 1.1 without a deploy) but editing one cannot change what somebody already signed; a signature pointing at mutable text is not evidence of anything. `template_save` refuses to overwrite a version in place for the same reason one step earlier: a pending signer could be reading it mid-sentence. (2) **A signed row is frozen by TRIGGER** (`tg_nda_freeze_signed`), not by convention — filing columns may still land after signature, since that is the same execution event finishing. (3) **One parse, two renderers** — `lib/nda-doc.mjs` parses the template markup into blocks and BOTH the on-screen document and the pdf-lib PDF consume those blocks; two copies of the text could quietly disagree about what was signed. (4) **The audit page is part of the document** — a drawn squiggle proves little, so the PDF's last page records typed name (intent), timestamp, signer IP + browser, when the link was sent and first opened, the consent flag and the template version. **Exhibit A is a running log, not a blank table**: `nda_disclosure_log` records every batching sheet, spec and sample you send them — the agreement says a missing entry does not remove protection, but that log is what makes "they had our formula" provable. **⚠ §21 (Electronic Signature and Records) was ADDED to the supplied text** — the draft had no ESIGN/UETA consent, no statement that the typed name plus drawn signature ARE the signature, and no retainable-copy delivery, which are exactly what makes an on-screen signature hold up; the page collects that consent as an explicit tick and prints it. **Token model** is the vendor-onboarding one: 32 random bytes, only the sha256 stored, so a database read cannot mint a signing link; 30-day expiry, resend mints a NEW token and kills the old, revoke refused once signed (an executed agreement is terminated in writing under §14, not by a button). **Filing never un-signs anything** — once the signature is recorded the PDF/vault/email steps are each best-effort and record their own failure into `notes`; the page says so and pushes the signer to download the PDF then and there rather than implying everything landed. **The company block is pre-executed at send time** by the staff member sending it — this is our paper on our terms and the assent being collected is theirs; a genuine two-step countersignature would be a different flow, not a blank block. Migration `20260826a` (applied live). New `/nda` public page + gateway proxy row — **deliberately no hub tile and no appswitcher inference**, unlike the visitor kiosk: there is no staff use of that page at all. Tests 61→77 (`tests/nda.test.mjs`, 16). ⚠ **A bug in the tests themselves, worth remembering:** the first version asserted the em dashes survived into the PDF by grepping the raw file for byte 0x97 — pdf-lib Flate-compresses its content streams, so that matched by COINCIDENCE against a PDF that had none; the test now inflates the streams and decodes the hex text runs. It caught the real bug too — an over-eager WinAnsi sanitizer had been stripping every em dash out of clause 2. Docs: `architecture/NDA-ESIGN.md`. |
| 2026-08-26 | qbo categories off | **QBO item Categories are gone company-wide and the button that would restore them is removed.** Sky removed every Category in QuickBooks by hand (verified: 475 items scanned, **0 Category rows, 0 items with a ParentRef**) — QBO transactions, reports and invoices now show bare item names with no `Category:Item` prefix. The risk was the other direction: **Items settings → 'Push to QBO'** called `push-qbo-item`'s `bulkSyncCategories`, which CREATES QBO Category Items and sets each item's `ParentRef` — one click would have put all 475 back. That button, its review modal (`PushCategoriesReviewModal`) and the three handlers are deleted, with a header note on `ItemsSettingsEditor.tsx` saying why. ⚠ **What stays, deliberately:** `inventory_settings.category_override`, **Align all to P&L** and **Smart suggest** are BRIX-LOCAL categorization for margin reporting and never touch QuickBooks; the red **Clean up QBO categories** button stays as the UNDO if one ever reappears. The `bulkSyncCategories` action still exists server-side but now has **no caller** — don't wire a new one. Verified with `tsc -b` + a full `vite build`; build output was deliberately NOT committed (ship source only). |
| 2026-08-26 | spending report v0.1.0 | **Spending: what are we spending, where, and what's growing — off the QBO mirror, so it sees ALL booked spend.** Roadmap item #3 ("Next"). **Backend (migration `20260826f`, applied live):** `ops.fn_spend_report(p_months)` — one jsonb round trip over `ops.qbo_expense_lines`: monthly totals + by-vendor + by-account rows, each with window total / txn count / prior-window total / per-month map for sparklines; `ops.fn_spend_vendor_detail(p_vendor, p_months)` — the drill, one vendor's 100 most recent lines. Both SECURITY DEFINER with the staff guard INLINE (`fn_assert_staff_or_service`, the 20260820b rule — vendor spend is exactly what the shared-project hardening protects), EXECUTE revoked from public/anon, called from the page with the caller's own JWT (`supabase.rpc`, the `fn_1099_candidates` pattern). ⚠ An apply-time paste typo briefly granted the detail fn to `anon` — caught and revoked in the same session, grants verified clean (the inline guard would have refused anon calls regardless; the repo file was always correct). **Conventions that matter:** VendorCredit lines count NEGATIVE (same as 1099); item-based lines fall back to `item_name` when `account_name` is blank — found live: without it, **$5.26M of item-based spend lumps into one "(no account)" bucket**; "Growing fastest" sorts by ABSOLUTE dollars added vs the prior window, not percent (percent alone makes a $12 vendor "growing 900%" outrank a $40k jump). **UI: Brixpense → Accounts payable → Spending** (`Spending.tsx` + `lib/spend.ts`, staff-only nav item): 6/12/24-month window, stat tiles (this month / last month / window total / Δ vs prior window), monthly column chart, By vendor / By account tables with search, per-row sparkline + growth badge + share %, click-to-drill on vendors, CSV export (prefix-quotes `=+-@` — OCR'd names, Excel formula injection). **Stated caveat on the page and in the SQL header:** accrual-dated, refreshed daily, mis-dated bills land in the month they claim (there is a 2028 Purchase in the mirror today) — a TRENDS report, not a P&L; the P&L is QuickBooks' to print. No manifest change (read-only RPCs, no new writers). |
| 2026-08-26 | vendor credits v0.1.0 | **A credit memo from a vendor has a home: file it like a bill, post it as a QBO VendorCredit, apply it inside a pay run.** Roadmap item #2 ("Next"). **One flag does the work:** `expense_requests.is_credit` (migration `20260826e`, applied live) — the amount stays POSITIVE and the flag carries the sign, so OCR, attachments, the duplicate gate, lifecycle tabs and RLS all work on credits unchanged. **Filing:** the expense form's unpaid-bill branch gains a "This is a credit memo" checkbox, and `expense-ocr-core` now extracts `is_credit` (headed "Credit Memo"/"Credit Note" or a negative printed total, which gets normalized positive) — a dropped credit-note PDF flips the form by itself. **Posting:** `expense-request-link-bill` lands `is_credit` rows as a QBO **VendorCredit** (same payload shape, different entity; attach-after-post works via EntityRef type VendorCredit). **Applying:** Pay Bills shows posted, unapplied credits as green negative rows per vendor; ticked credits ride the payment as BillPayment lines with `LinkedTxn TxnType='VendorCredit'` (positive Amount, `TotalAmt` = bills − credits — `buildBillPaymentPayload` refuses a negative payment and a credits-only payment; a **$0 apply-only** payment is allowed on the record rail, and the Stripe rail refuses net-zero with a pointer to it). Credits get their own `vendor_payments` ledger rows, so the one-live-payment-per-txn unique index also stops a credit being **applied twice**, for free; applied ⇒ `paid_at` stamped (consumed). Works on both rails: `record` books the netted BillPayment immediately; Stripe pays the NET and the webhook's `settleGroup()` books credits into the settlement BillPayment. **The paid-sync closes the loop the honest way:** `lib/qbo-bill-status.mjs` partitions its pool and asks the **VendorCredit** entity for credit rows — Balance there is the UNAPPLIED remainder, so 0 = consumed; asking Bill would repeat this morning's false-"missing" bug with the sign flipped (the header comment says so). A credit applied by hand inside QuickBooks therefore closes itself on the next daily run. **Guard rails:** credits are excluded from `ops.v_ap_aging` (a credit is not a payable — it lives where it is actionable), refused by the single-bill Pay button, and the remittance advice renders them as negative lines ("bills and credits") so the vendor sees exactly which credit offset which bills. `vendor_payment_groups.total_amount` CHECK relaxed to `>= 0` for apply-only payments. Tests 57→61 (credit line shape, over-application refusal, $0 apply-only, credits-only refusal, remittance credit rendering). |
| 2026-08-26 | pay run v0.1.0 | **Pay Bills: pay several bills to one vendor with ONE payment, ONE QuickBooks entry, and ONE remittance advice.** Ask (Sky): "start on pay run either by vendor or bills and pay multiple bills on one payment with remittances etc". **Built ON the Phase 3 rail, not beside it:** `ops.vendor_payments` stays one row per bill — its partial unique index (one LIVE payment per `qbo_bill_id`) is the duplicate guard and it must survive batching — and a new parent **`ops.vendor_payment_groups`** (migration `20260826d`, applied live) carries what is singular about the payment: the one Stripe payout id (the row-level unique index can't hold when five rows share one payout), the one **multi-line QBO BillPayment** (`buildBillPaymentPayload()` in `lib/vendor-payments-lib.mjs` — one Line + LinkedTxn per bill; the single-bill `recordQboBillPayment` now delegates to it so the two paths can't drift; refuses the same bill twice, and TotalAmt is float-noise-proofed), the check #, and the remittance send record. **New `/api/vendor-pay-run`** (`vendor-pay-run.mjs`, superadmin — same gate as vendor-pay): `list` (payable bills grouped by vendor + per-vendor Stripe readiness + balance) / `pay_stripe` / `record` (manual rails; qbo_billpay books nothing — Bill Pay already posted its own) / `remit` (resend). **Ledger before money:** group + per-bill rows insert BEFORE any payout or QBO write; any collision (a race with a single-bill Pay click elsewhere) aborts the whole batch with nothing paid, and a QBO refusal on a manual batch fails clean — the BillPayment is one atomic QBO write, so nothing half-books. **Remittance advice** (`lib/remittance.mjs`): one branded email per payment listing every bill (bill # / bill date / job / amount + total, everything HTML-escaped — vendor names come off OCR'd PDFs), recipient = pay-time choice → vendor contact email; sent immediately for manual rails, **at SETTLEMENT for Stripe** (webhook's new `settleGroup()` — an in-flight payout is not yet a payment); a failed send never fails a payment (stamped on the group, resendable from the page). **UI: Brixpense → Accounts payable → Pay Bills** (`PayRun.tsx`, staff-only nav item — first per-item `staffOnly` in the sidebar): vendor sections with per-bill checkboxes + select-all, due/overdue badges, in-flight rows shown locked, an editable remittance-email field, both rails, and a Recent payments panel (status, remittance state, resend). **Watcher: none added, deliberately** — batch rows are ordinary vendor_payments rows, so `fn_vendor_payments_health()` already reds a stuck or failed pay run. Tests 57 passing (+10: BillPayment payload shape/dedup/rounding, remittance content/escaping/rail wording). |
| 2026-08-26 | bill-paid-sync bills only | **The QuickBooks paid-check was flagging every posted Purchase as "deleted in QBO" — 20 false positives ($21.6k) drowning 3 real ones.** Found while triaging the yellow `bill_paid_sync` light: the sync's pool query filtered `qbo_bill_id not null + paid_at null` WITHOUT `as_bill=true`, then asked QBO's **Bill** entity for each id — but a posted Purchase's `qbo_bill_id` is a Purchase id (the column predates the split), so QBO "didn't return" every one of them and they were all recorded checked-with-null-balance = "missing". **Fixed:** `lib/qbo-bill-status.mjs` pool query + all three counts in `ops.fn_bill_paid_sync_health()` now scope `as_bill=true` (migration `20260826c`, applied live), and the meaningless check-stamps were wiped off the Purchases. The 3 REAL missing bills — the June "Brix Beverage" self-billed rows from the retired expense-to-bill flow (QBO Bills 168463/168468/168489, deleted in QBO) — were soft-archived with an explanatory `archived_by` (QBO is the source of truth; someone already deleted them there). Board verified: `bill_paid_sync` green pending next run, zero non-green checks except the operator-actionable `ap_inbox` red. |
| 2026-08-26 | attach-after-post + weekly reconcile | **Two loops closed: a late-arriving bill document can now reach the QBO transaction, and a weekly reconciler proves the SF pipeline missed nothing.** Both from Sky's "what else are we missing" review. **(1) Attach-after-post.** Receipts were pushed to QBO exactly once, at posting — a bill posted before its document arrived (the NORMAL case under 2026-08-25's data-only landing) could never get the file into QuickBooks, and the posted row's read-only form couldn't even take one. Now: `expense-request-link-bill` gains `mode=attach` (posted rows only; verifies the attachment belongs to the request under the CALLER's RLS before the service-role push; pushes exactly ONE attachment row via `attachReceiptsToQBO(..., {attachmentId})` so re-attaching can't duplicate files QBO already has — that function has no QBO-side dedup; audit-logged into `expense_approvals`). `ExpenseForm` shows an attach drop-zone on posted rows: upload → save on the Brixpense record → push onto the existing QBO Bill/Purchase, one gesture; a failed QBO push says so while keeping the file saved locally. **(2) Weekly reconciliation** (`sf-expense-reconcile-background.mjs`, pg_cron Mondays 16:00 UTC): re-reads every SF expense on Invoiced jobs updated in 30 days, computes the SAME dedup key as the lander (⚠ `candidateKey()` must change in the same commit as `expenseKey()` — header comment says so), checks each against `expense_requests`, **auto-lands stranded ones via `?landJob=`** (idempotent drafts, gate holds, 10-job cap/run) and emails a digest to `SF_RECONCILE_ALERT_TO` (default service@brixbev.com) ONLY when something was found. Migration `20260826b` (applied live): `sync_log` source allow-list + `ops.fn_sf_reconcile_health()` (red on error or >8d silent) wired into `fn_sync_health_extra` via the read-modify-write pattern (anchor-asserted — never rebuild that function from a copy) + the cron. This is the answer to "why is this having trouble weekly": every landing path stays best-effort, but a miss now surfaces in a Monday email instead of when an operator notices. |
| 2026-08-25 | lifecycle tabs everywhere | **The Open / Posted / Paid & closed tabs are now the ONE vocabulary on every expense list.** Ask (Sky): "all expense inboxes and everything should have the same tabs don't you think?" **Shipped:** the bucketing rule and pill row extracted to `lib/lifecycle.ts` + `components/LifecycleTabs.tsx` (one rule, one component — a page wanting different buckets wants something other than lifecycle tabs, not a fork) and applied to **SF Expenses** (refactored onto the shared pieces), **Expense History** (`PendingList` — terminal states `denied`/`fulfilled` land in Paid & closed, and a posted+paid row's badge reads "Paid <date>"), and the **Vendor Inbox** (`BillsInbox` — its `posted`/`unpaid`/`all` pills folded into the Posted / Paid & closed tabs; the triage pills — Waiting on you / Awaiting approval / Ready to post / Unassigned / Needs attention / Everything open — now render only inside the Open tab, since triage IS the open state; intake items with no linked request are open by definition; no backend change — `bills-inbox.mjs` already returned `as_bill`+`paid_at`). **Deliberately untouched: My Inbox** — that page IS the Open tab across all lists by design ("everything waiting on you"; its own header points filed/posted rows at Expense History), so giving it Posted/Paid tabs would just duplicate Expense History inside an action inbox. |
| 2026-08-25 | sf lifecycle tabs + switch docs | **SF Expenses gets Open → Posted → Paid & closed tabs, riding the existing QBO paid-sync; the `SF_FETCH_RECEIPTS` switch is now documented where it will be found.** Ask (Sky): bills paid in QuickBooks (a cheque, a Bill Pay run — most payments happen outside Brixpense) "should automatically just go to a paid and close tab", and the receipts switch "needs some explaining somewhere so we don't forget about it." **The paid plumbing already existed** — #402's `bill-paid-sync` (daily 11:40 UTC + the aging strip's Check QuickBooks button) asks QBO for every posted unpaid bill's balance and stamps `paid_at` from the linked BillPayment when it hits zero — so this is UI + docs only. **Tabs (`SFExpenses.tsx`):** Open (draft/approved — needs a human), Posted (in QBO, money still owed; shows "Partly paid — $X left" from `qbo_balance`), Paid & closed (`paid_at` set by anyone/anything, or a posted Purchase — `as_bill=false` was paid before it posted). Counts per pill; header total follows the visible tab; per-tab empty states explain how rows move. No new backend, no new cron. **Docs:** `SF_FETCH_RECEIPTS` gets a CLAUDE.md gotcha bullet (it lives in the SUPABASE edge env, not Netlify — the trap someone would hit) and a dedicated section in `architecture/BRIXPENSE.md`. |
| 2026-08-25 | sf data-only landing | **SF expenses now land DATA-ONLY (vendor/amount/date/job) — the bill document is the human's to attach in Brixpense.** Ask (Sky): "Rather than attaching the PDF image in Service Fusion… just take the total and the name of the vendor over into expense and just keep it open to attach the bill into" — the receipt file only exists on the admin-portal job page, and that scrape is the flakiest link in the pipeline (intermittent 20s+ SF-side hangs; "this issue with API in Service Fusion"). **Shipped (edge fn v24):** new `SF_FETCH_RECEIPTS` env flag, default OFF — `landJob` skips the attachment check + receipt download entirely, and `resolveJobAssets` skips the jobView scrape (the slower half of the portal round-trip), keeping only the cheap search call for the SF# deep link on rows that lack one. `?receipts=` diagnostics still resolve URLs regardless; flip the flag to 1 in Supabase env to restore auto-attach with no deploy. Sweep results now report `fetchReceipts`. **Frontend:** `ExpenseForm`'s details step gains attach controls (drop zone + Camera/Upload + PDF-chip with remove) for any non-read-only expense — previously the upload UI existed ONLY on the New Expense flow, so an open draft literally could not take a file; picking one runs the same OCR autofill and saves to the draft on Submit. The SF held-banner copy now says the document is yours to add. OCR-cron behavior unchanged: attachment-less SF drafts land `ocr_status='no_attachment'` (held from nothing — the manual gate already holds everything) with the one-time email as the "go attach the bill" nudge. |
| 2026-08-25 | brixpense qbo-mirror dup scan | **The duplicate gate now also sees HAND-KEYED QuickBooks bills.** The v0.5.0 gate (`lib/expense-dupes.mjs` → `ops.fn_bill_duplicate_candidates`) blocks when another BRIXPENSE row for the same vendor+bill# is already posted — but a bill someone keyed straight into QBO has no Brixpense row, so posting the Brixpense copy silently double-booked it (Sky's worry after the 12 recovered SF drafts — all 12 were verified clean against the mirror, vendor-fuzzy AND amount-only, before this shipped). **Shipped:** `expense-request-link-bill`'s duplicate gate gains a second check — `findLikelyQboDuplicates()` scans `ops.qbo_expense_lines` (service-role read) for bills/purchases in the last 60 days with the same amount that NO `expense_requests.qbo_bill_id` owns. Amount-only on purpose (hand-keyed vendor spellings differ: "ERIC SERRANO" vs "Serrano Refrigeration HTG&AC"); linked-txn exclusion keeps real sibling expenses (Serrano bills $170 flat, repeatedly) from false-positiving. Speaks the existing protocol (409 `possible_duplicate` + `can_override`; `confirmDuplicate` bypasses both checks in one confirm), so the shared `lib/postToQbo.ts` frontend needed zero changes. Best-effort: a failed mirror read never blocks a post. |
| 2026-08-25 | visitor sign-out | **Sign-out fixed, and the kiosk asks for a first name instead of a badge number.** Reported (Sky): "the compliance and safety portal isnt signing people out also i dont want to have to type a badge number in to sign them out, have it ask them to put their first name in, and then take them to 'sign out' when they click their name." **(1) The staff sign-out was a GRANT, not a policy.** `20260818e` enabled RLS and wrote a staff `FOR ALL` policy on `ops.visitor_visits`, but the table was only ever GRANTed **SELECT** to `authenticated` — and **Postgres checks table GRANTs BEFORE RLS**, so every PATCH from `/compliance` → Visitors was refused no matter what the policy said, and the button read as dead. Migration `20260825a` (applied live) grants UPDATE on visits + INSERT/UPDATE on `visitor_settings`, and **revokes the leftover anon SELECT on visits** — those rows are personal data (name, phone, photo path, signature), and RLS alone made that a policy detail rather than a hard wall. Staff get no INSERT/DELETE: rows are the kiosk's to create and are a compliance record. Verified live by running the UPDATE as `authenticated` under a superadmin JWT (rolled back). Same trap brix-order hit in its 1.10 session — **a new `ops.*` table needs its GRANTs written next to its policies.** **(2) Kiosk sign-out is now a name lookup.** Nobody at a door remembers `V-20260818-004`, and reading it off a lanyard while holding a laptop is worse. `#s-out` asks for a **first name** (Enter works), lists the people **currently on site** who match as big tappable rows — name, company, "here since 2:14 PM" — and one tap signs that person out. New `action:'find'` on `visitor-signout.mjs` backs it, and it is deliberately **minimal-disclosure**: open visits only, **first name + last initial**, company and time in — never an email, phone, purpose or photo. That is no more than a person standing in our lobby reads off the badges walking past. Input is stripped to name characters before it reaches a PostgREST filter and needs 2+ characters, so a single letter can't list the building; results cap at 8. **The badge QR now finishes the job** — `/visitors?out=<badge>` used to drop the scanner on a pre-filled form; it signs them out on arrival. A failed sign-out re-enables the row so it can be re-tapped. **(3) Bug found alongside:** the Visitors log's **Photo** button is a `.filebtn` carrying `data-vphoto` and no `data-path`, so the page's older delegated file handler also matched it and fired a junk `compliance-docs/null` fetch next to the real one. That handler is now scoped to `.filebtn[data-path]`. **Verified** headless: the two-letter guard, no-match copy, Enter-to-search, the rendered match list and its `find` payload, tap-to-sign-out and the confirmation, the QR deep link going straight to done, and an error re-enabling the row. |
| 2026-08-23 | brixpense v0.7.0 | **A W-9 in hand IS the vendor record, and Settings became three tabs instead of six stacked panels.** Ask (Sky), two parts. **(1) "i meant w9 to be turned into a vendor and saved thats what i meant."** The v0.6.0 drop zone could only file a W-9 against a vendor that ALREADY EXISTED — which is the case that does not arise. The one that does: a W-9 arrives for a firm nobody has paid yet, and the form already carries every field the record needs (legal name, business name, entity type, TIN, address). Keying that in by hand and then uploading the document is work the document can do. `VendorDocDrop` now also renders on the Vendors LIST with no `vendor_id`, and a W-9 dropped there creates the vendor through the SAME `lib/vendor-doc-apply` mapper — one record shape whichever door it came through. **It matches an existing vendor first and files against them instead** (normalised display name → legal name → TIN), because a duplicate is worse than no record: it splits bill history and hides the vendor from the 1099 worklist, which is the exact failure the 1099 page exists to prevent. The response says which happened, why, and links to the vendor either way. **A COI or a bill still requires a vendor** — neither identifies a payee well enough to mint one (a certificate names the INSURED, who may not be who we pay; an invoice letterhead is a trade name). **(2) "the settings section needs to be cleaned up… tables and organization that have cards or tabs that are clean."** Settings was one column of six unrelated panels, and per-department config was spread across THREE separate lists (departments, approver routing, default GL account) — so setting up a department meant finding it in three places and spelling it identically each time. Now: three tabs (**You** / **Organization** / **Connections**) of cards, with the department config as ONE table — department · approver · default account · remove, plus an add row. **Merging the lists surfaced a real bug and fixed it: renaming a department orphaned its routing AND its GL account**, because both are keyed by the department STRING — the rename now carries both across, which is what anyone fixing a typo already assumes is happening. The save bar is sticky IN-COLUMN, not fixed to the viewport (fixed sits under the collapsible sidebar), and clears the mobile tabbar. Verified by rendering the rebuilt page headlessly against a mocked PostgREST — tabs, the table, and specifically that a rename keeps the approver and account attached. Tests 52→55. |
| 2026-08-23 | brixpense v0.6.0 | **Drop a W-9, a COI or a bill on a vendor and it files itself.** Ask (Sky): "can i drag a 1099 pdf into the vendor file along with a vendor bill to get the vendor setup properly… also can we drag the insurance in?" **Answer was no on both, and the reason was interesting**: the W-9/COI OCR (`lib/vendor-doc-ocr.mjs`, ACORD-25 + W-9 schemas) already existed and was already good — it was reachable ONLY through the vendor's own emailed onboarding link, so staff holding a document had no way in at all. **And that path only half-worked**: `runW9Ocr` extracted `entity_type`, `tin_type` and `address`, wrote them into a free-text notes SENTENCE, and dropped them — so the structured 1099 columns added earlier the same day stayed empty and a vendor whose W-9 was sitting in the vault still read as needing one. The extraction was never the missing piece; landing it in columns was. **Shipped: (1) `lib/vendor-doc-apply.mjs`** — the one mapper, used by BOTH the vendor link and the new staff path so a form produces the same record whichever door it comes through. `mapEntityType()` handles the W-9 line-3 vocabulary incl. the LLC election variants (**"taxed as an S corporation" must land on `llc_s`, not `s_corp`** — the exemption is the same but the classification is what a human reads back; regex order is the only thing stopping it, and a test pins it). An ambiguous value is **left blank and reported, never guessed** — a bare "LLC" genuinely does not say, since a single-member LLC checks the INDIVIDUAL box. `is_1099` is deliberately never set from the form: an exempt payee code is about BACKUP WITHHOLDING, not 1099 reporting, and reading it as an exemption would silently drop a real obligation. A curated `legal_name` is never overwritten by OCR. An unreadable form still gets FILED with the fields blank and a warning — losing the document because the model choked is strictly worse. **COI expiry is the EARLIEST line, not the GL one** (`coiExpiration()`), because a certificate is only as current as the first coverage to lapse — that date is what the weekly digest chases; the additional-insured caveat from SOP-11 (the box is not the endorsement) is recorded on every row. **(2) `vendor-doc-upload.mjs`** (staff-gated) + a drop zone on the vendor record: several files at once, and it **classifies the document rather than making you pick from a dropdown first** — that friction is why paper sits in an inbox instead of getting filed. Classification is a cheap Haiku pass over page one that always reports what it decided, so a wrong guess is visible and one click to correct, and it ASKS when it cannot tell. A form 1099 is explicitly classified 'other', not 'w9' — running the wrong extractor on it produces confident nonsense. Creates the vendor's compliance-vault party on the fly if they have none. **⚠ A dropped BILL creates NO expense row.** It is read for what it says about the VENDOR (terms, legal name — BLANKS only, never overwriting curated values) and the read is handed to the expense form via sessionStorage (line items don't fit in a query string, and an invoice's contents have no business in browser history) behind an explicit "File this as a bill" click. A money row must not be a side effect of a drag gesture — the 2026-08-14 gate, one step earlier. **(3) `ExpenseForm` gains `?prefill=`** — resolved through the SAME `pickCogsAccount` the receipt-upload path uses, because setting a GL label on its own would leave it beside whatever id the remembered defaults put there: a row showing one account and posting to another, the exact failure the bill rules guard against. The effect was also **moved below its `useCallback`** — safe inside a deferred effect, but it is the temporal-dead-zone shape that blanked `compliance.html` on 2026-08-18 and not worth leaving to reasoning. Tests 44→52. Docs: `VENDOR-1099.md` gains a "Filing the paperwork" section. |
| 2026-08-23 | brixpense v0.5.0 | **AP hardening: the inbox gets a watcher, bills get a duplicate guard and a due date, and W-9s get chased in August instead of January.** Ask (Sky): "do it all" — the six gaps named in the previous reply. **(1) The AP inbox had no watcher**, which broke this repo's own no-pipeline-without-a-watcher rule (my omission when it shipped). New `ops.fn_ap_inbox_health()` + `ops.sync_log` writes from `bill-email-process-background`. Deliberately NOT a heartbeat — the pipeline is event-driven, so a quiet day is a quiet day and colouring on it would flap the way `sf_token` did before 2026-08-06. What it watches is mail we ACCEPTED and then failed to finish: red on `received`/`processing` older than 30 min or a processor error in 24h; yellow on unreadable mail with no expense request and not yet dismissed (so the light is CLEARABLE — a permanent amber is one nobody reads); green otherwise. Zero mail ever received reads green and says the ambiguity out loud, because Postgres cannot tell "nobody has emailed a bill" from "the webhook was never pointed here". **The wiring into `fn_sync_health_extra` is a read-modify-write against the LIVE definition, not a pasted body** — it inserts one line and raises if the anchor moved, so it cannot repeat the 2026-08-21 incident where a rebuild from an older copy silently deleted somebody's monitor. Verified after apply: 19 checks, all green, every prior monitor intact. **(2) Duplicate-bill guard.** The only dedup was on the Resend email id, which catches a webhook replay and nothing else; the duplicate that costs money arrives by a second road. `ops.fn_bill_duplicate_candidates` holds the rules so the automated and human paths can't drift — **exact** (same vendor + bill number) and **likely** (same vendor + amount within 10 days, no bill number) — and is deliberately NOT a unique constraint: a hard one would reject a corrected re-issue or an OCR misread and a pipeline that throws on insert loses the document. **Validated against live data, which then corrected the design**: replayed over every expense on file the amount rule produced three clusters, and only ARTURO SANTIAGO $375 on 08-11 (2 rows, 1 job — the QBO Bills 173048+173049 pair recorded on 08-14) was real; DESERT BEVERAGE and ERIC SERRANO were same-amount/different-job. Added a job-number discriminator → 3-of-3 correct. At intake an exact match drops an auto-approved bill back to `draft`; at POST time (`expense-request-link-bill`, the last point before a real QBO transaction) an exact match whose twin is ALREADY IN QUICKBOOKS refuses 409 and `confirmDuplicate` is recorded in `duplicate_cleared_by`, not just obeyed — two unposted drafts are a tidiness problem, not a money problem. New shared `lib/expense-dupes.mjs`; a check that cannot run returns null and never blocks a bill (a missed flag costs a conversation, a lost invoice costs a vendor call). Client side, five pages each had their own near-identical post fetch — extracted to `lib/postToQbo.ts`, because a page that doesn't know about the 409 shows "possible_duplicate" as an error and dead-ends. **(3) Due dates + AP aging.** `expense_requests` had NO due date, so an unpaid bill could sit indefinitely with nothing saying it was late. OCR now extracts `due_date` and `payment_terms` separately (printed only — it never derives one from the other); a printed date beats a derived one and `due_date_source` records which. New `ops.v_ap_aging` (security_invoker, so RLS decides whose payables you see) + an aging strip on both inboxes + per-row due/overdue badges. ⚠ **Three copies of the terms→date logic now exist** (SQL backfills history, `lib/due-date.mjs` stamps new bills, `lib/dueDate.ts` fills the field as you type) — `tests/due-date.test.mjs` pins them to one table; add a case there before touching any. Testing caught a bug all three shared: **`2/10 Net 30` read the discount percent as the term**, making such bills due two days out and instantly overdue; they now prefer the number after `net`. Unrecognised terms return null, never a guess — a wrong due date turns a genuinely overdue bill green. **(4) 1099 / W-9.** `vendors.w9_status` existed and nothing read it. Added the W-9 fields (`tax_classification`, `is_1099` override, `tin_type`, `w9_received_at`, `backup_withholding`, `tax_address`) + `ops.fn_1099_candidates(year, threshold)` over the QBO expense mirror + a **1099s** page with CSV. ⚠ Labelled a WORKLIST, not a filing, on screen and in the doc: the mirror is ACCRUAL and 1099 is CASH, so a December bill paid in January is in the wrong year — QuickBooks' module files the forms. The value is timing (chase in August, not January). `is_1099` is an override because the corporate exemption has carve-outs (attorneys, medical) that no W-9 checkbox can express. The likely-individual sort is a HINT that only reorders, never filters — dropping a real obligation because a sole proprietor trades under a company name is the failure that costs money. **(5) `process-inbound.mjs` was NOT deleted — deleting it would have broken the AP tool.** I'd proposed removing it as redundant; `public/index.html:64` calls it for the live PDF drop, so that was wrong on the facts. Did the narrower correct thing instead: removed the unverified SendGrid/Mailgun multipart + `attachments[]` **email-webhook** branches (nothing pointed at them, they had no signature verification per their own SECURITY TODO, and a forged POST would mint a real signed bill-approval token), kept the browser path, added a 10 MB + media-type cap on what one unauthenticated request can cost at the OCR provider. Verified: multipart 415, forged `attachments[]` 400, oversize 413, and the AP tool's exact payload still reaches OCR. ⚠ **Still open, deliberately not changed:** that browser path is unauthenticated because `public/index.html` carries no session (unlike approve/setup/control/dashboard) — adding a login to a daily driver's front door is the owner's call. **(6) Mobile + settings.** Both CLAUDE.md gaps were STALE: the mobile drawer and bottom tabbar already exist, and Settings already has the Organization editor for `expense_settings`. What was actually missing: the tabbar still pointed at the pre-restructure nav (Approvals is now a strict subset of My Inbox), and the AP inbox's `ap_inbox` config had a working `settings` endpoint with **no UI at all** — so `default_approver` and `sender_routes`, the two levers that decide who owns an emailed bill, were frozen at their seeded values. New `ApInboxSettings` panel on the Vendor Inbox (config and the queue it produces on one screen) + a Settings signpost. **Also hardened while in there:** the `settings` action was gated only by `requireBrixpense`, so anyone who could work the queue could rewrite the routing policy — now staff-only, matching how Vendors and Bill Rules are gated. **Found while probing the new check against synthetic rows — a bug in two OTHER monitors.** `ops.sync_log.source` carries a CHECK allow-list that `brixpense`, `distributor` and `vendors` are not on, and every writer wraps its log call in a try/catch (correctly — a logging hiccup must not fail a run), so **the inserts were being rejected silently**. `select distinct source from ops.sync_log` returns eight values and none of those three is among them, despite `distributor-notify` running every 15 minutes since 2026-08-20 and `vendor-funding-cron` daily since 08-21. Both of their health checks therefore read green as "has not logged yet" and **could never have gone any other colour** — the exact silent-outage shape the watcher rule exists to prevent, one level up. Constraint extended (migration `20260823060546`), not dropped: an allow-list is what stops a typo'd source orphaning a check, which is this same bug. Add to it in the same change that adds a writer. All five branches of `fn_ap_inbox_health` then verified against synthetic rows (stuck → red, processor error → red, unreadable → yellow, dismissed → clears, drafted → green). Migrations `20260823053156` / `053336` / `054530` / `060546` (applied live). Tests 32→44. Docs: `AP-BILL-INBOX.md` (watcher, duplicates, due dates), new `VENDOR-1099.md`. |
| 2026-08-23 | brixpense v0.4.0 | **Inboxes renamed and split, bill rules that auto-populate, and expense reports that cross payment types.** Ask (Sky): SF stays, "previous expenses" should be **Expense History**, add a personal **inbox**, add the **Vendor Inbox** as the master pile "and everybody should have that access as well so it's basically anything unassigned"; plus **rules** for specific/recurring vendor bills that auto-populate; plus **reports** with multiple **books** tying expenses together across payment types linked to a tag/job. **(1) Nav.** Expenses = My Inbox · Service Fusion · Expense History (renamed from Previous Expenses); Accounts payable = **Vendor Inbox** (renamed from AP Inbox) · Expense Reports; Vendors = Vendors · Bill Rules. New `MyInbox.tsx` answers one question — what is waiting on me — in three buckets (Waiting on you / Needs a fix / Ready to post), scoped by the existing `expense_requests_select` RLS rather than filtered client-side. **(2) "Everybody" had to be defined, not taken literally.** The Vendor Inbox drops `staffOnly`, but this is the SHARED Supabase project — brix-order customers, distributor partners and melt users authenticate here, and a vendor invoice carries our costs and GL coding. So new **`ops.fn_has_brixpense()`** (superadmin ‖ `user_metadata.modules` contains 'billing' ‖ legacy role map admin/finance) mirrors the gateway's `grantsAccess()` billing bucket; `bill_email_intake` SELECT moves from `fn_is_staff()` to it. **Verified live against 8 login shapes**: superadmin/admin/finance/modules-billing in; modules-orders-only, customer, distributor, bare login out. The API uses the SAME predicate via new **`requireBrixpense()`** — `requireAuth` with the role check OFF (`allowedRoles = null`, which it already supports) then `hasBrixpenseAccess()` — because a hard-coded role list in the function would drift from the RLS the first time someone is granted access by MODULE rather than role. **(3) Bill rules.** `ops.expense_rules` + pure matcher `lib/expense-rules.mjs` + `expense-rules.mjs` (CRUD + **dry-run**) + `BillRules.tsx`. Match on vendor/sender/text/amount-range (case-insensitive substring, inclusive bounds, ALL conditions must hold, lowest priority wins — deliberately no regex: a bad one in a config field is undebuggable); set department/entity/GL/tag/job/customer/owner. Applied in `bill-email-process-background` after OCR, stamping `expense_requests.applied_rule_id` so "why is this account on here" is answerable, and recording match_count/last_matched_at/last_amount. **⚠ A rule NEVER posts** — the 2026-08-14 gate is not something a config row may bypass. Four design rules, all tested: a rule fills BLANKS (OCR's vendor/amount/date always wins); an amount bound never claims a bill whose amount we could not read (unknown ≠ zero); a GL id never travels without its label; a bare domain matches the domain but a full address matches only that person (substring matching would let `acme.com` match `notacme.com.evil.net`). A conditionless rule is refused by BOTH the endpoint and a DB CHECK — it would claim every bill. Recurring rules carry period + usual amount + tolerance and FLAG (never block) a bill outside it — the monthly service that quietly triples. The editor's **Test** button replays a draft against the last 100 real expenses and warns when it matches >half of them. **(4) Expense reports (books).** `ops.expense_books`/`_book_items` + `v_expense_book_totals` (security_invoker) + `expense-books.mjs` + `ExpenseReports.tsx`. A book bundles expense_requests ACROSS payment types (card, check, emailed bill, SF job expense) tied to a tag/job/period; totals break out **by payment type**, GL account and entity — payment type being the axis an expense report is read on and one no single column carries. Membership is EXPLICIT, not a saved filter, because the point is grouping what a filter cannot express. Books never mutate their expenses; deleting one drops only the grouping. CSV export prefix-quotes leading `=+-@` — vendor names come off OCR'd PDFs and Excel would run them as formulas. RLS: Brixpense reads, creator-or-staff writes. Migration `20260823040853` (applied live). Tests 23→32. Docs: `architecture/BRIXPENSE-RULES-AND-REPORTS.md`. |
| 2026-08-23 | ap inbox v0.3.0 | **Approval turned OFF for emailed bills — routing kept, the extra click dropped.** Sky reversed the earlier answer: "No it doesn't need approval." The gate was already a config flag (`ap_inbox.require_approval`), but flipping it as built would have landed bills as `draft`, and `expense-request-link-bill`'s `LINKABLE_STATUSES` refuses to post from draft — so the flag alone would have made bills unpostable rather than easier to post. **Fix:** with the gate off an owned bill lands **`approved`** — the SAME auto-approve every other Brixpense expense gets on submit, stamped `approved_by='system (AP inbox — no approval required)'` so the row says what it is — which puts it in the owner's **Previous Expenses** (`/expense/pending`), a page that ALREADY renders a Post to QuickBooks button for approved+unposted expenses. Zero new UI for the sender, and verified they can actually post it: `expense-request-link-bill` runs as the USER, and RLS `expense_requests_update_self` covers `submitted_by`. `manager_email` is still set regardless of the gate — ownership and RLS visibility are what routing buys and they don't depend on approval. Unassigned mail still lands `draft` in the AP Inbox for triage. Gate ON is unchanged and still supported (`pending` → the owner's `/expense/queue`); the approval machinery stays wired but dormant, so turning it back on is a settings flip with no deploy. UI hides the two approval-only filters when the gate is off and lands on **Ready to post**; notification email says "ready to post" and links to Previous Expenses instead of the approvals queue. **The QBO gate is untouched** — posting is still an explicit human Post to QuickBooks click (2026-08-14). ⚠ Documented plainly in `AP-BILL-INBOX.md`: with approval off there is **no second pair of eyes** on an emailed bill — the sender posts their own; `sender_routes` or `require_approval:true` are the two no-deploy knobs if that ever needs to change. Migration `20260823035728` (applied live). Tests 20→23. |
| 2026-08-23 | ap inbox v0.2.0 | **An emailed bill now lands in a person's approval queue, and can't post to QuickBooks until they approve it.** Ask (Sky): "when different user emails send to the billing email box it sends it to their approval inbox" — routing = **back to the sender themselves**, approval **mandatory before posting**, and for vendor mail with no internal sender, a **mix** of vendor rule / department / default. **⚠ Flagged before building, and it stands: sender-owns + mandatory-approve means the sender approves their own bill — a REVIEW gate (a human must check the OCR against the real document before it becomes a QBO transaction), NOT separation of duties.** `sender_routes` is the escape hatch that buys the real thing without a rebuild. **Shipped:** `resolveBillRouting()` in `lib/ap-inbox.mjs` — a deterministic 6-rung ladder (sender_routes override → the sender if internal → vendor_routes on the OCR'd vendor → department_approvers on the OCR'd GL label → default_approver → **unassigned, held in the AP Inbox for triage**), every answer carrying the REASON so the queue can say why a bill went where. `default_approver` ships NULL on purpose: inventing one would quietly make one person responsible for every invoice a stranger sends us, and a visible pile beats a wrong owner. Rung 2 is gated on `hasBrixpenseAccess()` (mirrors the gateway `billing` bucket) — **the shared-project trap**: matching on "is a login" alone would hand a brix-order customer's invoice a staff owner and a spot in an approval queue. A routed bill lands `status='pending'` + `manager_email` and **shows up in the existing `/expense/queue` with no change to that page** — ManagerQueue already selects on (manager_email, status='pending') regardless of request_type, and RLS `expense_requests_select` matches on manager_email, so an owner who is NOT AP staff sees and approves their own bill. `expense-request-link-bill`'s existing LINKABLE_STATUSES is what enforces the gate — Post stays disabled until 'approved'. **`expense-request-decide` self-approval block opened NARROWLY** (`tag='AP Inbox' AND request_type='expense'` only; PRs keep the hard block) — otherwise those rows would be permanently stuck in their own queue; a self-review is stamped into `approved_by` as "<email> (own emailed bill)" rather than hidden. Verified live that RLS already permits it (update_self + update_manager both match when submitter == manager_email), so no policy change was needed. Also fixed while in there: the decision email said "Your **Purchase Request** was Approved" for every type — now names the actual noun (Bill / Expense / Purchase Request). `bills-inbox.mjs` gains `approve` (offered only to the person it's waiting on, so it can't be quietly cleared by whoever opened the page) + `assign` (triage for unassigned vendor mail), and its auth widened from the requireAuth default (**superadmin ONLY** — would have locked every non-superadmin out) to superadmin|admin, matching `ops.fn_is_staff()`, the RLS on `bill_email_intake`, and the sidebar entry. UI: filters (Waiting on you / Awaiting approval / Ready to post / Unassigned / …), an owner line per row, Approve, Assign, and Post disabled with a reason until approved. Migration `20260823034356_ap_inbox_routing.sql` (applied live). Tests 13→20 (the full ladder + the shared-project trap). |
| 2026-08-23 | ap inbox v0.1.0 | **`bills@alamedapointbg.com` is a real inbox now — emailed vendor bills become reviewable Brixpense bill drafts.** Asked (Sky): does Brixpense read bills@ and route to the sender's inbox, and can we have a general inbox to send bills to. **Answer to the first half was no, and nothing did** — the address was advertised in Brixpense's 3rd Party Bills page (`FORWARD_TO`) but had NO webhook behind it: `process-inbound.mjs` (the AP tool's scanner) is deployed and reachable yet is not registered as a Resend endpoint by any of the 10 live webhooks, and `vendor-email-intake` — which DOES receive the domain-wide `email.received` fan-out — found no `bills@` row in `ops.vendor_email_routes` and returned `ignored: no matching route`. (Both seeded vendor routes are also `active=false`.) Verified with two real test emails Sky sent 2026-08-22 19:13 PT: both sat in Resend, unprocessed, PDFs intact. Also note `process-inbound` never emailed the *sender* — it emails a single fixed `APPROVAL_EMAIL` (default `wgrandell@brixbev.com`) — and has no signature verification at all (its own SECURITY TODO). **Shipped:** migration `20260823024339_bill_email_intake.sql` (applied live) — `ops.bill_email_intake` (document-of-record per email, deduped on the Resend id, staff-SELECT RLS via `fn_is_staff()`, service-role writes) + `ops.expense_settings` key `ap_inbox` + the `AP Inbox` tag. New `bill-email-intake.mjs` (Svix-gated Resend webhook → records → hands off) and `bill-email-process-background.mjs` (attachment → OCR through the SAME `lib/expense-ocr-core.mjs` humans use → PDF into `expense-attachments` → an **unpaid bill DRAFT** in `ops.expense_requests`, `as_bill=true`, `tag='AP Inbox'`). **Nothing auto-posts** — the 2026-08-14 gate holds and this channel is exactly why: an email from outside the company must not be able to create a QBO transaction, so every draft waits for a human **Post to QuickBooks** (`expense-request-link-bill`). That gate is also what makes the deliberately OPEN sender policy safe (empty `allow_senders` = vendors can invoice us directly; worst case is a row in a queue). New staff-only **Brixpense → AP Inbox** (`/expense/bills`, `BillsInbox.tsx` + `bills-inbox.mjs`): queue with filters, the original PDF, Post to QuickBooks, Try again, Dismiss, and a **Check the intake** button that reports **armed, not verified** — a 401 proves only that *some* secret is set, so only a real email proves it matches. New `lib/resend-inbound.mjs` ports brix-order's proven inbound-attachment reader **plus the lesson**: `no_attachment` and `attachment_fetch_failed` are separate statuses and every failed Resend read is quoted verbatim into `diagnostics` — collapsing those is how brix-order lost a PO for a day to a send-only key. ⚠ Reading inbound mail needs a **FULL-ACCESS** Resend key (`RESEND_INBOUND_API_KEY`), and Resend mints a **separate signing secret per webhook endpoint** (shown once), so this route **cannot** share `RESEND_INBOUND_SECRET` → `RESEND_AP_INBOX_SECRET` (either may hold a comma-separated list, the only way to rotate). New `tests/ap-inbox.test.mjs` (13 tests, `npm test`) written against the live API shapes from Sky's two real emails; it caught two genuine bugs — a **case-sensitive header lookup** that let every `Auto-Submitted` out-of-office through as a bill, and `stripHtml` indenting every line of a pasted invoice. Docs: `architecture/AP-BILL-INBOX.md`. **Not built, on purpose:** paying the bills from here — `payment_method`/`paid_at`/`qbo_billpayment_id` exist on the row and no UI writes them; a pay run is a separate build on top of this queue. ⚠ Go-live needs two Netlify env vars + a Resend webhook (see the doc); until then forwarded bills are recorded by Resend but not processed. |
| 2026-08-23 | ci cleanup | **A CI job that could never run, and the stale note that justified it, both removed.** `.github/workflows/build-expense.yml` had failed on EVERY main commit since at least 2026-08-14 — nine days of a red check nobody could act on. Three independent reasons it's gone rather than fixed: (1) it can't run — `npm ci --prefix app-expense` and its setup-node cache both need `app-expense/package-lock.json`, which `.gitignore` line 16 excludes, so it dies before install; (2) it's redundant — `netlify.toml`'s build command already runs `npm install && npm run build --prefix app-expense` on every deploy (verified live: production serves `Vendors-IorbnbS4.js`, a hash that exists nowhere in this repo, while the committed `public/expense/` has been stale since 2026-08-17); (3) its actual job was to **commit built bundles back to main**, which is the churn-against-parallel-PRs practice abandoned on 2026-08-18. The root cause of all three was a **factually wrong NOTE in `.gitignore`** claiming "Netlify's deploy doesn't appear to be running our build command" — disproved twice and never corrected, so the workflow it justified outlived it. Note rewritten to the verified fact + an explicit SHIP SOURCE ONLY rule. **New `supabase/migrations/README.md`** — the convention that never existed: `YYYYMMDD<letter>_<slug>.sql`, list the directory before picking a letter, migrations apply via the **Supabase MCP not the CLI** (there is no `supabase/config.toml`; `schema_migrations.version` is an apply-time timestamp and `name` holds the filename, so nothing keys on the prefix and `supabase db push` is inoperable here), and the shared-function trap in full — never re-declare `ops.fn_sync_health_extra()` from a copy in an older migration, read `pg_get_functiondef` first. ⚠ **The 7 existing colliding prefixes (`20260514a/b/c`, `20260704j/k`, `20260820a/b`) are deliberately NOT renamed** — 5 predate this work, nothing functional depends on the prefix, docs cite the names, and renaming would only conflict with in-flight branches. Also left alone on purpose: the 80+ tracked files under `public/expense/`, `public/sales-next/`, `public/distributor/` — untracking them is the logical end of this cleanup but touches paths several active branches are writing to. |
| 2026-08-21 | vendor portal 3b | **The Stripe payout float is now a QuickBooks bank account, and funding it books itself.** Sky: "for bookkeeping we open a stripe account for funding, and then as it decrements it gets itemized out of that account? so we call that account the STRIPE Vendor Funding account. Can we set stripe to auto pull from an account when a balance falls to a specific amount and then do the bookkeeping?" **The model is right and it buys a control we didn't have:** QuickBooks holds a BANK account **Stripe Vendor Funding**; money in is a QBO **Transfer** from Chase 72 (the bank feed shows the debit — Match it), and every vendor payment is a **BillPayment drawn on that account**, so the register itemizes per vendor per bill. That makes the account's QBO balance equal the live Stripe balance, and any drift a real signal (money moved without booking, or a top-up nobody entered) instead of noise buried in Chase. The itemization needed NO new code — `recordQboBillPayment()` already draws on `QBO_VENDOR_PAY_BANK_ACCOUNT_ID` (Chase `72` by default); create the account and repoint the env var. **Stripe cannot auto-pull on a low balance** (verified in their docs, not assumed): pulling from a verified bank account is US-only and explicitly manual per transfer — *"You initiate this transfer each time you want to add funds (it isn't an automated, regular transaction)"* — 2–6 business days, capped 50k/txn · 50k/day · 100k/week; the programmatic form is **InboundTransfer API v2**. ⚠ **Stripe's one native recurring route — payments balance → financial account — is deliberately NOT used:** brix-order books Stripe payouts as QBO Deposits into Chase 72 (its sessions 1.80–1.83), so diverting that revenue would strand those Undeposited-Funds payments and break that reconciler. ("Automatic transfer rules" in Balance Settings are Connect-only.) **Shipped:** (1) migration `20260820d` (applied live) — `ops.vendor_funding_events`, one row per funding event keyed on `stripe_object_id`, which is what makes the QBO Transfer exactly-once **so a top-up made by hand in the Stripe Dashboard books itself too**; staff-read RLS, service-role writes; config on `ops.expense_settings` key `vendor_funding` (floor/target/auto_top_up/max_per_day) with **`auto_top_up` FALSE** — money movement starts with a human, same rule as the 2026-08-14 full gate. (2) `lib/vendor-funding-lib.mjs` — config (a garbled row degrades to defaults, never to unlimited), Stripe listing + pull, the QBO Transfer, and `syncFunding()`; **refuses to book when the funding account is unset or equals the source**, because a Chase→Chase transfer is a silent no-op that would break the very reconciliation the account exists for. (3) `vendor-funding.mjs` (SUPERADMIN, same gate as vendor-pay): `status`/`top_up`/`sync`/`save_config`, ledger row before money, capped against both our daily limit and Stripe's per-transaction cap. (4) `vendor-funding-cron.mjs` (daily 16:20 UTC) — reconcile first, then EMAIL the low balance or pull to target when auto-top-up is on; logs `ops.sync_log` (`vendors`/`vendor_funding`) carrying the observed balance + `below_floor`, **because Postgres cannot see Stripe** — that row is what the health check reads. (5) Brixpense → Vendors → **Stripe vendor funding** (superadmin): balance, floor/target, Top up naming the amount and the settlement delay in the confirm, Reconcile with QuickBooks, the auto-top-up switch, funding history with each row's Transfer state. (6) `ops.fn_vendor_funding_health()` in `fn_sync_health_extra` — red on a failed pull in 7d, settled funding unbooked >24h (the balances have diverged), or an errored run; yellow below the floor or no run in 48h. Whole board verified green. **Failure posture:** a QBO refusal leaves the event **settled but unbooked** with the reason on the row, retried next run and red after 24h — the money IS in Stripe, so "funded, not yet booked" is the honest state; a day-old placeholder that never got a Stripe id retires as **canceled** (not failed) noting to check Stripe, since that pull may have gone through. 27 scenario tests pass (config + transfer-direction guards, Dashboard/external discovery, book-once idempotency, failed pull books nothing, QBO-failure retry, placeholder retirement, daily-cap accounting). ⚠ **Still dark until:** the QBO "Stripe Vendor Funding" account + `QBO_VENDOR_PAY_BANK_ACCOUNT_ID` (defaulting to Chase 72 defeats the point), a verified Stripe bank account + `STRIPE_FUNDING_BANK_ACCOUNT_ID`, and **Money Management → Inbound transfers: write** added to `STRIPE_PAYOUTS_KEY`. Balance is $0.00. |
| 2026-08-21 | rebate maker v0.1.0 | **Rebate Maker: contract rebate programs, live accrual, annual settlement → Brixpense check.** Migration `20260821a_rebate_programs.sql` (applied live + verified with staff/non-staff sims and a rolled-back settlement round-trip): `ops.rebate_programs` (chain parent `qbo_customer_id`, payee `qbo_vendor_id`, optional pricing-contract link, Brixpense entity vocab) / `rebate_rules` (types **volume_growth** · **ordering_cadence** · **flat_per_unit** · **tiered_volume** · **fixed_per_store**; item scope = exact ids + ILIKE name patterns, both-empty matches NOTHING; contract ambiguities are config knobs — growth basis all|incremental, cadence period/min-orders/grace) / `rebate_settlements` (partial-unique per program-year, full calc snapshot = the customer data report). `fn_rebate_calculate` (staff-or-service guard **INLINE** — new fn, NOT generator-wrapped; keep the guard on any edit) reads the QBO mirror parent+subs, Invoices net of CreditMemos; **current-year runs compare YTD vs the same prior-year window** and only elapsed cadence windows count, so the accrual preview is apples-to-apples. `fn_rebate_settlement_create` (staff) snapshots + creates the Brixpense expense request (approved, as_bill, tag 'Rebate', bill `RB-<code>-<year>`, one line per rule) — the rebate check posts to QBO only via Brixpense's human **Post to QuickBooks** gate (2026-08-14 rule); `_void` releases the year + archives the unposted request, refuses once posted. **UI: Refractor → Pricing → new Rebates tab** (`app/src/pages/pricing/RebatesTab.tsx` + `lib/rebates.ts`, deep link `#pricing?tab=rebates`): program editor (chain picker, vendor search, contract link), rule editor per type, Calculate (YTD accrual / final year), Run settlement, settlements table with Brixpense link + void. Tables staff-only RLS both directions. **Seeded from the signed APBG–Chicken Coup master agreement (Starbird, 2025-08-25):** program STARBIRD (qbo 730 → vendor CHICKEN COUP LLC 1922, entity brix) + the two contract rebates — 5% YoY BIB growth $2.50/unit (basis 'all') and bi-monthly ordering recapture $0.50/unit (≥1 order per 2-month window), both scoped `3G%`. **Cadence clarified by Sky same-day: the deal is BI-WEEKLY deliveries — at most 2 orders a month** (the contract's 'bi-monthly' wording was wrong); cadence rules gained an optional `max_orders` cap per window and the Starbird rule is now `{period_months:1, min_orders:1, max_orders:2}`. Live YTD 2026: $689.50 (growth $552.50 / 2 stores; cadence $137.00 / only 3 of 14 active stores hold to ≤2 orders/month — grace_windows is the knob if that's too strict). **Rebate report email (same day):** `netlify/functions/rebate-report-email.mjs` (staff) sends the branded 'Your Alameda Soda rebate is ready' report — navy chrome, total, full per-location detail per rule, the reply-to-approve → 30-day-check language — from `rebates@alamedapointbg.com` (env `REBATE_EMAIL_FROM` overrides; replies land there) to recipients typed at send time; stamps `report_sent_at`/`report_sent_to` on the settlement (re-sends merge); UI = 'Email report…' per settlement row + sent chip. Exhibit C franchise terms (same rebates + $3,400/outlet/yr funding) documented in the program notes — add a FRANCHISE program (fixed_per_store rule exists for it) when franchise outlets come online. |
| 2026-08-20 | hardening | **Shared-project hardening pass: RPC guards on 130 functions, SF-token theft path closed, anon invoice mirror closed, distributor write-deny, notify health check.** Migration `20260820b_rpc_guard_anon_hygiene.sql` (applied live + verified with staff/finance/distributor/anon role simulations). **(F1 — the big one:** ~130 SECURITY DEFINER ops functions were executable by ANY login via Postgres's default PUBLIC EXECUTE — including `ops.sf_token_claim_refresh`, which RETURNS the live SF access+refresh tokens: credential theft one RPC away for every brix-order customer/vendor/distributor login; `fn_items_master` exposed purchase costs the same way.) **Fix: a guard-wrapper generator** — each such function is renamed `<name>__i` (grants stripped) and replaced by a same-signature SECURITY DEFINER wrapper that asserts `ops.fn_assert_internal()` (blocks distributor logins; internal roles incl. finance/sales unchanged) or, for the token functions + `qbo_token_diagnostics`, `ops.fn_assert_staff_or_service()` (blocks ALL non-staff authenticated logins; service_role/pg_cron/postgres pass). OID references (views, policies) keep hitting the renamed inner; name-based callers (PostgREST RPC, pg_cron, plpgsql bodies) hit the wrapper. ⚠ **Maintenance trap: `CREATE OR REPLACE` on a wrapped name replaces the WRAPPER (guard silently lost for that fn, `__i` orphaned)** — edit the `__i` inner instead, or re-run 20260820b's generator (idempotent: marker-skips live wrappers, drops stale inners). **(F2)** `ops.qbo_invoices`/`_lines` anon SELECT policies dropped + anon SELECT revoked — the full invoice mirror was readable with the public anon key alone; the only page leaning on it (`public/dashboard.html`) was querying the wrong schema and never worked — now fixed to ride the signed-in superadmin's bearer + `db.schema='ops'` (the page's data actually loads for the first time since the schema split). **(F3)** the 20260818f distributor deny-list policies upgraded from FOR SELECT to FOR ALL — several taxonomy tables (channels/segments/plans/settings) carry permissive `FOR ALL USING(true)` write policies a distributor could have written through. **(F4)** function grant hygiene: EXECUTE revoked from PUBLIC + anon on every ops function (authenticated/service_role grants made explicit first — zero internal change); anon keeps only `fn_is_staff` + `fn_is_distributor`. **(F5)** `distributor-notify` now logs every run to `ops.sync_log` and `fn_sync_health_extra()` gains a `distributor_notify` check (yellow >2h, red >12h/error) — no pipeline without a watcher. Remaining known exposure: none of the flagged items — both 2026-08-18 limits are closed. |
| 2026-08-20 | sub-distributors v0.2.0 | **Sub-distributor phase 2: notifications, fee settlement, Melt-grade agreement e-sign, QBO vendor link.** Migration `20260820a_sub_distributor_settlements_notify.sql` (applied live). **(1) Notifications** — new `netlify/functions/distributor-notify.mjs` (scheduled */15 + staff kick via requireScheduledOrAuth): order_submitted → staff (`DISTRIBUTOR_ALERT_TO`, default service@brixbev.com), order_fulfilled + transfer_shipped → partner (contact_email ∪ active portal users), short receipt (has_discrepancy) → staff, agreement_sent → partner with the portal sign link, agreement_signed → both. Exactly-once via new service-role-only `ops.sub_distributor_notifications` (UNIQUE(event_type, ref_id); failed sends retried next tick; scans capped to 30 days so a deploy can't blast history). **(2) Fee settlement** — `ops.sub_distributor_settlements` + `fn_distributor_settlement_create` (staff; Refractor → Sub-Distributors → Depletions → Generate settlement): sweeps un-settled fee-carrying depletions for a period, stamps them (`depletions.settlement_id`), and creates a **Brixpense expense request** (status=approved, as_bill, tag='Sub-Distributor', bill_number `SD-<code>-<YYYYMM>` → QBO DocNumber, per-account line items, vendor from the QBO vendor link) — so the actual QBO posting stays behind Brixpense's human "Post to QuickBooks" gate (2026-08-14 rule holds). `fn_distributor_settlement_void` releases the depletions + archives the request; refuses once posted. **(3) Agreements** — new `scope` field (territory/accounts/products, shown to the signer in the portal) and the sign RPC now records `signer_ip` + `signer_user_agent` from PostgREST `request.headers` alongside the typed name + signature PNG (the melt-dashboard store_order_approvals audit treatment). **(4) QBO vendor link** — `sub_distributors.qbo_vendor_id` + Refractor Overview vendor search + an "Accounting (QBO mirror)" panel showing the partner's recent `ops.qbo_expense_lines` (what they've billed us) — and the settlement bill lands on that vendor. sync-manifest: sub-distributors writer gains settlements + expense_requests (multi-writer with brixpense); new `distributor-notify` writer. |
| 2026-08-18 | sub-distributors v0.1.0 | **Sub-distributor program: registry + agreements in Refractor, external partner portal at `/distributor`, and the RLS scoping that makes an outside login safe on the shared project.** Sky: manage distribution agreements with Origins Soda Co. / Desert Beverage (etc.), show their inventory in Refractor, and give THEM a portal login to see inventory, receive BOLs, order, and work agreements — for Melt and Starbird territories both. Consignment now (they charge us a per-case delivery fee), sell-in later → `model` field per distributor AND per agreement version. **(1) Migration `20260818f_sub_distributors.sql` (applied live):** `ops.sub_distributors` (+ new `inventory_locations.kind='distributor'`; seeded ORIGINS + DESERTBEV with locations), `sub_distributor_users` (login↔distributor membership, matched by user_id OR JWT email — the RLS key), versioned `sub_distributor_agreements` (draft→sent→signed with in-portal e-sign: signer + PNG data-URL, per-version fee/model; PDFs in new private bucket `distributor-docs`), `sub_distributor_accounts` (which Melt/Starbird stores each partner services), `sub_distributor_orders`/`_order_lines` (SDO-YYYY-#### restock orders; sell-in lines snapshot `ops.resolve_price`), `sub_distributor_depletions` (cases delivered per account, consignment fee snapshot, posts `shipment` movements out of the distributor's location — chain-wide visibility). **Inventory/BOLs reuse the Stock machinery unchanged** — a shipment to a distributor is an ordinary `inventory_transfers` BOL. New RPCs: `fn_distributor_create_order`/`_cancel_order`, `fn_fulfill_distributor_order` (staff; order → draft BOL transfer), **`fn_distributor_receive_transfer`** (per-line counted quantities — shortfall STAYS IN TRANSIT and flags new `inventory_transfers.has_discrepancy` + `receiver_notes`, unlike the internal all-or-nothing `fn_receive_transfer`), `fn_distributor_sign_agreement`, `fn_distributor_record_depletion`. **(2) The RLS work is the load-bearing part:** ~100 ops tables carried `SELECT USING (true)` for every authenticated login on the shared project — an invited partner would have seen every cost, formula, and invoice. Added `AS RESTRICTIVE` policies (`fn_is_staff() OR NOT fn_is_distributor() OR <scope>`): zero change for staff/internal/brix-order logins by construction; distributor logins are narrowed to transfers/movements/locations touching THEIR location, their own QBO invoices/customers + serviced stores, and denied everything else (formulas, WOs, POs, expenses, fleet, HR, pricing, `qbo_items` — replaced by cost-free `ops.v_distributor_catalog`). `v_inventory_on_hand`/`v_sales_lines`/`v_work_orders`/`v_purchase_orders` flipped to `security_invoker` so views inherit the scoping. **Verified live** with a temp membership: distributor context saw 1 registry row, 1 location, catalog names, and ZERO invoices/formulas/items/WOs/movements/expenses; internal context unchanged. ⚠ **Known limit:** SECURITY DEFINER RPCs granted to authenticated (e.g. `fn_items_master`, returns costs) remain callable by ANY login — pre-existing exposure (applies to brix-order customers today too); do an RPC-guard pass before onboarding partners you don't trust. ⚠ Also surfaced, untouched: `ops.qbo_invoices`/`_lines` have ANON read policies — the whole invoice mirror is readable with just the public anon key; needs its own decision. **(3) Refractor → Sub-Distributors** (nav after Production): roster + per-distributor tabs — Overview, Agreements (upload/version/send/signed status), Users, Accounts (QBO store search), Orders (fulfill → BOL), Inventory, Depletions (fee totals — what THEY bill US). **(4) `/distributor` portal** (`app-distributor/`, third SPA via the Brixpense recipe; gateway proxy + admin tile + appswitcher key `distributor` shipped in apbg-gateway same-day): login → dashboard (on-hand, consignment notice), shipments (BOL print, receive flow with per-line counts + typed signature), orders, depletions, agreements (view + e-sign), billing (their QBO invoices). Onboarding a partner: gateway admin creates the login → add the email on the distributor's Users tab → they sign in at `alamedapointbg.com/distributor`. sync-manifest: new `sub-distributors:app-and-rpcs` writer (multi-writer on the transfer/movement ledger with brix-stock, both RPC-only). ⚠ ARCHITECTURE.md row + `projects/sub-distributor-portal/` plan need manual apply — the architecture repo isn't in this session's GitHub grant. |
| 2026-08-18 | proposal builder gamma | **Gamma export actually delivers a deck now — the flow never polled Gamma's async API.** Reported (Sky): "it also wont push into gamma." Gamma's Generations API (`POST public-api.gamma.app/v1.0/generations`) is asynchronous: the create call returns only a `generationId`; `gammaUrl`/`exportUrl` exist only once `GET …/generations/{id}` reports `status: completed` (typically 1–3 min). `proposal-gamma.mjs` returned straight after the POST — `out.gammaUrl` was never present — so the UI always toasted "Gamma generation started." and no link ever appeared, even on a fully successful generation. (`GAMMA_API_KEY` was verified configured on Netlify — the key was never the problem. Side note: env var `VITE_GAMMA_API_PROXY_URL` is misnamed vs the code's `VITE_GAMMA_PROXY_URL`; harmless, the fallback URL is correct.) **Fix:** `proposal-gamma.mjs` gains a GET `?generationId=` status proxy (completed → `{status:'created', gammaUrl, pdfUrl}`; failed → definitive `status:'error'` as 200-data so the client stops polling; else pending) and the POST now validates a `generationId` came back; all error paths return both `error` + `message` so the toast shows Gamma's real reason (bad key, credits, etc.) instead of failing silently. Frontend: `checkGammaProposal()` in `proposalBuilder.ts`; `ProposalBuilderPage.exportGamma()` polls every 5s for up to 5 min (tolerating 3 consecutive transient poll failures), keeps the spinner up, then lights the **Open Gamma** + **Download PDF** buttons and the URLs ride the existing save-proposal persistence. Build-output note: verified live that Netlify DOES run the app build (`/margin` serves `index-BdGCvnR2.js`; the committed `public/sales-next/` is a stale `index-BViRnREk.js` that production never serves) — the `.gitignore` "commit build output" note is outdated, so this ships source-only to avoid asset churn against other open PRs. |
| 2026-08-18 | proposal builder products | **Proposal Builder product picker rebuilt on `orders.catalog` — grill specs off the sodas, missing beverages back, junk rows gone.** Reported (Sky): "i only show some products… the table looks kind of jacked… i can see other items and info mixed and i cant see some items." Three bugs in `proposal-products.mjs`, all from sweeping every active `ops.qbo_items` row through name regexes and fuzzy-matching `public.contract_items` (THE MELT's equipment catalog) onto them: **(1) Equipment info stamped onto every beverage** — `findOrderItem`'s substring fallback (`rowName.includes(key)`) matched Melt contract items whose SKU normalizes to a single character (`"3"`, `"4"`, `"ice"`), so nearly every product carried a Taylor grill's (or ice maker's) description, manufacturer, model and spec sheet. **(2) Real beverages hidden** — `beverageClassOf()` needed a bib/can token in the NAME, so all four Sugar-Free BIBs and the Hangar 25 Cola/Diet Cola, Cable Car, Oaktown Root Beer 24-packs (8 catalog rows) returned null and were dropped. **(3) Junk shown as products** — "Fountain Flavor Stickers", "12OZ CAN FILL LABOR", "12OZ PACK OFF", empty-can raw material and backroom pump packages passed the regex test ("PUMPING" defeats the `\bpump\b` equipment guard). **Fix:** the picker is now driven solely by `orders.catalog` (the curated beverage catalog the order portal sells — category ≠ Gas/Other, active+orderable) joined to BX-1 price-book prices by `qbo_item_id`; beverage class comes from the STRUCTURAL fields (`container_type`/`category`: Bag-in-Box → fountain, Can/24-Pack → packaged), never the flavor name; the `contract_items` and `ops.qbo_items` reads are gone. Same-flavor multi-pack rows get the pack suffixed into the display name ("Hangar 25 Cola (3-gal BIB)" vs "(24 × 12 oz cans)") so the picker shows no duplicate-looking rows. Product ids (QBO item ids) and the response shape are unchanged, so saved proposals still resolve. Verified by replaying the handler locally against live-data dumps: 37 beverages (was 35 = 29 real + 6 junk), 0 rows carrying equipment fields, all 8 missing catalog rows present. No frontend change. |
| 2026-08-18 | visitor kiosk + handbook ack strip | **A front-door sign-in kiosk that prints a visitor badge, and the handbook sign-off moved to where you can see it.** **(1) Visitor kiosk** — new public page `public/visitors.html` at `alamedapointbg.com/visitors`, for the tablet at the door. Four steps: who they are (visitor / contractor / vendor / inspector / driver / interview), who they're seeing + purpose + vehicle + areas, the **visitor agreement** with a signature pad, and a **camera photo**. It ends on a **4 × 3 in badge** that prints from the tablet — big name, company, host, time in, escort-required line, photo, and a **QR that signs them out** (`/visitors?out=<badge>`). **Architecturally the key point: a visitor cannot log in**, so the page is deliberately unauthenticated and both writes go through service-role Netlify functions (`visitor-signin` / `visitor-signout`) — `ops.visitor_visits` is closed to the anon key. Badge numbers are `V-YYYYMMDD-NNN`, unique-constrained with retry so two tablets can't collide. Photo upload and the host email are **best effort and record their own failure**, because a blocked camera or a Resend outage must never strand someone at the door — the log then shows that nobody was actually told. **(2) `/compliance` → Visitors tab** (staff, `ops.fn_is_staff()`): **On site right now** first, with a 🖨 **Evacuation list** print — that list is the head-count an evacuation runs on, which is the real reason this beats a paper book — plus per-row sign-out, the 200-visit log with photo open, agreement version, and notify status. **(3) The agreement** (v1.0) is written for this building: escort, **CO₂ never-enter-to-rescue**, forklift traffic, evacuation, PPE, food safety incl. illness exclusion, no smoking/vaping, incident reporting, photography + confidentiality, and a **contractor insurance confirmation** that ties into the uninsured-sub premium trap in SOP-11. Migration `20260818e` (applied live): `ops.visitor_visits` + `ops.visitor_settings` + the private `visitor-photos` bucket. Env: `SUPABASE_SERVICE_ROLE_KEY` (already set), optional `VISITOR_ALERT_TO`. **(4) Handbook acknowledgment strip.** Reported as "the handbook signing disappeared" — it had not: Sky signed v2.0 at 01:00, so the form was correctly replaced by the signed banner. The real fault was placement — the sign-off panel sits **below the entire 30-page manual**, so your own status is invisible from the tab. The Safety Manual tab now opens with a status strip: signed/not-signed for you, how many signatures exist on this version, how many are on an older one and need re-signing, and buttons that jump to the sign-off and the roster. **Verified** headless end-to-end: all four kiosk validation gates, type selection, signature, a fake-camera capture, the submitted payload, the badge (photo + QR + one page — `#badgeWrap`'s 26px margin was pushing it to two), QR-deep-link sign-out, and the staff tab's on-site count, notify chips and ack strip. |
| 2026-08-18 | poster wall + printable versions | **The actual notices tiled on one screen, and every view printable.** Sky: "Keep all of these as different versions that I can print out… I mean the actual notices themselves all organized on the screen or two instead of huge ones playing across the screen." **(1) Poster wall** (`🧱` on the board, `?wall=1` / `?wall=2`) — the physical board reproduced: real poster art tiled, **all on one screen** (8 across, ~179×230 tiles at 1080p / 359×460 at 4K) or **two screens** (6 across, ~242×310; the toggle halves the count rather than using a fixed page size). `bestGrid()` searches every column count for the largest tile that fits the box, so it re-fits on resize, at any notice count, on any aspect. Tapping a tile opens the real PDF in display mode. **Tiles are pre-rendered page-1 JPEGs** (`public/postings/thumbs/`, 1100px long edge, q78, 4.9 MB for 29) — thirty PDF iframes would flatten a TV browser. Notices with no mirrored copy render a labelled placeholder instead of a broken tile. **(2) Every view has a printable twin.** New `printView(kind)` stamps `printing` + `printing-<kind>` on the body: the poster wall prints as a **4-page contact sheet** (3 across, captions, page-break-inside avoided), the index prints as **one landscape sheet** with QRs. Two bugs found and fixed in test — the first print rule force-showed *both* overlays so the wall's header and footer leaked above the index, and the overlay's own Print/Exit buttons printed; both now scoped to the view actually open. Fullscreen is dropped before printing (Chrome renders a fullscreen document badly to paper). **(3) `APBG-required-postings-packet.pdf`** — all 27 mirrored notices merged into one 40-page file behind a generated cover: the full index by jurisdiction plus the three things a packet can't do for you (whistleblower needs legal stock, Payday/DWC-7 are fill-ins, and the IWC order + WC carrier notice aren't included and why). Linked from the toolbar as **Print the whole board**. Built with pymupdf; cover authored as HTML and printed via headless Chromium. |
| 2026-08-18 | one-page TV board | **Every notice on a single screen, sized to be read across a break room.** Sky: "Do you think you could fit all of the postings on one page that when blew up to 65\" TV you could read it?" Worked the numbers first: a 65-inch 16:9 panel is 56.7 × 31.9 in, so at the 1:200 legibility rule a 28px label has a 0.59-inch cap height and reads from ~10 ft — and 30 notices on one 1080p screen leaves roughly 640 × 94 px per cell. **A legible directory fits; readable poster text does not** (30 dense legal pages on one panel is a thumbnail each, ~1–2px type), so the one-page view is the index and display mode stays the way to actually read a notice. **Shipped:** new **📺 One page on the TV** button (and `?tv=1`) opens a fullscreen board — masthead, then Federal / California / City of Alameda in balanced columns, one row per notice with a short label, agency · form code, and a ~2-inch QR. **Everything sizes in `em` off a root set in `vh`**, so the physical type size is identical on a 1080p and a 4K panel of the same diagonal — measured 0.56" cap at both, readable to ~9.3 ft. **`tvFit()` shrinks that root until the content stops overflowing**, so the page fits on one screen at any notice count or aspect ratio; column balancing is CSS `column-count` (the hand-rolled splitter left column 1 short, which forced the type *smaller* — the browser's balancer got it back up from 25.8px to 26.6px). Detection subtlety: in a fixed-height multi-column box, content that will not fit spills **sideways** into an extra column, so the fit test is `scrollWidth`, not `scrollHeight`. Migration `20260818d` (applied live) adds `short_title` — official titles run past 90 characters and the board affords ~34 per line — populated for all 30 and editable in Manage postings. Re-fits on `resize` and 350 ms after entering fullscreen, so a TV that reports a different size on wake can't leave the board clipped. **Verified** at 1920×1080, 3840×2160 and 1366×768: three columns, zero overflow, all 29 QRs drawn, no errors. |
| 2026-08-18 | posting board shows the real posters | **The board now displays the actual notices, not summaries of them.** Sky: "You can't recreate the posters yourself? Or link them to the monitor." Framing the agency PDFs directly does not work — dol.gov sits behind Akamai bot protection (403 to curl, connection reset to a headless browser) and gov hosts generally refuse to be framed — so **each notice's official PDF was downloaded from its `source_url` and committed to `public/postings/`** (27 files, 16 MB), served at `/billing/postings/<file>`. New `ops.compliance_postings.mirror_path` (migration `20260818c`, applied live) names the file; `fileFor()` prefers a staff upload to the bucket, then the mirror, then falls back to the notice card. Every PDF was verified with pypdf against its expected first-page text — the Alameda notice confirms **$17.76 effective 7/1/2026**, and the DOL four needed a plain retry loop because Akamai 403s intermittently. **Two notices were generated rather than mirrored, because they are the employer's to issue and no agency artwork exists:** a Labor Code §6404.5 **No Smoking** sign (covers vaping and cannabis, names the CO₂/flammables hazard, carries the anti-retaliation line) and our **completed Emergency Phone Numbers** sheet — the Cal/OSHA S-500 is a blank fill-in form, so the useful artefact is the filled one, built from the Dashboard contact list with the never-enter-to-rescue rule and the 8-hour Cal/OSHA line on it. Both authored as HTML and printed to Letter PDF via headless Chromium (`public/postings/house-*.pdf`). ⚠ **Two deliberately left unmirrored:** the **IWC Wage Order** (which of Order 1 / 7 / 9 applies is still open with DLSE, and posting the wrong one is worse than posting none) and the **workers' comp carrier notice** (the form comes from AmTrust / Embroker and must name the current carrier) — both still show the QR card. **Not done, on purpose: recreating official posters as artwork.** A facsimile of a Cal/OSHA or CRD poster is not the official poster, and several carry format rules of their own. Mirroring the genuine PDF is the honest version of "recreate it". Manage postings now shows a **Posted copy** link per row and the stat reads *28 / 30 with our posted copy filed*. |
| 2026-08-18 | posting board (digital signage) | **The postings tab became an actual screen — a digital posting board you put on the break-room monitor.** Sky: "I want you to make a screen that I can use as a posting screen and have digital posting… I need a visual that can be placed on a monitor." The earlier build was a reference table; this is the display. **(1) Posting board view** (now the default; the table moved to a *Manage postings* sub-tab): dark full-bleed board headed **Employee Notices** with the legal name, facility address, date and notice count, grouped Federal / California / City of Alameda, one tile per notice — title, agency, form code, and a **QR code** so anyone can pull the official copy up on their phone. Container widened to 1700px (`.container.wide`) so a 1920 monitor shows five tiles per row. **(2) Display mode** — **Put this on the monitor** goes true fullscreen and auto-advances one notice at a time (seconds-per-notice configurable, default 20) with a progress bar, position counter, ◀/▶/pause controls, and ←/→/space/Esc keys. If we've uploaded our posted copy it renders **the actual document** (PDF in an iframe, image inline); otherwise it renders a large designed notice card — jurisdiction, title, form code, who it applies to, where we post it, and a 160px QR. Tapping any tile on the board opens display mode paused on that notice, so it doubles as the reader. `?kiosk=1` autostarts playback for a dedicated break-room machine (fullscreen still needs one click — browsers require a gesture). **(3) Employees can actually read it.** Migration `20260818b` (applied live) adds a SELECT-to-authenticated policy on `ops.compliance_postings` and a storage policy for `postings/*` — required notices are public by law, and a board only staff can see is not a posting. Everything else in `compliance-docs` (insurance limits, audit corrective actions, SDS) stays behind `ops.fn_is_staff()`. `reloadAll` switched from `Promise.all` to `allSettled` so one staff-only read failing can't blank the board, and a non-staff login now lands in **employee mode**: the vault tabs hide and they get Posting Board + Safety Manual. **(4) QR encoder is vendored, not CDN-loaded** (`public/vendor/qrcode-generator.js`, MIT, 55 KB) — a screen that has to come up in a break room shouldn't depend on jsdelivr; drawn straight to canvas at device pixel ratio, and any failure just hides the QR. **Verified** with a headless pass at 1920×1080 in both staff and employee modes: tile grouping, `required=false` rows excluded from the board, 5-across layout, QR rendering, tap-to-open, next/prev, keyboard, embedded-document rendering for a notice with a file, and the employee-mode tab set. |
| 2026-08-18 | compliance CRUD + dashboard + postings | **Every compliance table is now add/edit/archive with in-app file upload, a Dashboard is the landing tab, and the required-postings board ships with 30 verified entries.** Sky: "i dont like that these arent tables and columns that give me ability do add or delete or change… there should be a compliance and safety dashboard that shows all the phone numbers… i want all the state, local, and federal postings that normally people have to hang on a board be a section… all of these need to be stored on a supabase drive so we can change, download etc." **(1) Generic CRUD engine.** One `SPECS` map (documents / sds / parties / postings) drives the toolbar **+ Add** button, the modal form, and per-row **Edit / Upload / Replace / Archive** on every table in the app — adding a column is one line. Archive is soft (`archived_at`+`archived_by`, or `active=false` for parties) with a **Show archived** toggle; nothing is ever deleted. **(2) Files go in from the browser.** The `compliance-docs` bucket's staff policies cover insert/select/update/delete, so upload runs on the signed-in user's own bearer (`documents/*`, `sds/*`, `postings/*`; 25 MB cap, replace deletes the old object) — the **'Attach PDF'** dead end is gone, and no service-role key or separate admin surface is involved. **(3) Dashboard tab** (new default landing, absorbs Emergency Contacts): six click-through stat tiles (expired · expiring ≤60d · no file attached · SDS with no sheet · postings tracked · postings due for review), a ranked **Needs attention** list, the facility/CERS/EIN fact grid, and every phone number grouped into six cards — life safety, Concentra + the Cal/OSHA 8-hour line, company 24-hr, agencies, insurance/brokers, labor agencies. Print it and it fills in the Cal/OSHA **S-500** poster. **(4) Required Postings tab** — new `ops.compliance_postings` (migration `20260817c`, applied live) **seeded with 30 rows** (`20260818a`) verified this session against dir.ca.gov, dol.gov, eeoc.gov, edd.ca.gov, calcivilrights.ca.gov, sos.ca.gov and alamedaca.gov: federal / state / local sections, official source link + our posted copy, form code, version-on-the-wall, review-by dates, and a **read-before-you-print** panel. Findings worth knowing: **City of Alameda minimum wage $17.76 from 7/1/2026** (AMC 4-60, 2-hr trigger, no size exemption — the city rate is what we pay, and it re-indexes every July 1); **SB 294 'Know Your Rights' is a distribution duty** — every employee by Feb 1 annually, up to $500 each if missed; **Whistleblowers must print on 8.5×14 legal paper** or it's non-compliant; **Form 300A posts Feb 1–Apr 30** and must be mailed to route drivers and field techs; **SB 553 has no poster** (written plan + incident log + training); the **MPN notice is a delivery duty, not a posting**; FMLA is filed `required=false` (50-employee threshold). ⚠ **Open: which IWC Wage Order applies** — Order 1 Manufacturing indexes "Bottling (soft drinks)", Order 7 Mercantile indexes "Wholesale houses", Order 9 is transport/warehousing *for others*; DIR's test is principal purpose and only one applies. Confirm with DLSE before printing. **(5) Bug found in test:** `showArchived` was a `const` arrow but `reloadAll()` calls it from the boot block above — temporal dead zone would have rendered the entire page empty. Converted to a function declaration; caught by a headless Playwright pass with mocked PostgREST, which also verified insert/patch bodies, the `ops` content-profile, required-field validation, and archive stamping. |
| 2026-08-17 | insurance + company docs | **Our insurance, W-9, and resale certificate filed; carrier quick links, fleet schedule, and SOP-11 shipped.** Sky sent the 2026 landlord COI packet (LPC/City of Alameda), the W-9, and the CDTFA-230. **Filed live in `ops.compliance_documents`** (staff-RLS, no party = ours): **GL** Spinnaker NAIC 24376 via Thimble/Coterie `CSB-00128559-00` 1/1–12/31/26, $1M occ / $2M agg / **$2M products-comp-op**; **Auto** Progressive Express NAIC 10193 `975506554` 1/1–12/31/26, **$2M CSL** + $1M UM/UIM, 7 scheduled VINs, $3,500 comp/collision each; **WC** AmTrust–Security National NAIC 19879 via Embroker `SWC1470931` 1/1/26–**1/1/27**, EL $1M×3, **officers INCLUDED**; **W-9** EIN **82-1580658**, S-corp, signed 8/30/24; **Resale cert** seller's permit **103104468**, signed Sloan 8/25/24. App: 'Our Insurance' tab → **Insurance & Company Docs** — adds a **company identity card grid** (legal name / EIN / seller's permit / address, readable without opening a file), a tax-document table, a **carriers & brokers quick-links panel** (Thimble/Coterie, Progressive, AmTrust/Embroker, Concentra, with phones + portals), and the **7-vehicle fleet schedule**. Missing-file state changed from 'No file' to **'Attach PDF'** (drag-drop in Refractor is the attach path — storage upload needs a session the MCP doesn't have). **New handbook chapter SOP-11** (`32-sop-insurance.md`): policy schedule, renewal calendar (bind by early December — GL+auto die 12/31), issuing a COI incl. **certificate ≠ coverage, additional insured needs the endorsement**, collecting vendor/contractor COIs and the uninsured-sub premium trap, claims by line (collision → handbook §C8, injury → §G1 + Concentra + DWC-1 + Cal/OSHA 8-hr, customer-site damage, product), fleet admin, premium-audit prep, company identity numbers, 9 hard rules. ⚠ **Six broker questions raised, not answered**: no property/contents line on any certificate; no umbrella; whether the GL carrier's class covers the CO₂ fill station + co-packed production; the ACORD 101's own note that *'liability coverage may not apply to all scheduled vehicles'*; named-insured/address inconsistent across the three policies; **certificate-holder box blank on the landlord COI**. Also: GEICO was named by Sky but appears on no certificate in the packet; and the resale cert carries a third DBA ('Alameda Point Craft Soda, Co') absent from the W-9. |
| 2026-08-17 | compliance app v2 | **Compliance & Safety restructured into six tabs; inspections become a section with per-agency checklists; safety manual gets diagrams + a print-as-manual mode.** (1) **Tabs:** Inspector Mode (our docs + SDS, unified search) · **Our Insurance** (company policies — insurance-category docs with NO party) · **Vendors & Contractors** (Vendor production & food safety / Vendor insurance / Contractor insurance / Other third-party — split on `party_id` + `party_type='contractor'`) · **Emergency Contacts** (the full CERS/ERCP directory: 911, Poison Control 800-222-1222, Alameda Hospital, both Concentra Oakland clinics, Sky/Sloan 24-hr, Airgas 866-734-3438, ACEH CUPA 510-567-6702, Cal OES 800-852-7550, NRC, Cal/OSHA + employee hotline, DTSC, RWQCB 510-622-2300, US EPA, CDFW, USCG, State Fire Marshal — flags Sloan's blank 24-hr number on the filing) · **Inspections** · **Safety Manual**. (2) **Inspections** sub-tabs with real checklists: CUPA (records + physical walk), Cal/OSHA (written programs incl. IIPP §3203 / heat §3395 / SB 553, records incl. Log 300 + 300A posting, physical), Workers' Comp (**premium audit** — payroll by class code, and the uninsured-sub COI trap that gets charged to our policy — plus **loss-control survey**), Insurance (property/premises + operations incl. the CO₂ fill station and vendor COIs), OU Kosher (per-ingredient LOCs, Schedule A, co-packer cert, changeover, label artwork), Other (Fire/IFC 5307, FDA/FSMA + CDPH, **CHP BIT**, **Weights & Measures — the fill-station scale is a commercial device**, BAAQMD/DIR/EDD/ADA), plus a Visit runbook. The 'say these two things first' self-report block was **removed** — both corrections are filed. (3) **File links**: private `compliance-docs` bucket is staff-gated, so an Open button fetches with the session bearer and hands over a blob (delegated click on `.filebtn`, so paths never sit inside an inline onclick). Falls back to the first URL in `notes`. (4) **Safety manual**: 8 inline SVG diagrams in `public/safety/` referenced from the markdown (CO₂ cellar accumulation, ladder 4:1 + belt-buckle rule, lifting posture, double-park cone placement, forklift load centre + tip-over, hot-work 35ft/fire-watch, cylinder securement, PASS) + **Print the manual** (`print-manual` body class: page-break before each Part, diagrams/tables kept whole, TOC and sign-off omitted). Version stays **2.0** — nobody had signed 2.0 yet, so diagrams added no re-sign burden. |
| 2026-08-17 | safety handbook v2.0 | **Health & Safety Training Handbook rewritten to a full ~32-page (11.5k word, 71-section) program covering the work Brix people actually do.** Sky: the prior handbook was 30 pages covering bar accounts, customer restaurants, ladders, power/electrical tools, truck parking + double-parking with cones, and collision procedure — v1.0 was too thin. **v2.0 structure:** Part A program/IIPP (8 CCR §3203 elements table, rights incl. Cal/OSHA 1-833-579-0927, zero-tolerance list, **stop-work authority**); Part B core (PPE matrix, lifting/team-lift/§5110 repetitive motion, hand trucks incl. cellar-stair rule, **ladders** — 3-point contact/belt-buckle-between-rails/fiberglass-near-electrical/never-customer's-ladder, slips, **hand+power tools+knives**, **electrical** GFCI/cords/panel clearance, fire/PASS, HazCom + never-mix table, **compressed gas** CO₂/N₂/beer gas, heat §3395, noise, housekeeping, SB 553 violence, fitness-for-duty); Part C road (pre-trip, **no handheld phone**, backing/GOAL, **height-clearance/parking-garage**, **parking + double-parking + cones + never-the-bike-lane**, curbside load/unload, liftgates, **collision 6-step incl. never-admit-fault + photos/witnesses**, breakdowns, route security/robbery); Part D customer accounts (arrival look-around, **bars** — glass/wet/dark/cramped/intoxicated patrons, **restaurants** — fryers/grease/knives/announce-yourself, **§D4 cellars + CO₂ accumulation incl. the never-enter-to-rescue rule**, walk-ins, no customer ladders/tools/help, food safety incl. illness exclusion, damage reporting, lone work); Part E warehouse (forklift 1910.178/§3650 cert + 3-yr re-eval, racking, **CO₂ fill station** — inspect/never-overfill/alarm-is-life-safety, cylinder storage, dismantling incl. R290/R600a flammables, receiving); Part F trades (**lockout §3314**, refrigerant §608 + 80% recovery-cylinder rule, **brazing/hot work** — 35ft clearance + 30-min fire watch + never-braze-unrecovered + N₂-regulator-with-relief, pressure testing — **never oxygen**, line-cleaning caustics, ice machines, installs, **no permit-required confined space entry**); Part G emergencies (**Concentra Oakland** treatment table + DWC-1 within 1 working day + 8-hr Cal/OSHA serious-injury rule, Hangar 200 + customer-site emergencies, spills/used-oil/never-down-a-drain, near-miss no-blame, earthquake/fire/outage, regulators → SOP-10); Part H training schedule + records + 7-point acknowledgment. **Version bumped 1.0 → 2.0** (`HANDBOOK_VERSION` in compliance.html) because 2 people had already signed 1.0 — the UNIQUE(user_id, version) index re-prompts them, which is correct for materially changed content. compliance.html also gains a **dynamic TOC** (`buildToc()` — marked v12 dropped `headerIds`, so ids are assigned from heading text post-render and the contents list is built from what actually rendered; no hand-written anchors to drift) + h3/h4 styling. |
| 2026-08-17 | compliance app + safety handbook | **Standalone Compliance & Safety app (`/compliance`) + signable Health & Safety Training Handbook.** (1) New **`public/compliance.html`** served at `alamedapointbg.com/compliance` (netlify.toml pretty-URL + gateway proxy row, same pattern as the 2026-08-06 MCP move): **Inspector Mode** — facility/CERS header, emergency-contact bar (incl. Airgas 24-hr (866) 734-3438), live stats, unified search over `ops.compliance_documents` + `ops.compliance_sds`, expiry chips, per-product SDS links (first URL parsed from `notes`), print view; **Safety Handbook & Sign-off** — renders `docs/handbook/31-safety-training-handbook.md` (marked+DOMPurify, same stack as the handbook viewer) with a canvas signature pad. Auth is deliberately NOT `auth.js requireSuperadmin` — any valid `apbg_session` login may sign (warehouse hires won't be staff); RLS enforces (insert self-only, read own-or-staff; staff via `ops.fn_is_staff()` see the full roster + records). (2) **Handbook chapter 31** (registered in manifest.json): OSHA/CUPA training handbook scoped to Hangar 200 — rights, ERCP emergency response (assembly areas, equipment locations), CO₂ asphyxiation/cylinder rules, HazCom/SDS incl. the never-mix table (bleach vs acid ice-machine cleaners; Kem-Tek oxidizer segregation), PPE matrix, **forklift** (1910.178/8 CCR §3650+: reading ≠ certification — formal cert + 3-yr re-eval required; pedestrian + operator rules), lifting/ladders, fire/PASS, used-oil + universal waste + never-down-a-drain, refrigerant §608 (cert PA620014460), lockout basics, injury/near-miss reporting, inspection etiquette, training schedule table, acknowledgment terms. (3) Migration **`20260817b_safety_handbook_acks.sql`** (applied live): `ops.safety_handbook_acks` — immutable signature rows (name/email/version/PNG data-URL signature/UA; UNIQUE(user_id, version) so re-signing is per-version; INSERT self-only, SELECT own-or-staff, no UPDATE/DELETE). New-hire flow: provision login (SOP-1) → they sign at /compliance#handbook. Companion gateway change: `/compliance` proxy row + `compliance` registry tile + appswitcher inference. |
| 2026-08-17 | compliance sds + sop-10 | **SDS library + safety-training log added to the Compliance vault; CUPA readiness filed as handbook SOP-10.** Trigger: unannounced-CUPA-inspection prep (Alameda County EH, CERS 10666381 — HMBP accepted 8/12, accuracy review pending). The facility had ZERO SDS in any form, and the signed ERCP (§I/§J2) promises training records with no system of record. **Shipped:** (1) Migration `20260817a_compliance_sds_training.sql` (applied live): `ops.compliance_sds` — one row per hazardous chemical PRODUCT on site (product/manufacturer/CAS/storage location/container/max qty/`cers_reported`/SDS revision date/file), deliberately a LONGER list than the CERS inventory because Cal/OSHA HazCom (8 CCR §5194) requires an SDS regardless of quantity (small paints, cleaners, aerosols included; the 55 gal/500 lb/200 cu ft numbers are CERS *reporting* thresholds only) — seeded with the CO₂ line from CERS; `ops.compliance_training` — one row per session (date/type/topics/trainer/attendees/signed roster), annual-refresher due date computed (`trainingDue()`). Both staff-only RLS both directions (same lesson as 20260726a); files reuse the private `compliance-docs` bucket (`sds/*`, `training/*`). (2) `ComplianceTab.tsx` gains a section switcher — **Documents / SDS Library / Safety & Training** — new grids + modals in the existing conventions; SDS rows without a sheet flag amber "MISSING — request it", the CERS checkbox carries the crossing-a-threshold-means-30-day-update hint. (3) **Handbook SOP-10** (`docs/handbook/30-sop-cupa-compliance.md`, registered in manifest.json): digital-records rule + quarterly access drill, per-facility scope table, record index, SDS policy, inspection-day runbook, monthly walk checklist, hard rules, and a draft section for the **Red Bull cooler dismantling/recycling op running AT Hangar 200** (used compressor oil = CA hazardous waste at ANY quantity → the facility is a used-oil generator and the 8/10 Business Activities "No" needs correcting; EPA/state ID + registered hauler + manifests required — CODs are supporting records, not manifests; refrigerant nameplate check before cutting — CO₂/R744 exempt from the §608 venting ban, R290/R600a/R134a are NOT; ≥55 gal aggregate would also add it to the HMBP inventory). Inspection-day packet with the filing discrepancies (bulk-tank-vs-"empty cylinders" inventory line, zeroed Largest Container/Days on Site, unchecked H6-3/H1, missing Sloan 24-hr phone, ERCP address mismatch, garbled D-21) delivered as a session artifact, not committed. ⚠ Follow-ups: `architecture/schema-snapshot.json` was hand-extended with only the two new tables — a full `refresh-snapshot.mjs` run shows **33 unregistered ops.* tables drifted in since 7/24** (resq_sync_*, shopify_sync_*, remittances, equipment_assets, …) that need writers/orphan entries in a dedicated pass; standalone `/compliance` app with Inspector Mode (move out of Refractor → Production, same pattern as the MCP move) planned next. |
| 2026-08-14 | sf sweep by update | **Fresh receipt sweep now pages SF's Invoiced jobs by `-updated_at`, closing the added-to-an-old-job blind spot.** Reported: "DESERT BEVERAGE was submitted a few days ago but we don't see it" (ResQ R-1036738 → SF job 1091248795). Root cause: the expense was added on 8/13 to a job CREATED in June — the fresh sweep paged `sort=-id` (creation order), so a months-old job never re-entered the newest pages no matter what happened to it, and the invoiced-email hook only fires on the status change (which can precede the expense). A scan of the 200 most-recently-updated Invoiced jobs found **12 missed expenses (~$9k)**, incl. both Desert Beverage jobs — all landed as drafts with receipts via `?landJob=` (nothing auto-posts; the gate holds). **Fix (edge fn v22):** verified live that adding/editing an SF expense bumps the JOB's `updated_at` and that `GET /jobs?sort=-updated_at` is fast (<2s) — the fresh sweep now uses it (a brand-new job is also recently updated, so it strictly dominates `-id` for freshness); the historical crawl keeps the stable `-id` walk. Same-day repairs: refreshed the SF portal cookie and backfilled `sf_admin_job_id` deep-links + missing receipts on ~10 older rows (the portal scrape times out intermittently on SF's side — `landJob` re-runs are the retry). Also surfaced (separate issue, pre-gate): two duplicate QBO bills from the old autopost — Arturo $375 (Bills 173048+173049; blank-vendor draft re-landed after the vendor was fixed) and Pro Mechanical (172953 $256.59 + 173000 $317.59; amount edited in SF re-landed) — flagged to Sky for deletion in QBO. |
| 2026-08-14 | brixpense card connection services | **Card & Expense Match moved out of Master Control into Brixpense → Settings → Card Connection Services (`/expense/cards`), and the cardholder user list no longer shows everyone.** Reported (Sky): the card→user assignment listed every login on the shared Supabase project — brix-order customers, melt users, drivers — because `expense-cc-match.mjs list_users` dumped the raw GoTrue admin list; and the section belonged in the Brixpense module anyway. **Shipped:** (1) `list_users` now filters to internal users whose gateway role/modules grant Brixpense's access bucket (`billing`), mirroring the gateway's `grantsAccess()` (superadmin always; explicit `user_metadata.modules` must include `billing`; else the legacy role map — admin + finance), and paginates GoTrue (single 200-row page had been outgrown — staff could silently drop off). (2) New superadmin-only Brixpense page **Card Connection Services** (`app-expense/src/pages/CardMatch.tsx`, route `cards`; deliberately NO sidebar nav — the only way in is a superadmin-only entry card on Settings, per Sky "only in settings for superadmins"; backend gate unchanged) — full port of the panel with real UI instead of `prompt()` dialogs: date-range load + totals, cardholder table with inline assign/reassign editor (user dropdown + receipts-from date) and unassign, suggested merges, import-to-Brixpense for unmatched charges, unmatched-expense list, merged list with unlink. (3) `netlify.toml`: `/expense/api/expense-cc-match` + `/api/expense-cc-match` → `/.netlify/functions/expense-cc-match` (v1 function, no `config.path` — same dual-shape pattern as `qbo-pos-*`; the gateway strips the `/expense/` prefix). (4) `control.html`: panel + all `cc*` JS removed, replaced by an "Open Company Cards →" link card (same pattern as the 2026-08-06 MCP move). Backend function name/gate/actions unchanged. |
| 2026-08-14 | brixpense one-click post | **Paid expenses post to QuickBooks in the same click as Submit — no second "Post to QuickBooks" step for the common case.** Ask (Sky): "approve now, post later after paid — or if it's already paid, just approve now." The two-step gate from earlier today (Submit approves, a separate click posts) stays exactly as designed for **unpaid Bills** (`as_bill=true`) — that's the "post later, once it's actually ready" case, deliberately deferred. But for an expense already marked **paid** (posts as a QBO Purchase), there's nothing left to wait for once Submit's validation passes (payment account on file), so making the user come back for a second click just adds friction. **Shipped (frontend only — no backend change; `expense-request-notify` already returned `mode: 'bill'|'purchase'`, and `expense-request-link-bill` was already independently callable):** `ExpenseForm.tsx`'s submit handler now checks `notifyData.mode` — `'purchase'` → immediately calls `expense-request-link-bill` right after the approve, in the same Submit click (button reads "Submit & Post to QuickBooks — $X"); `'bill'` → unchanged, shows the separate "Post to QuickBooks" button. A failed auto-post lands exactly like a failed manual post always has (autopost_error stamped, `REPORT_TO` emailed per #357) and falls back to the same manual button as a retry — so editing + notify-on-failure still cover this path. Extracted the actual `fetch` to `expense-request-link-bill` into a shared `attemptPostToQuickBooks()` so the auto path and the manual button call the identical code. |
| 2026-08-14 | brixpense post-fix | **Approved expenses can be edited before posting; a failed post now notifies someone.** Hit live: an SF-landed expense (job 350361686, "Bill Pace", $200, bill # 1025561) carried a bad `receipt_date` of `2020-08-04` — posting failed with QBO 6210 "Account Period Closed, Cannot Update Through Services API" (QBO refuses API writes into a closed accounting period, correctly). But the row was already `approved` and `ExpenseForm.tsx` was read-only for any status past `draft`, so there was no way to fix the date in Brixpense. Ask (Sky): let it be edited, and notify someone when a post fails instead of it just silently sitting there. **Shipped:** (1) `expense-request-notify.mjs` now accepts resubmission from `draft` **or** `approved` for `request_type='expense'` (re-validates vendor/payment-account and re-approves; PRs unchanged, still `draft`-only). (2) `ExpenseForm.tsx` unlocks editing for `approved` rows (still locked once `posted`/`denied`/etc.), with an amber banner explaining why it's back in edit mode. (3) `expense-request-link-bill.mjs` now parses QBO's actual Fault message out of the raw error blob (`qboFaultMessage()` — was surfacing the literal JSON to the user) and, on **any** post failure (QBO error, no vendor match, no payment account), stamps `autopost_error` on the row and emails `REPORT_TO` — a failed post no longer depends on the person who clicked "Post to QuickBooks" noticing or sticking around. `PendingList.tsx`/`SFExpenses.tsx` show that reason inline under the row. Side effect (no code change needed, just confirmed): since the "Was this already paid?" toggle uses the same `disabled={readOnly}` as every other field, an `approved` expense can now be flipped between unpaid-Bill and paid-Purchase (and back) before posting, not just have its date/vendor/amount fixed. |
| 2026-08-14 | brixpense full gate | **No expense auto-posts to QuickBooks anymore — every one requires an explicit human "Post to QuickBooks" click.** Immediate trigger: right after the 2026-08-13 OCR-gate PR merged, an SF draft was posted within minutes via the *existing* "Submit" flow — which had always silently created the real QBO Bill/Purchase as a side effect of Submit, for every expense (not just SF ones), no separate confirmation. Ask (Sky): gate ALL expenses in Brixpense, don't let anything auto-load into QuickBooks, force a real look at the bill before it's real. **Shipped:** (1) `expense-request-notify.mjs`'s `expense` branch no longer calls QBO at all — Submit only auto-approves (draft→approved, same no-manager-workflow behavior as always) after validating the post *would* succeed (vendor match for a bill, payment account for a purchase). All the QBO-posting code (`buildBillPayload`/`buildPurchasePayload`/`maybeMarginMatch`) was removed from this file. (2) `expense-request-link-bill.mjs` is now the ONE place a human explicitly posts to QuickBooks (`mode=create`) — extended to handle BOTH the unpaid-Bill case (already there) and the paid-Purchase case (new — moved over from expense-request-notify, including the PaymentType resolution chain, optional-vendor EntityRef, PR-fulfillment flip, and margin-match). (3) Frontend: `ExpenseForm.tsx`'s success screen shows a "Post to QuickBooks" button right after Submit instead of assuming it already happened; `PendingList.tsx` ("My Submissions") and `SFExpenses.tsx` both grow a per-row "Post to QuickBooks" button on any `approved`-but-unposted expense, so a review done later (not just at submit time) still has somewhere to finish. (4) The `sf-expense-autopost` daily pg_cron job is **unscheduled** (migration `20260814000001`, applied live) — nothing may auto-post SF drafts on a timer anymore; the function itself is untouched as a superadmin-gated manual fallback nobody calls automatically. Its `ops.sync_health()` check was dropped (would've sat permanently red and spammed the 15-min health-alert cron) — `sf_expense_ocr`'s check stays, since that pipeline (OCR + bill-number extraction, from 2026-08-13) is still live and still feeds the review UI, it just no longer feeds an automatic poster. |
| 2026-08-13 | brixpense OCR gate | **SF expenses now go through OCR, and a bill number gates auto-post.** Reported: a SF-landed draft (PRO MECHANICAL, SF job 1094476063) had no PDF attached and no bill number in QBO — investigation found (1) SF's own expense record often carries no receipt at all (this account's `admin.servicefusion.com` portal-scrape fallback for finding one is intermittently timing out — 9/24 live SF drafts have an attachment, 15 don't), and (2) the automated pipeline never ran OCR or set a bill number on ANYTHING — `buildBillPayload()` never set `DocNumber`, and the interactive `expense-ocr.mjs` (human receipt uploads) didn't even extract a bill number field. Ask (Sky): OCR every bill, carry the bill number through to QBO when found, and if a PDF won't attach at all, don't auto-post — hold it for review. **Shipped:** (1) `netlify/functions/lib/expense-ocr-core.mjs` — the Claude OCR extraction (vendor/bill_number/date/line_items/account_guess/job_number/customer/memo/notes) factored out of `expense-ocr.mjs` so the automated and human-upload paths share one schema/prompt; added `bill_number` to the schema (previously not extracted anywhere). (2) New **`sf-expense-ocr-background.mjs`** (daily pg_cron 10:15 UTC, 15 min ahead of autopost's 10:30 UTC; same cron-secret/superadmin auth as autopost) — for every un-OCR'd SF draft: no attachment → `ocr_status='no_attachment'` (held) + email; attachment found → OCR it, write the raw extraction to `ops.expense_request_attachments.ocr_result` (declared since the original migration, never used until now) and promote `bill_number` onto the request row (`ocr_status='processed'`); OCR failure → `ocr_status='failed'` (held) + email; OCR success but no printed bill number → still held + email ("no bill number found"). Migration `20260813000002` (applied live): `bill_number`/`ocr_status`/`ocr_processed_at`/`ocr_error`/`ocr_notified_at` on `ops.expense_requests`. (3) **`sf-expense-autopost-background.mjs`** now only auto-posts drafts with `ocr_status='processed' AND bill_number IS NOT NULL` — anything held stays a draft (visible in Brixpense → SF Expenses with a "Needs review — …" badge) until a human opens it, attaches/fixes the receipt or types the bill number in, and hits Submit, which posts immediately through `expense-request-notify` (gate-free — a human just looked at it). Preview digest now reports a `held_for_ocr_review` count. (4) `bill_number` → QBO `Bill.DocNumber` (capped 21 chars) wired into all three bill-creation call sites: `sf-expense-autopost-background`, `expense-request-notify`'s as-bill path, `expense-request-link-bill`. (5) Frontend: `ExpenseForm.tsx` gets a "Bill / Invoice #" field (auto-filled by OCR on any receipt upload, human or SF) + an amber banner explaining why an SF draft was held; `SFExpenses.tsx` list badges swap "Draft"/"Posted" for status-aware labels ("Needs review — no receipt" / "…OCR failed" / "…no bill #" / "Ready — auto-posting" / "Posted"). **Not solved (flagged, not fixed):** the admin-portal receipt-scrape fallback in `sf-receipt-sync` is still intermittently timing out — that's a separate, pre-existing SF-portal-cookie/network issue, not something this change touches; it just means more drafts land with no attachment (and now correctly get held instead of silently missing a bill number). |
| 2026-08-13 | brixpense OCR gate | **SF expenses now go through OCR, and a bill number gates auto-post.** Reported: a SF-landed draft (PRO MECHANICAL, SF job 1094476063) had no PDF attached and no bill number in QBO — investigation found (1) SF's own expense record often carries no receipt at all (this account's `admin.servicefusion.com` portal-scrape fallback for finding one is intermittently timing out — 9/24 live SF drafts have an attachment, 15 don't), and (2) the automated pipeline never ran OCR or set a bill number on ANYTHING — `buildBillPayload()` never set `DocNumber`, and the interactive `expense-ocr.mjs` (human receipt uploads) didn't even extract a bill number field. Ask (Sky): OCR every bill, carry the bill number through to QBO when found, and if a PDF won't attach at all, don't auto-post — hold it for review. **Shipped:** (1) `netlify/functions/lib/expense-ocr-core.mjs` — the Claude OCR extraction (vendor/bill_number/date/line_items/account_guess/job_number/customer/memo/notes) factored out of `expense-ocr.mjs` so the automated and human-upload paths share one schema/prompt; added `bill_number` to the schema (previously not extracted anywhere). (2) New **`sf-expense-ocr-background.mjs`** (daily pg_cron 10:15 UTC, 15 min ahead of autopost's 10:30 UTC; same cron-secret/superadmin auth as autopost) — for every un-OCR'd SF draft: no attachment → `ocr_status='no_attachment'` (held) + email; attachment found → OCR it, write the raw extraction to `ops.expense_request_attachments.ocr_result` (declared since the original migration, never used until now) and promote `bill_number` onto the request row (`ocr_status='processed'`); OCR failure → `ocr_status='failed'` (held) + email; OCR success but no printed bill number → still held + email ("no bill number found"). Migration `20260813000002` (applied live): `bill_number`/`ocr_status`/`ocr_processed_at`/`ocr_error`/`ocr_notified_at` on `ops.expense_requests`. (3) **`sf-expense-autopost-background.mjs`** now only auto-posts drafts with `ocr_status='processed' AND bill_number IS NOT NULL` — anything held stays a draft (visible in Brixpense → SF Expenses with a "Needs review — …" badge) until a human opens it, attaches/fixes the receipt or types the bill number in, and hits Submit, which posts immediately through `expense-request-notify` (gate-free — a human just looked at it). Preview digest now reports a `held_for_ocr_review` count. (4) `bill_number` → QBO `Bill.DocNumber` (capped 21 chars) wired into all three bill-creation call sites: `sf-expense-autopost-background`, `expense-request-notify`'s as-bill path, `expense-request-link-bill`. (5) Frontend: `ExpenseForm.tsx` gets a "Bill / Invoice #" field (auto-filled by OCR on any receipt upload, human or SF) + an amber banner explaining why an SF draft was held; `SFExpenses.tsx` list badges swap "Draft"/"Posted" for status-aware labels ("Needs review — no receipt" / "…OCR failed" / "…no bill #" / "Ready — auto-posting" / "Posted"). **Not solved (flagged, not fixed):** the admin-portal receipt-scrape fallback in `sf-receipt-sync` is still intermittently timing out — that's a separate, pre-existing SF-portal-cookie/network issue, not something this change touches; it just means more drafts land with no attachment (and now correctly get held instead of silently missing a bill number). |
| 2026-08-06 | mcp app | **MCP moved out of Master Control into its own app + sf_token alert-flap fixed.** (1) New **`public/mcp.html`** served at **`alamedapointbg.com/mcp`** (netlify.toml pretty-URL redirect here + a gateway `/mcp` proxy row): superadmin-gated, carries the per-server OAuth discovery status (`mcp-auth-status`), the per-endpoint `/qbo`+`/zoho` probes (`pacer-health`), the Claude-connector-vs-provider-token reconnect explainer, and links to `/pacer/connect.html` + `/oauth/consent`. Master Control's MCP Servers panel + its `loadMcpStatus()` are **gone**, replaced by one "Open MCP Servers →" button — there is now exactly ONE place to operate MCP from. Registry: the `pacerConnect` tile is renamed **MCP Servers** and repointed `/pacer/connect.html` → `/mcp` (migration `20260806000001` in apbg-gateway, applied live; app_key kept so role/access wiring and bookmarks still resolve). Lockable as key **`mcp`** (added to `MAINT_CORE`; `MAINT_KEY_MAP.pacerConnect` `gateway`→`mcp`; new `/mcp` inference in the gateway's `appswitcher.js appKey()`). Handbook ch. 09 rewritten to match. (2) **`ops.fn_sync_health_core()` sf_token yellow rule** (migration `20260806a`, applied live): was `access_expires_at < now() - 2h`, which is the NORMAL resting state between the deliberately-throttled 6h-apart SF crons — it flapped yellow ~3h after every run and healed on the next, sending 4 alert + 4 "resolved" emails a day with nothing actually wrong. Now `updated_at < now() - 8h` (widest normal cron gap is 6h, so 8h means a run was genuinely missed). Red rules untouched: refresh error newer than the last write, or no write in 30h. Verified live — `ops.sync_health()` returns zero non-green rows. |
| 2026-08-06 | control panel | **MCP consolidation + OAuth-era health probes.** All MCP status/reconnect now lives ONLY in the Master Control MCP Servers panel: the "MCP Pacer Finance" card was removed from the health grid and the `sf_pacer` row from Connections & Reconnect (both pointed at pre-conversion plumbing). `pacer-health.mjs` rewritten for OAuth 2.1: the old probe sent `x-api-key` (dead auth) and counted any <500 as alive — post-conversion it "passed" on meaningless 401s. New definition of healthy: RFC 9728 discovery served + pointing at the Supabase AS, AND /qbo + /zoho rejecting anonymous calls with the OAuth challenge (an anonymous 200 now reads as a FAILURE — auth missing). Response vocabulary unchanged (gateway hub status dots keep parsing it). Live-verified: discovery ✓, both endpoints 401-challenge ✓, melt /api/health ✓. |
| 2026-08-05 | control panel | **Master Control MCP panel rebuilt for the OAuth 2.1 conversion.** The old "MCP Connectors" card (single link to `/pacer/connect.html`) became **MCP Servers**: live status row per resource server via new `netlify/functions/mcp-auth-status.mjs` (superadmin; server-side probe of each `/.well-known/oauth-protected-resource` + endpoint — the well-knowns have no CORS). Green = discovery served + pointing at the Supabase authorization server (`gfsdpwiqzshhexkofiif.supabase.co/auth/v1`). Panel copy distinguishes the two reconnects: Claude-connector re-auth (client-initiated, consent at `/oauth/consent`, per-server `MCP_ALLOWED_EMAILS`) vs provider-token re-auth (QBO/Zoho/SF, still `/pacer/connect.html`). Probed live at build time: pacerfinance ✓, asm-mcp-tools ✓, retell ✓, pacer-outlook discovery 404 (conversion merged in its repo but not serving — flagged amber). Handbook ch. 09 updated to match. |
| 2026-08-05 | build gate | **Inline-script parse gate** (`architecture/lint-inline-scripts.mjs`, wired into the Netlify build after lint-manifest). Parses every inline `<script>` block in the committed static pages under `public/` (SPA output dirs skipped) with `vm.compileFunction`; any unparseable block fails the build with file + line. Born from the 2026-08-05 Master Control outage (PR #344): a merge between two parallel sessions dropped one closing brace in `control.html`, the whole inline script stopped parsing, and login hung at "Checking access…" for everyone. This class of bug is now a red build, not a dead page. |
| 2026-08-04 | brixpense sync repair | **SF→Brixpense expense sync was dropping almost everything; fixed, instrumented, and made event-driven.** Reported as "job 1093536433 didn't kick its expense". Root cause: SF returns the **UNIX epoch** (not null) in `updated_at` for a never-edited expense, so the sweep's `ex.updated_at || ex.created_at` date gate tested 1970 < cutoff and **silently skipped every never-edited expense** — 10 landed in two months, every run logged `drafts: 0`, nothing alerted. Its sibling job 1093537348 landed only because someone had edited that expense. Deploying the fix exposed three more: (1) the `pageCount` probe called `GET /jobs` with **no explicit sort**, hanging ~100s of the 150s edge wall (the brix-order 1.18 rule) — and `t0` started after it, so the budget never saw it; the daily crawl was killed before logging for 9 days and its cursor never advanced. Probe deleted; `sort=-id` + hyphenated `per-page=50` make page 1 the newest and an empty page the end of history. (2) Nothing bounded a single job or fetch — added a per-EXPENSE deadline + `AbortSignal` caps; budget 110s→60s. (3) **216 of 222 SF "expenses" are blank rows** — `isEmptyExpense()` drops them (14 that landed were archived). **Prevention:** every run now logs `expensesSeen/skippedEmpty/skippedByDate/drafts/alreadyLanded/elapsedMs`, new `ops.fn_sf_receipt_coverage()` (migration `20260804a`, applied live, wired into `ops.sync_health()`) goes RED when informative expenses are seen but none land, and **`sf-status@` invoiced emails now trigger `sf-receipt-sync?landJob=<id>` directly** (`vendor-email-intake.mjs`) so an expense lands when the job is invoiced instead of when a cron reaches it. New `?landJob=<sfJobId>` repair endpoint (the old `?job=` scan 504'd). Edge fn v18→v21. |
| 2026-07-26 | compliance drag-drop | **Drag-and-drop filing on Compliance & Safety + first co-packer documents filed.** Drop files anywhere on the tab → a pre-filled New Document form per file (multi-file drops queue, "N more queued"); drop a file **onto a grid row** → uploads and attaches to that document in one gesture (row resolved from MUI's `data-id`, hovered row highlights). The edit modal's file picker is now a drop zone too, with a **Download** button for the attached file; the grid File column is a download link, or "drop a file to attach" when empty. New `guessFromFilename()` in `lib/compliance.ts` infers category + doc type from the filename (audit report / cert / corrective actions / permit / COI / W-9 / resale / CERS-CUPA / FDA…) so a dropped file lands ~80% filled in. Data: the seeded co-packer party was corrected to its real legal identity **Quantum J's Canning LLC** (3540 State Hwy 52 Unit A2, Frederick CO; audit lists the site as Erie CO) and its documents filed — PJRFSI GMP certificate **C2025-06124** (eff 2025-10-30, exp 2026-10-29, 21 CFR 117 subpart B, scope "manufacturing and canning of beverages"), full 46-page GMP audit report **A2025-13406** (2025-09-18, zero major/critical NCRs, ~15 minors all closed 2025-10-24; its `expiration_date` is the ANNUAL RE-AUDIT DUE DATE), the F212 corrective-action log, CDPHE manufacturing registration **MFE24913 (EXPIRED 2026-06-30 — renewal needed before the Compass upload)**, and FDA registration **10289932698** (exp 2026-12-31, document not yet supplied). |
| 2026-07-26 | copack retired | **Co-Pack (Legacy) tab removed from Production** (operator decision — superseded by the formula-driven work-order pipeline since 2026-07-21). Deleted `CopackOrdersTab.tsx`, `CopackOrderWorksheetPanel.tsx`, `formulaReadiness.ts`, `FormulaReadinessPanel.tsx` (last two were copack-only) + the copack types/CRUD in `lib/production.ts`. **Data untouched**: `ops.copack_orders`/`_costs` + the `fn_*_copack_order` RPCs stay live, moved to manifest orphans (read-only historical cost data; drop in a future cleanup). |
| 2026-07-26 | compliance vault | **Compliance & Safety document vault (Phase 1).** New Refractor tab **Production → Compliance & Safety** (`app/src/pages/production/ComplianceTab.tsx` + `lib/compliance.ts`): tracks the company's own compliance paper (insurance COIs, health permits, CERS/CUPA, FDA registrations, food-safety audit reports) AND third parties' documents we must keep current (co-packer GMP audits, contractor COIs) — born out of the Compass Group/Foodbuy supplier-QA onboarding. New tables `ops.compliance_documents` + `ops.insured_parties` (migration `20260726a`, applied live; staff-only RLS via `ops.fn_is_staff()` in both directions — shared-project logins must not read insurance limits/audit corrective actions) + private staff-gated Storage bucket **`compliance-docs`**. Expiration status (expired / expiring ≤60d / current) is computed where displayed, nothing stored. Weekly cron `compliance-expiry-cron.mjs` (Mondays 15:30 UTC) emails an expired/expiring digest to `COMPLIANCE_ALERT_TO` (default service@brixbev.com); silent when current. Seeded party: Alameda Quantum Canning (co-packer). Phase 2 (planned, not built): contractor COI upload portal — per-party coverage requirements, token-gated public upload page, Claude ACORD extraction, auto-chase emails. |
| 2026-07-22 | vendor intake | **Vendor email → SF ticket automation (Red Bull + Freshpet), zero crons.** New `netlify/functions/vendor-email-intake.mjs` — Resend inbound webhook (Svix-verified via `RESEND_INBOUND_SECRET`): emails forwarded to `rbfreeflow@`/`freshpet@alamedapointbg.com` are Claude-parsed (per-route hints) and become SF jobs under the route's `sf_customer_name`; SF's own job-status notification emails pointed at `sf-status@alamedapointbg.com` are parsed (regex + Claude fallback) and relayed to the route's `send_list` — SF has no API webhooks, its notification email IS the event. New tables `ops.vendor_email_routes/_email_tickets/_ticket_events` (migration `20260722a`, service-role only, seeded routes; `sf_customer_name` NULL = record-but-don't-create + alert). Category attach retries without on 422; dedup on Resend email id; setup checklist in `architecture/VENDOR-EMAIL-INTAKE.md`. |
| 2026-05-11 | v0.9.22 | Items active/managed toggles, customers parent/sub + address, Margin hero-stamp polish. |
| 2026-05-11 | v0.9.23 | **P&L alignment audit shipped.** `fn_item_pl_audit` + `fn_apply_pl_category_suggestions`. Items master gets a P&L Alignment column + bulk auto-categorize. |
| 2026-05-11 | v0.9.24 | **Per-customer entity + FIRST QBO WRITEBACK PATH.** New `push-qbo-item` edge function. |
| 2026-05-11 | v0.9.25 | **Category writeback.** `push-qbo-item v2` adds `bulkSyncCategories`. |
| 2026-05-11 | — | **Docs + arch shipped.** Focused architecture doc, user guide, sidebar link. |
| 2026-05-11 | — | **TS build fix in `app/src/pages/settings/ItemsSettingsEditor.tsx`.** Index signature for MUI X DataGrid Pro v7. |
| 2026-05-12 | v0.9.26 | **Data hygiene + chain rollups preview.** `fn_item_hygiene_summary` + `fn_preview_rollup_match`. |
| 2026-05-12 | v0.9.27 | **Runtime fuzzy match for Chain Rollups.** `expandModifierFilters`. |
| 2026-05-12 | — | **`CLAUDE.md` rewritten** to a living current-state doc. |
| 2026-05-12 | brixpense v0.1.0 | **Brixpense backend shipped.** Migration `20260512_create_expense_tables.sql` (4 tables + RLS + triggers + base seed). Four Netlify functions: `expense-request-notify`, `expense-request-decide`, `expense-request-link-bill`, `process-inbound`. |
| 2026-05-12 | brixpense | **Migration reconciliation (20260512o + 20260512p).** Dropped orphan plural-named approvals table; finished `expense_settings` seed (cogs_accounts, manager_emails, tags); re-aligned departments to entity/COGS taxonomy. `sync-manifest.json` corrected to reference `ops.expense_approvals` (singular). |
| 2026-05-12 | brixpense | **`expense-request-link-bill` now creates QBO bill end-to-end.** Modes: `create` (default, vendor match + POST /bill), `preview` (dry-run), `link` (legacy passive). Falls back to Service COGS (101). |
| 2026-05-12 | brixpense | **Approval model finalized — in-app auth, no magic-link.** Sequence: migration `20260512q` (in-app), `20260512r` (reverted to magic-link), `20260512s` (final: drop anon RLS, install self/manager UPDATE pair, deprecate `approval_token`). Net effect: anon RLS gone, two UPDATE policies (self + manager-by-email). Final flow — expense auto-approves on submit; PR sends a notification email (via Resend/SendGrid) to the chosen approver from `manager_emails` and points at `/expense/queue`. Approver authenticates via Supabase, approves at `/expense/review/:id`. `expense-request-notify` validates the approver against the allowlist. `expense-request-decide` is Bearer-only; takes `{ requestId, action, notes?, signatureUrl? }`; guards self-approval + email match. Frontend route swap: `/approve/:token` (public) removed; `/review/:id` (auth-gated) added. ManagerQueue links to `/review/:id`. ApprovalPage rewired to use `:id` + session + Bearer. |
| 2026-06-02 | sync v2 P3 | **On-demand single-job ResQ↔SF sync.** `resq-sf-sync-background.mjs` exports `syncSingleByCode(resqCode)` (same create/dedup + status/invoice logic as the cron, for one WO). New `sf-webhook.mjs` (secret-gated, `{resq_code}`/`{sf_job_id}` — internal/manual trigger; **not** Zapier — SF has no native webhook). Authed `resq-sf-sync?syncOne=<code>` + dashboard "⚡ Sync this WO now" button. Cron unchanged as the safety-net reconciler. (PR #127) |
| 2026-06-02 | sync customers | **Customer matching moved to SF-record linking by id.** `ops.sync_customers` gains `sf_customer_id`; Master Control → Linked Customers (`control.html`) gets a live SF customer **search** (`sync-customers?sfSearch=`) that links a row to the real SF record + a manual "SF #" field. `resolveSfCustomerName` resolves a linked id by matching it inside the SF list-search (the by-id GET endpoint is unreliable). Hardcoded the two live names as a backstop: **`THE MELT RESQ`**, **`STARBIRD CHICKEN: RESQ`** (note the colon the seeded row had dropped — root cause of the Melt/Starbird "matched no SF customer" outage). Unlinked the `BRIX WAREHOUSE EQUIPMENT` row whose broad `brix` keyword (sorts first) was hijacking matches. SF job creation uses `customer_name` (SF rejects `customer_id`, 422). (PRs #128, #130, #131, #132, #133) |
| 2026-06-03 | sync v2 P4 | **SF job photos auto-push to ResQ.** New `lib/sf-assets.mjs`: lists a SF job's pictures, fetches bytes host-aware (public S3 anon / `api`→Bearer / `admin`→Cookie via the `orders.sf_portal_session` cookie that Make refreshes), and **relays each through the new public Storage bucket `resq-photo-relay`** (short filename) because ResQ stores the image ref in a `varchar(100)` — inline base64 data-URLs overflow it. Pushed as after-images to the WO's ResQ **visit** (`addAfterImagesToVisit`); starts a visit via the WO appointment when none exists. Wired into the sync lifecycle in the existing `needsPhotoTransfer` slot — **after** visit-complete, **before** invoice-submit. Manual photo UI removed from `sync.html` (button + upload modal + Photos column). (PRs #134–#139) |
| 2026-06-03 | SF→Brixpense | **SF expense + QBO bill lands in Brixpense.** `expense-to-bill` (the sync.html 💰 Bill action), after creating the QBO bill, inserts an `ops.expense_requests` row (`request_type=expense`, `status=posted`, `as_bill`, `tag='Service Fusion'`, `qbo_bill_id`, `job_number`, vendor, amount, `line_items`, customer; `submitted_by`=the operator). Non-fatal — never undoes the bill. New separate **🔒 Close** button (per WO row) sets the SF job to **`Invoiced`** (`resq-sf-sync?closeJob=`) — the close-out step once the 3rd-party bill is entered, which then triggers the existing ResQ invoice submission. SF workflow: *Completed - Service* (→ ResQ visit + photos) → 💰 Bill (→ QBO bill + Brixpense landing) → 🔒 Close (→ Invoiced → ResQ invoice). Payment features on `expense_requests` (`payment_account_*`) are the next step. (PRs #140, #141) |
| 2026-06-03 | infra | **`SUPABASE_SERVICE_ROLE_KEY` now used by apbg-billing functions** (previously anon-only): reads the `orders.sf_portal_session` cookie, uploads to the `resq-photo-relay` public bucket, and writes the Brixpense expense-landing row. New public Storage bucket **`resq-photo-relay`**. ResQ is no longer read-only — we now write back via GraphQL mutations (`startVisit`, `addAfterImagesToVisit`). |
| 2026-06-28 | control panel | **Linked Customers restored + per-app Service/Maintenance UI.** Re-added the **Linked Customers** section to `control.html` — the live ResQ sync (`apbg-resq-sync` `_shared/customers.ts`) DEPENDS on `ops.sync_customers` (a WO whose facility matches no linked customer is skipped, no SF job), so the add/manage UI is needed to onboard new facilities. New **Service & Maintenance** section: per-app **banner** or full **lockout** with a service note, written via the gateway `POST /api/maintenance` (superadmin) and rendered in every app by `appswitcher.js`. (Net: this reverses the same-day removal of these two sections — Maintenance is now per-app + lockout instead of a single global flag.) |
| 2026-06-28 | control panel | **Master Control trimmed + APBG app switcher embedded.** Removed the Maintenance Mode and Linked Customers sections from `public/control.html` (the `site-settings` + `sync-customers` functions and `ops.sync_customers` data are untouched — `ops.sync_customers` is still read by the live ResQ sync + `expense-to-bill`; edit it via SQL if a facility needs relinking). Embedded the gateway's `appswitcher.js` (top-right waffle + shared light/dark toggle keyed on `localStorage.apbg_theme`) into `control.html`, `dashboard.html`, `setup.html`, `index.html`, and the two Vite app templates (`app/index.html`, `app-expense/index.html`). Full light/dark theming of these surfaces (control.html is dark-only today) is a follow-up. |
| 2026-07-04 | proposal builder | **Brandox retired → Supabase brand library.** Brandox is a Meteor DDP single-page app (empty HTML shell, assets delivered over a WebSocket after JS runs) — it can't be read server-side, which is why the scrape only ever returned the local logo fallbacks. Cut it entirely: deleted `proposal-brandox.mjs`, added new public Storage bucket **`brand-assets`** + `netlify/functions/proposal-brand-assets.mjs` (GET lists the bucket, POST uploads base64, DELETE removes; service-key backed, roles superadmin/admin/sales). The Proposal Builder's "Brandox Assets" panel became **"Brand Library"** with in-app upload (type-tagged: logo/can/equipment/hero/testimonial/sell-sheet/other) + delete; the 4 built-in Brix/Alameda logos still merge in as a never-empty fallback. Client: `getBrandAssets` repointed to the new endpoint, added `uploadBrandAsset`/`deleteBrandAsset`/`fileToBase64` (env override `VITE_BRAND_ASSETS_URL`, was `VITE_BRANDOX_PROXY_URL`). **`BRANDOX_*` env vars are now dead and the subscription can be cancelled.** |
| 2026-07-04 | proposal builder | **Refractor Proposal Builder — product images, Brandox scrape, venue templates.** (1) `proposal-products.mjs` now joins `ops.qbo_items` → `orders.catalog` by `qbo_item_id` and serves the catalog's real public image URLs (`image_url`/`image_thumb_url`/`bib_image_url` in the `brix-catalog-images` bucket) instead of guessing non-existent buckets off `contract_items.image_key` — the "bad connection" that left product images blank (37 beverage rows now resolve). Added `brix-catalog-images`/`brix-order-files` to the bucket fallback lists. (2) `proposal-brandox.mjs` extraction broadened well beyond `src`/`href` image-extension matches: `data-*` lazy attrs, `srcset`, `poster`, `og:image`, CSS `background-image`, embedded hydration JSON (`<script>`/`__NEXT_DATA__`), extension-less DAM/CDN URLs (host/path hinting), a wider JSON key set, and more DAM API candidate endpoints — so a JS-rendered Brandox portal yields its full library, not just the 4 local logo fallbacks. **Brandox still requires `BRANDOX_WORKSPACE_URL` (+ optional `BRANDOX_EMAIL`/`BRANDOX_PASSWORD` for private portals, or `BRANDOX_ASSET_URLS` for a manual list) set on Netlify to pull anything beyond the local brand art.** (3) New venue templates (Restaurants, Corporate Cafes, Bars, Fast Casual, Grocery) in `proposalBuilder.ts` (`PROPOSAL_TEMPLATES` + `selectTemplateProducts`) with a "Start from a Template" gallery on `ProposalBuilderPage.tsx` that pre-fills business type, lease terms, service plan, and a suggested beverage lineup. |
| 2026-07-09 | qbo writeback | **`qbo-customer-lookup` edge function shipped (v1 lookup → v2 +deactivate same day).** Live QBO Customer query by exact display name for brix-order's onboarding queue — `ops.qbo_customers` only refreshes daily (sync-qbo), which left "Finish onboarding" greyed up to ~24h after a new customer's first $0 setup invoice posted. On a hit the function upserts the customer into `ops.qbo_customers` (same conflict key as sync-qbo) so the mirror self-heals. Same INTERNAL_PAY_SECRET + token-lease pattern; verify_jwt=false. v2 adds `action: 'deactivate'` (SyncToken fetch → sparse `Active=false` → mirror flip) for brix-order's account-closure final step — QBO-side customer inactivation; the SF side has no API archived flag and is handled by a close-out ticket in brix-order. |
| 2026-07-09 | qbo writeback | **`qbo-return-order` edge function shipped (v1).** Books a customer RETURN ORDER as a QBO Credit Memo for brix-order's account-closure refund flow — SF's jobs API hard-rejects negative amounts (multiplier min 1, rate min 0, verified live), so the negative order lives in QBO. Same INTERNAL_PAY_SECRET + qbo_token lease pattern as `qbo-charge`; read-only `preview` mode; on create, instant-mirrors the credit memo into `ops.qbo_invoices` (txn_type='CreditMemo', same conflict keys as sync-qbo, which already mirrors credit memos). Caller passes catalog `qbo_item_id` lines. Deployed to the shared Supabase project (v1), verify_jwt=false. |
| 2026-07-09 | qbo writeback | **`qbo-returned-payment` edge function shipped (v1 void → v2 expense-swap same day).** Books a returned/bounced customer payment for brix-order's admin "Record returned payment" action using QBO's official bounced-payment bookkeeping: creates an Expense (Check) to the customer against A/R on the deposit bank account (the bank-feed match for Intuit's clawback), re-links the original Payment from the invoice to that expense (deposit stays reconciled, invoice reopens), and creates the $35 "Returned Payment Fee" invoice (item/account find-or-created). v1's plain void was replaced because it orphans both real bank events. Same INTERNAL_PAY_SECRET + qbo_token lease pattern as `qbo-charge`; read-only `preview` resolves the bank + A/R accounts; refuses (needs_manual) when the payment spans multiple invoices or the deposit bank can't be resolved. Deployed to the shared Supabase project (v2), verify_jwt=false. |
| 2026-06-28 | resq-sync decommission | **Legacy ResQ↔SF sync removed from this repo.** Deleted `public/sync.html` + 8 functions/libs (`resq-sf-sync`, `-background`, `-cron`, `resq-sf-links`, `sf-webhook`, `lib/resq-sf-links`, `lib/resq-job-notify`) and the 5-min `resq-sf-sync-cron` from `netlify.toml`. Replaced by the edge-function sync in **`skypace/apbg-resq-sync`** (already live). New **Master Control → ResQ Sync** panel (`control.html`) with two switches (Write mode, Sync enabled) + controls (drive a WO, run a tick, live status), backed by new `netlify/functions/resq-sync-control.mjs` → `ops.resq_sync_set_write/_set_active/_status` RPCs. Kept (still used elsewhere): `sf-helpers`, `sync-customers` + `ops.sync_customers` (now shared with apbg-resq-sync), `sf-oauth-callback`, `lib/sf-assets` (Brixpense receipts via `lib/sf-expense`), `resq-helpers` (health-watchdog), the Brixpense `sf-receipt-*`/`sf-expense-sweep` Phase-3 expense landing. Manifest: `resq-sf-sync` writer removed; `ops.resq_sf_links` + `ops.sync_events` moved to orphans (retained read-only). |
| 2026-07-22 | handbook v1.1 | **Architecture & Data section + automatic Change Log + one-button auto-update.** New handbook parts: *Architecture & Data* (`12-architecture-overview.md` — Mermaid diagrams of the system map, QBO-mirror pipeline, order flow, payment rails, ResQ sync, cron inventory; the viewer now renders ```mermaid fences) and a **Live Architecture Mirror** chapter (`handbook-architecture.mjs` fetches `Skilliosis_Mytosis_Architecture/ARCHITECTURE.md` from GitHub at view time — can't drift). New *Change Log* tab (`handbook-changelog.mjs` — commits across 12 APBG repos composed into a dated markdown feed; git is the logger, nothing hand-entered). Live chapters are `remote` entries in `manifest.json`; the viewer calls them with the `apbg_session` Bearer (admin/superadmin) and shows a sign-in notice otherwise. **Auto-update**: `handbook-autoupdate-background.mjs` — Master Control button; re-sweeps, has Claude (`HANDBOOK_UPDATE_MODEL`, default sonnet) rewrite each stale chapter against its changed sources, commits to a `handbook/auto-update-*` branch and opens a DRAFT PR — never merges (SOP-0 policy). Needs `GITHUB_WRITE_TOKEN` (or write-scoped `GITHUB_TOKEN`) + `ANTHROPIC_API_KEY`. Shared GitHub helpers in `netlify/functions/lib/handbook.mjs`; sweep refactored onto it and skips remote chapters. **Weekly drift cron**: `handbook-sweep-cron.mjs` (Mondays 15:00 UTC) re-runs the sweep and emails a stale-chapter digest to `HANDBOOK_ALERT_TO` (default service@brixbev.com) via the existing Resend helper; silent when fresh. ⚠ Env still needed on Netlify: `GITHUB_TOKEN` (classic PAT, `repo` scope — read for sweep/mirror/changelog, write enables Auto-update; or split write into `GITHUB_WRITE_TOKEN`). Known blocker: the ASM GitHub MCP stack-overflows on files >~60KB, so the companion ARCHITECTURE.md rows for this release are pinned in PR #307 pending manual apply / asm-mcp-tools fix. |
| 2026-07-22 | handbook | **APBG Master Handbook shipped.** 22 chapters in `docs/handbook/` — Part I Master User Guide (one chapter per app: gateway, brix-order portal + /admin, driver audit PWA, AI assistants, Refractor, Brixpense, AP tool, Master Control, Production, companion apps) + Part II Operations SOP Manual (SOP-0…9: governance, security/access, customer lifecycle, orders, billing/payments, cylinders, service/incidents, expenses, production, data/engineering; new policies drafted are marked `Draft policy — pending owner approval`). Multi-chapter HTML viewer at `public/docs/handbook/index.html` (hash routing, grouped sidebar, search, print). New **Master Control → APBG Handbook** panel + `handbook-sweep.mjs`: compares each chapter's manifest-registered source files' GitHub commit dates against `last_reviewed`, renders fresh/stale, and generates a copy-paste Claude update prompt per stale chapter (set `GITHUB_TOKEN` on Netlify for private-repo sources). Content sourced from the same KB the bots use + per-app guides; internal chapters may be RAG-ingested only with `customer_visible=false` (SOP-0). |
| 2026-07-21 | production redesign | **Formula-driven BOM → Work Order → PO pipeline (Production rebuilt).** The product spec sheet / formula is now the driver: new `ops.product_formulas` + `_ingredients` + `_revisions` (seeded with the seven Alameda Quantum Canning batching sheets — Hangar 25 Cola/Diet Cola, Cable Car Lemon-Lime, Lost Island Ginger Beer, Old Fountain Cream Soda, Golden Gate Orange, Oaktown Root Beer; % by weight, QC specs, batching instructions, revision history) + private Storage bucket **`product-formulas`** for the original spreadsheets. BOM became a pure parts list (`product_bom.formula_id`, `product_bom_lines.preferred_qbo_vendor_id`; new `fn_bom_save_v2`) — **all quantity/cost calcs moved to the work order**: `fn_wo_create_pipeline` snapshots per-vendor material requirements into new `ops.work_order_materials`; `fn_wo_generate_pos` creates ONE PO PER VENDOR from the WO (`purchase_orders.work_order_id`); `fn_wo_advance` drives the new status pipeline draft → ordered → at_copacker → in_production → yield_recorded (posts yield movement + cost snapshot incl. yield %, co-pack fee/freight) → in_transit (creates + ships a real BOL transfer co-packer → warehouse) → received (finished goods land in inventory) → closed. New `ops.work_order_events` audit trail + `v_work_orders` view; `v_purchase_orders` exposes the WO link. Legacy in-house consume/close flow retired (tables were empty live); Co-Pack Orders tab kept as "Co-Pack (Legacy)". Frontend: new Formulas & Spec Sheets tab (interactive batch scaler, printable batching sheet, attachment upload), rebuilt BOMs + Work Orders tabs (pipeline stepper, per-stage actions). Manifest/snapshot: all production tables registered (`brix-production:app-and-rpcs`, `push-qbo-item:vendors`). Migrations 20260721a/b applied live; pipeline smoke-tested end-to-end (rolled back). ⚠ RPC overload trap struck twice: `fn_create_transfer`/`fn_ship_transfer`/`fn_receive_transfer` have live legacy overloads — `fn_wo_advance` calls the longest signatures explicitly. |
